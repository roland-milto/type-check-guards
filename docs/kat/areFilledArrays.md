# areFilledArrays

## აღწერა

`areFilledArrays` ამოწმებს, არის თუ არა ორზომიანი მასივი არაცარიელი და არის თუ არა მისი ყველა ქვემასივი არაცარიელი.

### გამოყენების შემთხვევა

გამოიყენეთ `areFilledArrays` ტაბულარული ან მატრიცის მსგავსი შეყვანის (მაგ., CSV რიგები, ბადის მონაცემები, დაჯგუფებული
შედეგები) ვალიდაციისთვის, რათა უსაფრთხოდ ჩათვალოთ, რომ არსებობს მინიმუმ ერთი ქვემასივი და არცერთი ქვემასივი არ არის
ცარიელი.

> **შენიშვნა TypeScript მომხმარებლებისთვის:**
>
> გამოიყენეთ `areFilledArrays`, როცა გჭირდებათ დარწმუნდეთ, რომ 2D მასივს აქვს მინიმუმ ერთი რიგი და თითოეულ რიგს აქვს
> მინიმუმ ერთი ელემენტი, სანამ მასზე იტერაციას დაიწყებთ ან ინდექსით მიმართავთ.

### უპირატესობები

- ამოწმებს, რომ გარე მასივი არ არის ცარიელი და რომ ყოველი შიდა მასივიც არ არის ცარიელი; აბრუნებს `true`-ს მხოლოდ მაშინ,
  როცა ორივე პირობა სრულდება.
- მუშაობს ქვემასივებში არსებული ნებისმიერი ტიპის ელემენტებთან (მაგ., რიცხვები, სტრიქონები, ობიექტები, ჩადგმული
  მასივები), რადგან ამოწმებს მხოლოდ მასივის „შევსებულ“ სტატუსს და არა ელემენტების შინაარსს.
- იძლევა მარტივ ბულურ შედეგს (`true`/`false`), რომელიც გამოდგება დამცავ შემოწმებად ორზომიანი მონაცემების დამუშავებამდე.

## გამოყენება

### სინტაქსი

ფუნქცია:

- `areFilledArrays(array)`

პარამეტრები:

- `array`: შესამოწმებელი ორზომიანი მასივი.

### ფუნქციის ლოკალური იმპორტი

```ts
import { areFilledArrays } from "@type-check/guards";

const a: unknown[][] = [[1]];
const b: unknown[][] = [["a", "b"], [1, 2, 3]];
const c: unknown[][] = [[{}, []], ["string"]];
const d: unknown[][] = [[]];
const e: unknown[][] = [[], [1, 2, 3]];
const f: unknown[][] = [];

console.log(areFilledArrays(a)); // ჭეშმარიტი
console.log(areFilledArrays(b)); // ჭეშმარიტი
console.log(areFilledArrays(c)); // ჭეშმარიტი
console.log(areFilledArrays(d)); // მცდარი
console.log(areFilledArrays(e)); // მცდარი
console.log(areFilledArrays(f)); // მცდარი

```

### ობიექტის გლობალური იმპორტი

ფუნქციების, როგორც გლობალური ობიექტის მეთოდების, იმპორტისთვის გამოიყენეთ:

```ts
import "@type-check/guards/register-global-object.mjs";
```

შემდეგი მეთოდი გლობალურად იქნება ხელმისაწვდომი:

- `Type.areFilledArrays(array)`

## ფუნქციების ანალიზი

აქ დოკუმენტირებულია ცხრილური ანალიზი იმ შედეგის შესახებ, რომელიც მიიღება ფუნქციებში სხვადასხვა პარამეტრის
შეყვანისას: [areFilledArrays](../_analysis/areFilledArrays.md)

<br>

---

<small>ფაილი შეიქმნა 6 February 2026 at 11:57:26 (UTC)-ზე *
*[markdown-documentation-generator](https://github.com/roland-milto/markdown-documentation-generator)**-ის გამოყენებით,
ავტორი: **[Roland Milto](https://roland-milto.de/)**.</small>