# areNumbers

## აღწერა

`areNumbers` ამოწმებს, არის თუ არა მნიშვნელობა არაცარიელი მასივი, სადაც ყველა ელემენტი რიცხვია.

### გამოყენების შემთხვევა

მომხმარებლის მიერ მოწოდებული ან API-დან მიღებული მონაცემების ვალიდაცია, რათა დარწმუნდეთ, რომ ეს არის არაცარიელი
რიცხვების მასივი, სანამ გამოთვლით ჯამებს, საშუალოებს ან სხვა რიცხვით აგრეგაციებს.

> **შენიშვნა TypeScript მომხმარებლებისთვის:**
>
> `areNumbers` გამოიყენეთ უცნობი მასივების ვალიდაციისთვის რიცხვითი გამოთვლების შესრულებამდე; ის `false`-ს აბრუნებს
> ცარიელი მასივებისთვის და იმ მასივებისთვის, რომლებიც შეიცავს ნებისმიერ არარიცხვით მნიშვნელობას.

### უპირატესობები

- `true`-ს აბრუნებს მხოლოდ მაშინ, როცა შეყვანა არის არაცარიელი მასივი და ყოველი ელემენტი არის რიცხვი.
- ცრუ დადებით შედეგებს თავიდან იცილებს ცარიელი მასივებისა და არამასივური შეყვანების უარყოფით.
- გამოსადეგია როგორც დამცავი შემოწმება რიცხვით ოპერაციებამდე (მაგ., ჯამი, საშუალო) შესრულების დროის შეცდომების თავიდან
  ასაცილებლად.

## გამოყენება

### სინტაქსი

ფუნქცია:

- `areNumbers(array)`

პარამეტრები:

- `array`: მასივი, რომელიც უნდა შემოწმდეს რიცხვითი ელემენტების არსებობაზე.

### ფუნქციის ლოკალური იმპორტი

```ts
import { areNumbers } from "@type-check/guards";

const input: unknown = [1, 2, 3];

if (Array.isArray(input) && areNumbers(input)) {
  const total = input.reduce((sum, n) => sum + n, 0);
  console.log(total);
} else {
  console.log("Not a non-empty number array");
}

```

### ობიექტის გლობალური იმპორტი

ფუნქციების, როგორც გლობალური ობიექტის მეთოდების, იმპორტისთვის გამოიყენეთ:

```ts
import "@type-check/guards/register-global-object.mjs";
```

შემდეგი მეთოდი გლობალურად იქნება ხელმისაწვდომი:

- `Type.areNumbers(array)`

## ფუნქციების ანალიზი

აქ დოკუმენტირებულია ცხრილური ანალიზი იმ შედეგის შესახებ, რომელიც მიიღება ფუნქციებში სხვადასხვა პარამეტრის
შეყვანისას: [areNumbers](../_analysis/areNumbers.md)

<br>

---

<small>ფაილი შეიქმნა 30 January 2026 at 13:04:53 (UTC)-ზე *
*[markdown-documentation-generator](https://github.com/roland-milto/markdown-documentation-generator)**-ის გამოყენებით,
ავტორი: **[Roland Milto](https://roland-milto.de/)**.</small>