# isFilledArray

## აღწერა

ამოწმებს, არის თუ არა `value` მასივი მინიმუმ ერთი ელემენტით და აბრუნებს `true`-ს ან `false`-ს.

### გამოყენების შემთხვევა

გამოიყენეთ `isFilledArray` შემომავალი მონაცემების (მაგ., API payload-ები, ფორმის მნიშვნელობები, კონფიგურაცია)
ვალიდაციისთვის, სანამ იტერაციას დაიწყებთ, პირველ ელემენტს მიმართავთ ან გამოიყენებთ ლოგიკას, რომელსაც მინიმუმ ერთი
ელემენტი სჭირდება.

> **შენიშვნა TypeScript მომხმარებლებისთვის:**
>
> `isFilledArray` არის შესრულების დროის guard, რომელიც აბრუნებს ბულურ მნიშვნელობას; ის არ ავიწროებს ელემენტების ტიპებს
> იმის მიღმა, რომ ადასტურებს მასივი არაცარიელია.

### უპირატესობები

- `Array.isArray`-ისა და სიგრძის შემოწმების გამოყენებით არაცარიელი მასივის მარტივი და სწრაფი შემოწმება.
- ეხმარება თავიდან აიცილოს შესრულების დროის შეცდომები, როცა კოდი ვარაუდობს, რომ მასივს მაინც ერთი ელემენტი აქვს.
- გასაგები ბულური შედეგი: აბრუნებს `true`-ს არაცარიელი მასივებისთვის და `false`-ს სხვა შემთხვევაში.

## გამოყენება

### სინტაქსი

ფუნქცია:

- `isFilledArray(value)`

პარამეტრები:

- `value`: მნიშვნელობა, რომლის შემოწმებაც ხდება, არის თუ არა ის არაცარიელი მასივი.

### ფუნქციის ლოკალური იმპორტი

```ts
import { isFilledArray } from "@type-check/guards";

const input: unknown = [1, 2, 3];

if (Array.isArray(input) && isFilledArray(input)) {
  // input არის არაცარიელი მასივი შესრულების დროს
  console.log(input[0]);
} else {
  console.log("Not a non-empty array");
}

```

### ობიექტის გლობალური იმპორტი

ფუნქციების, როგორც გლობალური ობიექტის მეთოდების, იმპორტისთვის გამოიყენეთ:

```ts
import "@type-check/guards/register-global-object.mjs";
```

შემდეგი მეთოდი გლობალურად იქნება ხელმისაწვდომი:

- `Type.isFilledArray(value)`

## ფუნქციების ანალიზი

აქ დოკუმენტირებულია ცხრილური ანალიზი იმ შედეგის შესახებ, რომელიც მიიღება ფუნქციებში სხვადასხვა პარამეტრის
შეყვანისას: [isFilledArray](../_analysis/isFilledArray.md)

<br>

---

<small>ფაილი შეიქმნა 6 February 2026 at 11:47:40 (UTC)-ზე *
*[markdown-documentation-generator](https://github.com/roland-milto/markdown-documentation-generator)**-ის გამოყენებით,
ავტორი: **[Roland Milto](https://roland-milto.de/)**.</small>