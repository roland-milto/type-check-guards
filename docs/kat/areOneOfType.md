# areOneOfType

## აღწერა

`areOneOfType` ამოწმებს, არის თუ არა არაცარიელ მასივში ყველა ელემენტი მითითებული runtime ტიპებიდან ერთ-ერთი.

### გამოყენების შემთხვევა

შეამოწმეთ შემომავალი მონაცემები (მაგ., დამუშავებული JSON), სადაც ველი უნდა იყოს არაცარიელი მასივი, რომლის ელემენტებიც
შეზღუდულია პრიმიტიული ტიპების ცნობილ ნაკრებზე; დააბრუნეთ `false`, როცა მასივი ცარიელია ან შეიცავს რომელიმე დაუშვებელ
ტიპს.

> **შენიშვნა TypeScript მომხმარებლებისთვის:**
>
> ეს ფუნქცია აბრუნებს boolean-ს და კომპილაციის დროს არ ავიწროებს მასივის ელემენტების ტიპებს; გამოიყენეთ როგორც runtime
> ვალიდაციის ნაბიჯი შემდგომ დამუშავებამდე.

### უპირატესობები

- უზრუნველყოფს, რომ მასივში ყოველი ელემენტი ემთხვეოდეს მინიმუმ ერთ დაშვებულ runtime ტიპს და აბრუნებს `true`-ს მხოლოდ
  მაშინ, როცა მთლიანი მასივი გაივლის შემოწმებას.
- ადრეულად უარყოფს არასწორ შეყვანებს: აბრუნებს `false`-ს, როცა `array` ან `types` ცარიელია ან არ არის შევსებული მასივი.
- სასარგებლოა შერეული ტიპების კოლექციების (მაგ., რიცხვები და სტრიქონები) ვალიდაციისთვის `areOneOfType`-ზე ერთი
  გამოძახებით.

## გამოყენება

### სინტაქსი

ფუნქცია:

- `areOneOfType(array, types)`

პარამეტრები:

- `array`: ელემენტების მასივი, რომელიც უნდა შემოწმდეს მოწოდებული ტიპების მიმართ.
- `types`: სტრიქონების მასივი, რომელიც წარმოადგენს მონაცემთა ტიპებს, რომელთა მიმართაც უნდა მოხდეს შემოწმება.

### ფუნქციის ლოკალური იმპორტი

```ts
import { areOneOfType } from "@type-check/guards";

const values1: unknown[] = [1, "a", true];
const ok1 = areOneOfType(values1, ["number", "string", "boolean"]);

const values2: unknown[] = [1, "a", {}];
const ok2 = areOneOfType(values2, ["number", "string"]);

const values3: unknown[] = [];
const ok3 = areOneOfType(values3, ["number"]);

console.log({ ok1, ok2, ok3 });
```

### ობიექტის გლობალური იმპორტი

ფუნქციების, როგორც გლობალური ობიექტის მეთოდების, იმპორტისთვის გამოიყენეთ:

```ts
import "@type-check/guards/register-global-object.mjs";
```

შემდეგი მეთოდი გლობალურად იქნება ხელმისაწვდომი:

- `Type.areOneOfType(array, types)`

## ფუნქციების ანალიზი

აქ დოკუმენტირებულია ცხრილური ანალიზი იმ შედეგის შესახებ, რომელიც მიიღება ფუნქციებში სხვადასხვა პარამეტრის
შეყვანისას: [areOneOfType](../_analysis/areOneOfType.md)

<br>

---

<small>ფაილი შეიქმნა 31 January 2026 at 23:37:26 (UTC)-ზე *
*[markdown-documentation-generator](https://github.com/roland-milto/markdown-documentation-generator)**-ის გამოყენებით,
ავტორი: **[Roland Milto](https://roland-milto.de/)**.</small>