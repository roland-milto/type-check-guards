# areWeakMaps

## აღწერა

`areWeakMaps` ამოწმებს, არის თუ არა მნიშვნელობა არაცარიელი მასივი, სადაც ყოველი ელემენტი არის `WeakMap`; `true`-ს
აბრუნებს მხოლოდ ამ შემთხვევაში და სხვა შემთხვევაში `false`-ს.

### გამოყენების შემთხვევა

დაავალიდირეთ runtime მონაცემები (მაგ., დამუშავებული JSON, პლაგინის შეყვანები ან სუსტად ტიპიზებული კონფიგურაცია), რათა
დარწმუნდეთ, რომ ეს არის `WeakMap`-ის ინსტანციების არაცარიელი მასივი, სანამ იტერაციას დაიწყებთ და `WeakMap`-ის მეთოდებს
გამოიძახებთ; აბრუნებს `false`-ს, თუ რომელიმე ელემენტი არ არის `WeakMap` ან თუ მასივი ცარიელია.

> **შენიშვნა TypeScript მომხმარებლებისთვის:**
>
> გამოიყენეთ `areWeakMaps` უცნობი შეყვანის ვალიდაციისთვის, სანამ მას არაცარიელ `WeakMap[]`-ად მიიჩნევთ; ცარიელი
> მასივებისთვის ის აბრუნებს `false`-ს.

### უპირატესობები

- უზრუნველყოფს, რომ მოწოდებულ მასივში ყოველი ელემენტი იყოს `WeakMap`-ის ინსტანცია.
- ცარიელი მასივებისთვის აბრუნებს `false`-ს, რაც ხელს უშლის „მონაცემები არ არის“-ის შემთხვევით მიღებას ვალიდურ შეყვანად.
- სასარგებლოა როგორც დამცავი შემოწმება, სანამ ყველა ელემენტზე `WeakMap`-ზე სპეციფიკურ ოპერაციებს შეასრულებთ.

## გამოყენება

### სინტაქსი

ფუნქცია:

- `areWeakMaps(array)`

პარამეტრები:

- `array`: მასივი, რომელიც უნდა შემოწმდეს `WeakMap`-ის ინსტანციებზე.

### ფუნქციის ლოკალური იმპორტი

```ts
import { areWeakMaps } from "@type-check/guards";

const list: unknown[] = [new WeakMap<object, number>(), new WeakMap<object, number>()];

if (areWeakMaps(list)) {
  // სია არის WeakMap-ის ინსტანციების არაცარიელი მასივი
  for (const wm of list) {
    wm.set({}, 1);
  }
} else {
  // არაა არაცარიელი WeakMap[]
}

```

### ობიექტის გლობალური იმპორტი

ფუნქციების, როგორც გლობალური ობიექტის მეთოდების, იმპორტისთვის გამოიყენეთ:

```ts
import "@type-check/guards/register-global-object.mjs";
```

შემდეგი მეთოდი გლობალურად იქნება ხელმისაწვდომი:

- `Type.areWeakMaps(array)`

## ფუნქციების ანალიზი

აქ დოკუმენტირებულია ცხრილური ანალიზი იმ შედეგის შესახებ, რომელიც მიიღება ფუნქციებში სხვადასხვა პარამეტრის
შეყვანისას: [areWeakMaps](../_analysis/areWeakMaps.md)

<br>

---

<small>ფაილი შეიქმნა 30 January 2026 at 13:37:56 (UTC)-ზე *
*[markdown-documentation-generator](https://github.com/roland-milto/markdown-documentation-generator)**-ის გამოყენებით,
ავტორი: **[Roland Milto](https://roland-milto.de/)**.</small>