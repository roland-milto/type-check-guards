# Références de fonctions

## Fonctions 

| Fonction                                    | Description                                                                                                                                                                                                                 |
|---------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| [areArrays](areArrays.md)                   | `areArrays` détermine si une valeur est un tableau bidimensionnel rempli dont tous les éléments sont des tableaux.                                                                                                          |
| [areBigInts](areBigInts.md)                 | `areBigInts` détermine si une valeur est un tableau non vide ne contenant que des valeurs `bigint`.                                                                                                                         |
| [areBinaries](areBinaries.md)               | Vérifie si la valeur fournie est un tableau non vide de chaînes binaires valides et renvoie `true` uniquement si tous les éléments passent la validation.                                                                   |
| [areBooleans](areBooleans.md)               | `areBooleans` vérifie si un tableau non vide donné ne contient que des valeurs booléennes, en renvoyant `true` si c’est le cas et `false` sinon.                                                                            |
| [areBuffers](areBuffers.md)                 | `areBuffers` vérifie si la valeur fournie est un tableau non vide et rempli dont chaque élément est un `Buffer`, en renvoyant `true` si c’est le cas et `false` sinon.                                                      |
| [areDates](areDates.md)                     | `areDates` détermine si un tableau donné est rempli et ne contient que des objets `Date`, en renvoyant `true` uniquement lorsque tous les éléments sont des dates valides.                                                  |
| [areDecimals](areDecimals.md)               | Vérifie si tous les éléments d’un tableau sont des nombres décimaux et si le tableau est rempli, en renvoyant `true` ou `false`.                                                                                            |
| [areEqual](areEqual.md)                     | `areEqual` vérifie si tous les éléments d’un tableau sont égaux à une valeur attendue donnée, en renvoyant `true` uniquement pour les tableaux non vides dont chaque élément correspond.                                    |
| [areErrors](areErrors.md)                   | Vérifie si un tableau n’est pas vide et ne contient que des objets `Error`, en renvoyant `true` ou `false`.                                                                                                                 |
| [areFalse](areFalse.md)                     | `areFalse` vérifie si tous les éléments d’un tableau fourni sont strictement le booléen `false`.                                                                                                                            |
| [areFilledArrays](areFilledArrays.md)       | `areFilledArrays` vérifie si un tableau bidimensionnel n’est pas vide et si tous ses sous-tableaux ne sont pas vides.                                                                                                       |
| [areFinite](areFinite.md)                   | `areFinite` vérifie si une valeur est un tableau non vide dont les éléments sont tous des nombres finis, en renvoyant `true` si c’est le cas et `false` sinon.                                                              |
| [areFloats](areFloats.md)                   | `areFloats` vérifie si un tableau donné est rempli et si tous ses éléments sont des flottants.                                                                                                                              |
| [areHexadecimals](areHexadecimals.md)       | Vérifie si tous les éléments d’un tableau sont des chaînes hexadécimales, en renvoyant `true` uniquement pour les tableaux non vides où chaque élément est valide.                                                          |
| [areIndexesFound](areIndexesFound.md)       | `areIndexesFound` vérifie si une valeur est un tableau non vide dont les éléments sont tous des index valides, en renvoyant `true` si c’est le cas et `false` sinon.                                                        |
| [areIntegers](areIntegers.md)               | `areIntegers` détermine si tous les éléments d’un tableau donné sont des entiers, en renvoyant `true` si c’est le cas et `false` sinon.                                                                                     |
| [areJson](areJson.md)                       | Vérifie si tous les éléments d’un tableau sont des chaînes JSON, en renvoyant `true` uniquement si le tableau est rempli et que chaque élément est un JSON valide ; sinon renvoie `false`.                                  |
| [areMaps](areMaps.md)                       | `areMaps` détermine si un tableau donné est non vide et si tous ses éléments sont des instances de `Map`.                                                                                                                   |
| [areNaNs](areNaNs.md)                       | `areNaNs` vérifie si tous les éléments d’un tableau sont `NaN` et renvoie `true` uniquement si chaque élément est `NaN`.                                                                                                    |
| [areNullOrUndefined](areNullOrUndefined.md) | Vérifie si tous les éléments du tableau donné sont `null` ou `undefined`.                                                                                                                                                   |
| [areNumbers](areNumbers.md)                 | `areNumbers` vérifie si une valeur est un tableau non vide dont tous les éléments sont des nombres.                                                                                                                         |
| [areNumerics](areNumerics.md)               | `areNumerics` vérifie si une valeur est un tableau non vide dont tous les éléments sont numériques.                                                                                                                         |
| [areObjects](areObjects.md)                 | `areObjects` vérifie si un tableau non vide fourni ne contient que des objets.                                                                                                                                              |
| [areOctals](areOctals.md)                   | `areOctals` détermine si la valeur fournie est un tableau non vide de chaînes octales valides.                                                                                                                              |
| [areOfType](areOfType.md)                   | Vérifie si tous les éléments du `array` donné sont du `type` spécifié.                                                                                                                                                      |
| [areOneOfType](areOneOfType.md)             | `areOneOfType` vérifie si tous les éléments d’un tableau non vide appartiennent à l’un des types d’exécution spécifiés.                                                                                                     |
| [arePlainObjects](arePlainObjects.md)       | Vérifie si tous les éléments d’un tableau sont des objets simples, en renvoyant `true` uniquement si chaque élément est admissible.                                                                                         |
| [arePrimitives](arePrimitives.md)           | `arePrimitives` évalue si tous les éléments d’un tableau fourni et non vide sont des types primitifs.                                                                                                                       |
| [arePromises](arePromises.md)               | `arePromises` détermine si tous les éléments d’un tableau sont des instances de `Promise`.                                                                                                                                  |
| [areRegExes](areRegExes.md)                 | `areRegExes` vérifie si une valeur est un tableau rempli ne contenant que des objets `RegExp`.                                                                                                                              |
| [areSets](areSets.md)                       | Vérifie si un tableau non vide donné ne contient que des instances de `Set`, en renvoyant `true` si c’est le cas et `false` sinon.                                                                                          |
| [areStreams](areStreams.md)                 | `areStreams` détermine si une valeur est un tableau non vide dans lequel chaque élément est un `Stream`.                                                                                                                    |
| [areStrings](areStrings.md)                 | `areStrings` vérifie si un tableau est non vide et si tous ses éléments sont des chaînes de caractères, en renvoyant `true` uniquement dans ce cas.                                                                         |
| [areSymbols](areSymbols.md)                 | Vérifie si une entrée est un tableau rempli dont les éléments sont tous des symboles, et renvoie `true` ou `false`.                                                                                                         |
| [areTrue](areTrue.md)                       | Vérifie si un tableau non vide ne contient que des valeurs booléennes `true`.                                                                                                                                               |
| [areUndefined](areUndefined.md)             | `areUndefined` vérifie si chaque élément d’un tableau fourni est `undefined`.                                                                                                                                               |
| [areValidDates](areValidDates.md)           | Détermine si un tableau est non vide et composé exclusivement d’objets `Date` valides.                                                                                                                                      |
| [areWeakMaps](areWeakMaps.md)               | `areWeakMaps` vérifie si une valeur est un tableau non vide dont chaque élément est un `WeakMap`, en renvoyant `true` uniquement dans ce cas et `false` sinon.                                                              |
| [areWeakSets](areWeakSets.md)               | Vérifie si une entrée est un tableau non vide dont chaque élément est un `WeakSet`, et ne renvoie `true` que dans ce cas.                                                                                                   |
| [getTypeOf](getTypeOf.md)                   | `getTypeOf` renvoie un libellé de type détaillé et lisible par des humains pour une valeur donnée, y compris des types numériques affinés et des catégories d’objets spécifiques.                                           |
| [isArray](isArray.md)                       | `isArray` vérifie si une valeur donnée est un tableau et renvoie `true` si c’est le cas, sinon `false`.                                                                                                                     |
| [isBigInt](isBigInt.md)                     | `isBigInt` vérifie si une valeur donnée est de type `bigint`, en renvoyant `true` pour les primitives BigInt et `false` sinon.                                                                                              |
| [isBinary](isBinary.md)                     | `isBinary` détermine si une valeur est une chaîne binaire (éventuellement préfixée par `0b`/`0B`) et renvoie `true` ou `false`.                                                                                             |
| [isBoolean](isBoolean.md)                   | Détermine si une valeur donnée est un `boolean`.                                                                                                                                                                            |
| [isBuffer](isBuffer.md)                     | Vérifie si une valeur est un `Buffer` Node.js et renvoie `true` ou `false`.                                                                                                                                                 |
| [isDate](isDate.md)                         | `isDate` détermine si une valeur fournie est un `Date`, renvoyant `true` pour les instances de `Date` et `false` sinon.                                                                                                     |
| [isDecimal](isDecimal.md)                   | `isDecimal` vérifie si une valeur est une représentation décimale sous forme de chaîne, sans espaces en début/fin et avec un format décimal valide.                                                                         |
| [isEmpty](isEmpty.md)                       | Détermine si une valeur donnée est vide, en renvoyant `true` pour `null`, `undefined`, les chaînes vides/composées d’espaces, les tableaux vides, les `Map`/`Set` vides, ou les objets sans propriétés énumérables propres. |
| [isError](isError.md)                       | Vérifie si une `value` donnée est une instance de `Error`.                                                                                                                                                                  |
| [isFalse](isFalse.md)                       | `isFalse` vérifie si une valeur donnée est strictement égale au littéral booléen `false`.                                                                                                                                   |
| [isFilledArray](isFilledArray.md)           | Vérifie si `value` est un tableau contenant au moins un élément, en renvoyant `true` ou `false`.                                                                                                                            |
| [isFinite](isFinite.md)                     | Détermine si une `value` donnée est un `number` fini.                                                                                                                                                                       |
| [isFloat](isFloat.md)                       | `isFloat` détermine si une `value` donnée est un nombre à virgule flottante fini (un `number` qui n’est pas un entier).                                                                                                     |
| [isHexadecimal](isHexadecimal.md)           | `isHexadecimal` vérifie si une valeur donnée est un littéral de chaîne hexadécimale avec un préfixe `0x`/`0X` obligatoire.                                                                                                  |
| [isIndexFound](isIndexFound.md)             | `isIndexFound` détermine si une valeur donnée est un entier non négatif, indiquant qu’un index a été trouvé.                                                                                                                |
| [isInteger](isInteger.md)                   | Détermine si une `value` donnée est un nombre entier sûr.                                                                                                                                                                   |
| [isMap](isMap.md)                           | Vérifie si la `value` donnée est une `Map`, en renvoyant `true` si c’est le cas et `false` sinon.                                                                                                                           |
| [isNaN](isNaN.md)                           | Détermine si la `value` fournie est un `NaN` de type `number` sans convertir les chaînes.                                                                                                                                   |
| [isNull](isNull.md)                         | Détermine si la `value` fournie est `null`.                                                                                                                                                                                 |
| [isNullOrUndefined](isNullOrUndefined.md)   | Vérifie si une valeur donnée est `null` ou `undefined`.                                                                                                                                                                     |
| [isNumber](isNumber.md)                     | `isNumber` vérifie si une valeur est un nombre fini, non `NaN`.                                                                                                                                                             |
| [isNumeric](isNumeric.md)                   | `isNumeric` détermine si une `value` donnée est considérée comme numérique en comparant son type résolu à `NUMERIC_TYPES`.                                                                                                  |
| [isObject](isObject.md)                     | Détermine si une `value` donnée est un `object` (en excluant `null`).                                                                                                                                                       |
| [isOctal](isOctal.md)                       | Détermine si une valeur est une chaîne de littéral octal valide (p. ex. `0o755`).                                                                                                                                           |
| [isOfType](isOfType.md)                     | Détermine si une `value` donnée correspond à une chaîne de type spécifiée, en utilisant `typeof` pour les primitifs et un mécanisme de secours pour les types complexes.                                                    |
| [isOneOfType](isOneOfType.md)               | `isOneOfType` détermine si une `value` donnée correspond à au moins une des chaînes de type fournies, en renvoyant `true` si une correspondance est trouvée et `false` sinon.                                               |
| [isPlainObject](isPlainObject.md)           | Vérifie si la `value` donnée est un objet simple et renvoie `true` si c’est le cas, sinon `false`.                                                                                                                          |
| [isPrimitive](isPrimitive.md)               | `isPrimitive` détermine si une valeur donnée est un primitif (`null`, `undefined`, `boolean`, `number`, `string`, `bigint`, `symbol`).                                                                                      |
| [isPromise](isPromise.md)                   | Détermine si une valeur donnée est une `Promise`.                                                                                                                                                                           |
| [isRegEx](isRegEx.md)                       | Détermine si une valeur fournie est une instance de `RegExp`.                                                                                                                                                               |
| [isSet](isSet.md)                           | Détermine si une valeur donnée est un `Set`.                                                                                                                                                                                |
| [isStream](isStream.md)                     | `isStream` vérifie si une valeur donnée est un objet flux (de type flux Node.js, `ReadableStream` ou `WritableStream`).                                                                                                     |
| [isString](isString.md)                     | `isString` détermine si une valeur donnée est une chaîne de caractères.                                                                                                                                                     |
| [isSymbol](isSymbol.md)                     | `isSymbol` détermine si une valeur donnée est de type `symbol`, en renvoyant `true` pour les symboles et `false` sinon.                                                                                                     |
| [isTrue](isTrue.md)                         | `isTrue` détermine si une valeur donnée est strictement égale à `true`.                                                                                                                                                     |
| [isUndefined](isUndefined.md)               | Vérifie si une valeur donnée est `undefined`.                                                                                                                                                                               |
| [isValidDate](isValidDate.md)               | `isValidDate` vérifie si une valeur donnée est un objet `Date` valide et renvoie `true` uniquement pour de vraies dates non invalides.                                                                                      |
| [isWeakMap](isWeakMap.md)                   | Détermine si une `value` donnée est une instance de `WeakMap`.                                                                                                                                                              |
| [isWeakSet](isWeakSet.md)                   | Détermine si la `value` donnée est un `WeakSet` d’objets.                                                                                                                                                                   |

## Fonctions comparées

| Fonctions  | Description |
|------------|-------------|
