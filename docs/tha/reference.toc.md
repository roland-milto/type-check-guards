# การอ้างอิงฟังก์ชัน

## ฟังก์ชัน

| ฟังก์ชัน                                    | คำอธิบาย                                                                                                                                                                                           |
|---------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| [areArrays](areArrays.md)                   | `areArrays` ใช้กำหนดว่าค่าหนึ่งเป็นอาร์เรย์สองมิติที่มีข้อมูล (ไม่ว่าง) และรายการภายในทั้งหมดเป็นอาร์เรย์หรือไม่                                                                                   |
| [areBigInts](areBigInts.md)                 | `areBigInts` ใช้กำหนดว่าค่าหนึ่งเป็นอาร์เรย์ที่ไม่ว่างและมีเฉพาะค่า `bigint` เท่านั้นหรือไม่                                                                                                       |
| [areBinaries](areBinaries.md)               | ตรวจสอบว่าค่าที่ให้มาเป็นอาร์เรย์ที่ไม่ว่างเปล่าของสตริงไบนารีที่ถูกต้องหรือไม่ และจะคืนค่า `true` ก็ต่อเมื่อทุกรายการผ่านการตรวจสอบ                                                               |
| [areBooleans](areBooleans.md)               | `areBooleans` ตรวจสอบว่าอาร์เรย์ที่ไม่ว่างเปล่าที่กำหนดมีเฉพาะค่าบูลีนเท่านั้น โดยคืนค่า `true` หากเป็นเช่นนั้น และคืนค่า `false` หากไม่ใช่                                                        |
| [areBuffers](areBuffers.md)                 | `areBuffers` ตรวจสอบว่าค่าที่ให้มาเป็นอาร์เรย์ที่ไม่ว่างและมีสมาชิกครบถ้วน โดยทุกองค์ประกอบเป็น `Buffer` หรือไม่ หากใช่จะคืนค่า `true` มิฉะนั้นคืนค่า `false`                                      |
| [areDates](areDates.md)                     | `areDates` ใช้กำหนดว่าอาร์เรย์ที่กำหนดมีข้อมูลและมีเฉพาะอ็อบเจ็กต์ `Date` เท่านั้น โดยจะคืนค่า `true` ก็ต่อเมื่อทุกองค์ประกอบเป็นวันที่ที่ถูกต้อง                                                  |
| [areDecimals](areDecimals.md)               | ตรวจสอบว่าองค์ประกอบทั้งหมดในอาร์เรย์เป็นตัวเลขทศนิยมและอาร์เรย์มีข้อมูล โดยคืนค่า `true` หรือ `false`                                                                                             |
| [areEqual](areEqual.md)                     | `areEqual` ตรวจสอบว่าองค์ประกอบทั้งหมดในอาร์เรย์เท่ากับค่าที่คาดหวังที่กำหนดหรือไม่ โดยจะคืนค่า `true` เฉพาะสำหรับอาร์เรย์ที่ไม่ว่างซึ่งทุกรายการตรงกันเท่านั้น                                    |
| [areErrors](areErrors.md)                   | ตรวจสอบว่าอาร์เรย์ไม่ว่างและมีเฉพาะอ็อบเจ็กต์ `Error` เท่านั้น โดยคืนค่า `true` หรือ `false`                                                                                                       |
| [areFalse](areFalse.md)                     | `areFalse` ตรวจสอบว่าองค์ประกอบทั้งหมดในอาร์เรย์ที่ให้มาเป็นบูลีน `false` อย่างเคร่งครัดหรือไม่.                                                                                                   |
| [areFilledArrays](areFilledArrays.md)       | `areFilledArrays` ตรวจสอบว่าอาร์เรย์สองมิติไม่ว่าง และอาร์เรย์ย่อยทั้งหมดไม่ว่าง                                                                                                                   |
| [areFinite](areFinite.md)                   | `areFinite` ตรวจสอบว่าค่าหนึ่งเป็นอาร์เรย์ที่ไม่ว่างซึ่งองค์ประกอบทั้งหมดเป็นตัวเลขที่เป็นจำนวนจำกัด (finite) หรือไม่ โดยจะคืนค่า `true` หากเป็นเช่นนั้น และคืนค่า `false` ในกรณีอื่น              |
| [areFloats](areFloats.md)                   | `areFloats` ตรวจสอบว่าอาร์เรย์ที่กำหนดมีข้อมูลและทุกองค์ประกอบเป็น float หรือไม่                                                                                                                   |
| [areHexadecimals](areHexadecimals.md)       | ตรวจสอบว่าองค์ประกอบทั้งหมดในอาร์เรย์เป็นสตริงเลขฐานสิบหกหรือไม่ โดยจะคืนค่า `true` เฉพาะสำหรับอาร์เรย์ที่ไม่ว่างและทุกรายการถูกต้อง                                                               |
| [areIndexesFound](areIndexesFound.md)       | `areIndexesFound` ตรวจสอบว่าค่าเป็นอาร์เรย์ที่ไม่ว่างซึ่งองค์ประกอบทั้งหมดเป็นดัชนีที่ถูกต้องหรือไม่ โดยจะคืนค่า `true` หากใช่ และ `false` หากไม่ใช่                                               |
| [areIntegers](areIntegers.md)               | `areIntegers` ใช้กำหนดว่าทุกองค์ประกอบในอาร์เรย์ที่กำหนดเป็นจำนวนเต็มทั้งหมดหรือไม่ โดยจะคืนค่า `true` หากใช่ และคืนค่า `false` หากไม่ใช่                                                          |
| [areJson](areJson.md)                       | ตรวจสอบว่าองค์ประกอบทั้งหมดของอาร์เรย์เป็นสตริง JSON หรือไม่ โดยจะคืนค่า `true` ก็ต่อเมื่ออาร์เรย์มีข้อมูลและทุกรายการเป็น JSON ที่ถูกต้อง; มิฉะนั้นจะคืนค่า `false`                               |
| [areMaps](areMaps.md)                       | `areMaps` ใช้กำหนดว่าอาร์เรย์ที่กำหนดนั้นไม่ว่างและองค์ประกอบทั้งหมดเป็นอินสแตนซ์ของ `Map` หรือไม่                                                                                                 |
| [areNaNs](areNaNs.md)                       | `areNaNs` ตรวจสอบว่าองค์ประกอบทั้งหมดในอาร์เรย์เป็น `NaN` หรือไม่ และจะคืนค่า `true` ก็ต่อเมื่อทุกองค์ประกอบเป็น `NaN` เท่านั้น.                                                                   |
| [areNullOrUndefined](areNullOrUndefined.md) | ตรวจสอบว่าองค์ประกอบทั้งหมดในอาร์เรย์ที่กำหนดเป็น `null` หรือ `undefined` หรือไม่                                                                                                                  |
| [areNumbers](areNumbers.md)                 | `areNumbers` ตรวจสอบว่าค่าเป็นอาร์เรย์ที่ไม่ว่างซึ่งทุกองค์ประกอบเป็นตัวเลขหรือไม่                                                                                                                 |
| [areNumerics](areNumerics.md)               | `areNumerics` ตรวจสอบว่าค่าเป็นอาร์เรย์ที่ไม่ว่างซึ่งทุกองค์ประกอบเป็นตัวเลขหรือไม่                                                                                                                |
| [areObjects](areObjects.md)                 | `areObjects` ตรวจสอบว่าอาร์เรย์ที่มีข้อมูล (ไม่ว่าง) ที่ให้มามีเฉพาะอ็อบเจ็กต์เท่านั้นหรือไม่                                                                                                      |
| [areOctals](areOctals.md)                   | `areOctals` ใช้กำหนดว่าค่าที่ให้มาเป็นอาร์เรย์ที่ไม่ว่างเปล่าของสตริงเลขฐานแปดที่ถูกต้องหรือไม่                                                                                                    |
| [areOfType](areOfType.md)                   | ตรวจสอบว่าองค์ประกอบทั้งหมดใน `array` ที่กำหนดเป็นชนิด `type` ที่ระบุหรือไม่                                                                                                                       |
| [areOneOfType](areOneOfType.md)             | `areOneOfType` ตรวจสอบว่าองค์ประกอบทั้งหมดในอาร์เรย์ที่ไม่ว่างเปล่าเป็นหนึ่งในชนิดข้อมูลขณะรันไทม์ที่ระบุหรือไม่                                                                                   |
| [arePlainObjects](arePlainObjects.md)       | ตรวจสอบว่าองค์ประกอบทั้งหมดของอาร์เรย์เป็นอ็อบเจ็กต์แบบ plain หรือไม่ โดยจะคืนค่า `true` ก็ต่อเมื่อทุกองค์ประกอบผ่านเกณฑ์                                                                          |
| [arePrimitives](arePrimitives.md)           | `arePrimitives` ประเมินว่าองค์ประกอบทั้งหมดในอาร์เรย์ที่ระบุและไม่ว่างเป็นชนิดค่าปฐมภูมิหรือไม่                                                                                                    |
| [arePromises](arePromises.md)               | `arePromises` ใช้กำหนดว่าองค์ประกอบทั้งหมดในอาร์เรย์เป็นอินสแตนซ์ของ `Promise` หรือไม่                                                                                                             |
| [areRegExes](areRegExes.md)                 | `areRegExes` ตรวจสอบว่าค่าเป็นอาร์เรย์ที่มีข้อมูลและมีเฉพาะอ็อบเจ็กต์ `RegExp` เท่านั้นหรือไม่                                                                                                     |
| [areSets](areSets.md)                       | ตรวจสอบว่าอาร์เรย์ที่ไม่ว่างที่กำหนดมีเฉพาะอินสแตนซ์ของ `Set` เท่านั้นหรือไม่ โดยคืนค่า `true` หากใช่ และ `false` หากไม่ใช่                                                                        |
| [areStreams](areStreams.md)                 | `areStreams` ใช้กำหนดว่าค่าหนึ่งเป็นอาร์เรย์ที่มีข้อมูล (ไม่ว่าง) ซึ่งทุกองค์ประกอบเป็น `Stream` หรือไม่                                                                                           |
| [areStrings](areStrings.md)                 | `areStrings` ตรวจสอบว่าอาร์เรย์ไม่ว่างและองค์ประกอบทั้งหมดเป็นสตริง โดยจะคืนค่า `true` เฉพาะในกรณีนั้นเท่านั้น                                                                                     |
| [areSymbols](areSymbols.md)                 | ตรวจสอบว่าอินพุตเป็นอาร์เรย์ที่มีข้อมูลและองค์ประกอบทั้งหมดเป็นสัญลักษณ์หรือไม่ โดยคืนค่า `true` หรือ `false`                                                                                      |
| [areTrue](areTrue.md)                       | ตรวจสอบว่าอาร์เรย์ที่ไม่ว่างเปล่ามีเฉพาะค่าบูลีน `true` เท่านั้นหรือไม่                                                                                                                            |
| [areUndefined](areUndefined.md)             | `areUndefined` ตรวจสอบว่าองค์ประกอบทุกตัวในอาร์เรย์ที่ให้มาเป็น `undefined` หรือไม่                                                                                                                |
| [areValidDates](areValidDates.md)           | กำหนดว่าอาร์เรย์ไม่ว่างและประกอบด้วยอ็อบเจ็กต์ `Date` ที่ถูกต้องทั้งหมดหรือไม่                                                                                                                     |
| [areWeakMaps](areWeakMaps.md)               | `areWeakMaps` ตรวจสอบว่าค่าเป็นอาร์เรย์ที่ไม่ว่างและทุกองค์ประกอบเป็น `WeakMap` หรือไม่ โดยจะคืนค่า `true` เฉพาะในกรณีนั้น และคืนค่า `false` ในกรณีอื่น                                            |
| [areWeakSets](areWeakSets.md)               | ตรวจสอบว่าอินพุตเป็นอาร์เรย์ที่ไม่ว่างและทุกองค์ประกอบเป็น `WeakSet` หรือไม่ โดยจะคืนค่า `true` เฉพาะในกรณีนั้นเท่านั้น                                                                            |
| [getTypeOf](getTypeOf.md)                   | `getTypeOf` ส่งคืนป้ายกำกับชนิดข้อมูลที่ละเอียดและอ่านเข้าใจได้สำหรับค่าที่กำหนด รวมถึงชนิดตัวเลขที่ปรับละเอียดและชนิดอ็อบเจ็กต์ที่เฉพาะเจาะจง                                                     |
| [isArray](isArray.md)                       | `isArray` ตรวจสอบว่าค่าที่กำหนดเป็นอาร์เรย์หรือไม่ และคืนค่า `true` หากเป็นอาร์เรย์ มิฉะนั้นคืนค่า `false`                                                                                         |
| [isBigInt](isBigInt.md)                     | `isBigInt` ตรวจสอบว่าค่าที่กำหนดเป็นชนิด `bigint` หรือไม่ โดยคืนค่า `true` สำหรับ BigInt แบบ primitive และคืนค่า `false` ในกรณีอื่น                                                                |
| [isBinary](isBinary.md)                     | `isBinary` ใช้ตรวจสอบว่าค่าเป็นสตริงไบนารีหรือไม่ (อาจมีคำนำหน้า `0b`/`0B`) และคืนค่า `true` หรือ `false`                                                                                          |
| [isBoolean](isBoolean.md)                   | กำหนดว่าค่าที่กำหนดเป็น `boolean` หรือไม่.                                                                                                                                                         |
| [isBuffer](isBuffer.md)                     | ตรวจสอบว่าค่าเป็น `Buffer` ของ Node.js หรือไม่ และคืนค่า `true` หรือ `false`                                                                                                                       |
| [isDate](isDate.md)                         | `isDate` ใช้ตรวจสอบว่าค่าที่ให้มาเป็น `Date` หรือไม่ โดยคืนค่า `true` สำหรับอินสแตนซ์ `Date` และคืนค่า `false` ในกรณีอื่น                                                                          |
| [isDecimal](isDecimal.md)                   | `isDecimal` ตรวจสอบว่าค่าเป็นการแทนค่าแบบสตริงของจำนวนทศนิยมหรือไม่ โดยไม่มีช่องว่างนำหน้า/ต่อท้าย และมีรูปแบบทศนิยมที่ถูกต้อง                                                                     |
| [isEmpty](isEmpty.md)                       | กำหนดว่าค่าที่กำหนดนั้นว่างหรือไม่ โดยคืนค่า `true` สำหรับ `null`, `undefined`, สตริงว่าง/มีแต่ช่องว่าง, อาร์เรย์ว่าง, `Map`/`Set` ว่าง หรืออ็อบเจ็กต์ที่ไม่มีพร็อพเพอร์ตีที่ enumerable ของตัวเอง |
| [isError](isError.md)                       | ตรวจสอบว่า `value` ที่กำหนดเป็นอินสแตนซ์ของ `Error` หรือไม่                                                                                                                                        |
| [isFalse](isFalse.md)                       | `isFalse` ตรวจสอบว่าค่าที่กำหนดมีค่าเท่ากับลิเทอรัลบูลีน `false` แบบเข้มงวดหรือไม่                                                                                                                 |
| [isFilledArray](isFilledArray.md)           | ตรวจสอบว่า `value` เป็นอาร์เรย์ที่มีอย่างน้อยหนึ่งองค์ประกอบหรือไม่ โดยคืนค่า `true` หรือ `false`                                                                                                  |
| [isFinite](isFinite.md)                     | กำหนดว่า `value` ที่กำหนดเป็น `number` ที่เป็นจำนวนจำกัดหรือไม่                                                                                                                                    |
| [isFloat](isFloat.md)                       | `isFloat` ใช้กำหนดว่า `value` ที่กำหนดเป็นเลขทศนิยมแบบจำนวนจำกัดหรือไม่ (เป็น `number` ที่ไม่ใช่จำนวนเต็ม).                                                                                        |
| [isHexadecimal](isHexadecimal.md)           | `isHexadecimal` ตรวจสอบว่าค่าที่กำหนดเป็นลิเทอรัลสตริงเลขฐานสิบหกที่ต้องมีคำนำหน้า `0x`/`0X` หรือไม่                                                                                               |
| [isIndexFound](isIndexFound.md)             | `isIndexFound` ใช้กำหนดว่าค่าที่กำหนดเป็นจำนวนเต็มที่ไม่ติดลบหรือไม่ ซึ่งบ่งชี้ว่าพบดัชนีแล้ว                                                                                                      |
| [isInteger](isInteger.md)                   | กำหนดว่า `value` ที่กำหนดเป็นตัวเลขจำนวนเต็มที่ปลอดภัยหรือไม่                                                                                                                                      |
| [isMap](isMap.md)                           | ตรวจสอบว่า `value` ที่กำหนดเป็น `Map` หรือไม่ โดยคืนค่า `true` หากเป็น และ `false` หากไม่ใช่                                                                                                       |
| [isNaN](isNaN.md)                           | ระบุว่า `value` ที่ให้มาเป็น `NaN` ของชนิด `number` หรือไม่ โดยไม่แปลงสตริง                                                                                                                        |
| [isNull](isNull.md)                         | กำหนดว่าค่า `value` ที่ให้มานั้นเป็น `null` หรือไม่                                                                                                                                                |
| [isNullOrUndefined](isNullOrUndefined.md)   | ตรวจสอบว่าค่าที่กำหนดเป็น `null` หรือ `undefined` หรือไม่                                                                                                                                          |
| [isNumber](isNumber.md)                     | `isNumber` ตรวจสอบว่าค่าเป็นตัวเลขที่มีขอบเขตจำกัดและไม่ใช่ `NaN` หรือไม่.                                                                                                                         |
| [isNumeric](isNumeric.md)                   | `isNumeric` ใช้กำหนดว่าค่า `value` ที่ให้มาถือว่าเป็นตัวเลขหรือไม่ โดยตรวจสอบชนิดที่ถูกแก้ไขแล้วของมันเทียบกับ `NUMERIC_TYPES`                                                                     |
| [isObject](isObject.md)                     | ตรวจสอบว่า `value` ที่กำหนดเป็น `object` หรือไม่ (ไม่รวม `null`)                                                                                                                                   |
| [isOctal](isOctal.md)                       | ระบุว่าค่าหนึ่งเป็นสตริงลิเทอรัลฐานแปดที่ถูกต้องหรือไม่ (เช่น `0o755`).                                                                                                                            |
| [isOfType](isOfType.md)                     | กำหนดว่า `value` ที่ให้มาตรงกับสตริงชนิดข้อมูลที่ระบุหรือไม่ โดยใช้ `typeof` สำหรับชนิดข้อมูลพื้นฐานและใช้วิธีสำรองสำหรับชนิดข้อมูลที่ซับซ้อน                                                      |
| [isOneOfType](isOneOfType.md)               | `isOneOfType` ใช้กำหนดว่าค่า `value` ที่กำหนดตรงกับสตริงชนิดที่ให้มาอย่างน้อยหนึ่งรายการหรือไม่ โดยจะคืนค่า `true` หากพบรายการที่ตรงกัน และคืนค่า `false` หากไม่พบ                                 |
| [isPlainObject](isPlainObject.md)           | ตรวจสอบว่า `value` ที่กำหนดเป็น plain object หรือไม่ และคืนค่า `true` หากใช่ มิฉะนั้นคืนค่า `false`                                                                                                |
| [isPrimitive](isPrimitive.md)               | `isPrimitive` ใช้กำหนดว่าค่าที่กำหนดเป็น primitive หรือไม่ (`null`, `undefined`, `boolean`, `number`, `string`, `bigint`, `symbol`).                                                               |
| [isPromise](isPromise.md)                   | ระบุว่าค่าที่กำหนดเป็น `Promise` หรือไม่                                                                                                                                                           |
| [isRegEx](isRegEx.md)                       | กำหนดว่าค่าที่ให้มานั้นเป็นอินสแตนซ์ของ `RegExp` หรือไม่                                                                                                                                           |
| [isSet](isSet.md)                           | ระบุว่าค่าที่กำหนดเป็น `Set` หรือไม่                                                                                                                                                               |
| [isStream](isStream.md)                     | `isStream` ตรวจสอบว่าค่าที่กำหนดเป็นอ็อบเจ็กต์สตรีมหรือไม่ (สตรีมแบบ Node.js, `ReadableStream` หรือ `WritableStream`).                                                                             |
| [isString](isString.md)                     | `isString` ใช้ตรวจสอบว่าค่าที่กำหนดเป็นสตริงหรือไม่                                                                                                                                                |
| [isSymbol](isSymbol.md)                     | `isSymbol` ใช้กำหนดว่าค่าที่กำหนดเป็นชนิด `symbol` หรือไม่ โดยคืนค่า `true` สำหรับสัญลักษณ์ และ `false` ในกรณีอื่น                                                                                 |
| [isTrue](isTrue.md)                         | `isTrue` ใช้กำหนดว่าค่าที่ให้มานั้นเท่ากับ `true` แบบเข้มงวดหรือไม่.                                                                                                                               |
| [isUndefined](isUndefined.md)               | ตรวจสอบว่าค่าที่กำหนดเป็น `undefined` หรือไม่                                                                                                                                                      |
| [isValidDate](isValidDate.md)               | `isValidDate` ตรวจสอบว่าค่าที่กำหนดเป็นอ็อบเจ็กต์ `Date` ที่ถูกต้องหรือไม่ และจะคืนค่า `true` เฉพาะสำหรับวันที่จริงที่ไม่ใช่วันที่ไม่ถูกต้อง                                                       |
| [isWeakMap](isWeakMap.md)                   | กำหนดว่าค่า `value` ที่กำหนดเป็นอินสแตนซ์ของ `WeakMap` หรือไม่                                                                                                                                     |
| [isWeakSet](isWeakSet.md)                   | ระบุว่า `value` ที่กำหนดเป็น `WeakSet` ของอ็อบเจ็กต์หรือไม่                                                                                                                                        |

## ฟังก์ชันที่เปรียบเทียบแล้ว

| ฟังก์ชัน | คำอธิบาย |
|----------|----------|
