# areEqual

## Опис

`areEqual` проверава да ли су сви елементи у низу једнаки датој очекиваној вредности, враћајући `true` само за непразне
низове у којима се свака ставка поклапа.

### Случај употребе

Потврдите да листа садржи само једну дозвољену вредност (нпр. да су све статусне заставице `true`, све улоге `"admin"`,
или да су сви бројчани уноси једнаки захтеваној константи), при чему се празан унос третира као неважећи (`false`).

> **Напомена за TypeScript кориснике:**
>
> Користите `areEqual` када вам је потребна строга провера да су све ставке исте; враћа `false` за празне низове и за
> било који унос који није низ или није попуњен.

### Предности

- Враћа `true` само када сваки елемент одговара очекиваној вредности; у супротном враћа `false`.
- Брзо прекида: престаје са провером чим се пронађе елемент који се не поклапа.
- Штити од неважећег уноса тако што враћа `false` када унос није попуњен низ.

## Употреба

### Синтакса

Функција:

- `areEqual(value, expected)`

Параметри:

- `value`: Низ који треба проверити.
- `expected`: Елемент са којим се пореди свака ставка у низу.

### Локални увоз функције

```ts
import { areEqual } from "@type-check/guards";

const allOnes = areEqual([1, 1, 1], 1);
const allTests = areEqual(["test", "test"], "test");
const notAllTwos = areEqual([2, 3, 2], 2);
const emptyIsFalse = areEqual([], 5);

console.log(allOnes, allTests, notAllTwos, emptyIsFalse);
```

### Глобални увоз објекта

За увоз функција као глобалних метода објекта користите:

```ts
import "@type-check/guards/register-global-object.mjs";
```

Следећи метод ће затим бити доступан глобално:

- `Type.areEqual(value, expected)`

## Анализа функција

Овде је документована табеларна анализа излаза који настаје при уносу различитих параметара у
функције: [areEqual](../_analysis/areEqual.md)

<br>

---

<small>Датотека је генерисана 31 January 2026 at 23:52:14 (UTC) уз коришћење *
*[markdown-documentation-generator](https://github.com/roland-milto/markdown-documentation-generator)** од *
*[Roland Milto](https://roland-milto.de/)**.</small>