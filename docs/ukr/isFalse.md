# isFalse

## Опис

`isFalse` перевіряє, чи є задане значення строго рівним булевому літералу `false`.

### Випадок використання

Валідуйте невідомі дані (наприклад, з JSON, параметрів запиту або вводу користувача), де лише явне булеве значення
`false` має вважатися валідним прапорцем, а все інше слід відхиляти.

> **Примітка для користувачів TypeScript:**
>
> Використовуйте `isFalse`, коли потрібно приймати лише літерал `false` і відхиляти всі інші хибні значення; він
> повертає `true` лише для `value === false`.

### Переваги

- Забезпечує сувору перевірку булевого літерала `false` без приведення типів.
- Допомагає відрізняти `false` від інших хибних (falsy) значень, як-от `0`, `""`, `null` та `undefined`.
- Покращує читабельність, роблячи намір явним під час валідації невідомого вводу.

## Використання

### Синтаксис

Функція:

- `isFalse(value)`

Параметри:

- `value`: Значення, яке потрібно перевірити.

### Локальний імпорт функції

```ts
import { isFalse } from "@type-check/guards";

const input: unknown = false;

if (isFalse(input)) {
  // тут input є рівно false
  console.log("Got false");
} else {
  console.log("Not false");
}

```

### Глобальний імпорт об’єкта

Щоб імпортувати функції як глобальні методи об’єкта, використовуйте:

```ts
import "@type-check/guards/register-global-object.mjs";
```

Наступний метод буде доступний глобально:

- `Type.isFalse(value)`

## Аналіз функцій

Тут задокументовано табличний аналіз виводу, що виникає під час підстановки різних параметрів у
функції: [isFalse](../_analysis/isFalse.md)

<br>

---

<small>Файл було згенеровано 31 January 2026 at 16:44:55 (UTC) із використанням *
*[markdown-documentation-generator](https://github.com/roland-milto/markdown-documentation-generator)** від *
*[Roland Milto](https://roland-milto.de/)**.</small>