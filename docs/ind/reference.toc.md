# Referensi fungsi

## Fungsi

| Fungsi                                      | Deskripsi                                                                                                                                                                                                    |
|---------------------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| [areArrays](areArrays.md)                   | `areArrays` menentukan apakah sebuah nilai adalah array dua dimensi yang terisi, dengan item-item yang semuanya berupa array.                                                                                |
| [areBigInts](areBigInts.md)                 | `areBigInts` menentukan apakah suatu nilai adalah array tidak kosong yang hanya berisi nilai `bigint`.                                                                                                       |
| [areBinaries](areBinaries.md)               | Memeriksa apakah nilai yang diberikan adalah array tidak kosong berisi string biner yang valid dan mengembalikan `true` hanya jika semua item lolos validasi.                                                |
| [areBooleans](areBooleans.md)               | `areBooleans` memeriksa apakah sebuah array tidak kosong yang diberikan hanya berisi nilai boolean, mengembalikan `true` jika ya dan `false` jika tidak.                                                     |
| [areBuffers](areBuffers.md)                 | `areBuffers` memeriksa apakah nilai yang diberikan adalah array yang tidak kosong dan terisi, di mana setiap elemennya adalah `Buffer`, mengembalikan `true` jika demikian dan `false` jika tidak.           |
| [areDates](areDates.md)                     | `areDates` menentukan apakah sebuah array tertentu terisi dan hanya berisi objek `Date`, mengembalikan `true` hanya ketika semua elemen adalah tanggal yang valid.                                           |
| [areDecimals](areDecimals.md)               | Memeriksa apakah semua elemen dalam sebuah array adalah angka desimal dan array tersebut terisi, mengembalikan `true` atau `false`.                                                                          |
| [areEqual](areEqual.md)                     | `areEqual` memeriksa apakah semua elemen dalam sebuah array sama dengan nilai yang diharapkan, mengembalikan `true` hanya untuk array yang tidak kosong ketika setiap item cocok.                            |
| [areErrors](areErrors.md)                   | Memeriksa apakah sebuah array tidak kosong dan hanya berisi objek `Error`, mengembalikan `true` atau `false`.                                                                                                |
| [areFalse](areFalse.md)                     | `areFalse` memeriksa apakah semua elemen dalam array yang diberikan benar-benar boolean `false`.                                                                                                             |
| [areFilledArrays](areFilledArrays.md)       | `areFilledArrays` memeriksa apakah sebuah array dua dimensi tidak kosong dan semua subarray-nya tidak kosong.                                                                                                |
| [areFinite](areFinite.md)                   | `areFinite` memeriksa apakah suatu nilai adalah array yang tidak kosong dengan elemen-elemen yang semuanya merupakan angka hingga (finite), mengembalikan `true` jika demikian dan `false` jika tidak.       |
| [areFloats](areFloats.md)                   | `areFloats` memeriksa apakah sebuah array tertentu terisi dan semua elemennya adalah float.                                                                                                                  |
| [areHexadecimals](areHexadecimals.md)       | Memeriksa apakah semua elemen dalam sebuah array adalah string heksadesimal, mengembalikan `true` hanya untuk array tidak kosong di mana setiap item valid.                                                  |
| [areIndexesFound](areIndexesFound.md)       | `areIndexesFound` memeriksa apakah suatu nilai adalah array tidak kosong yang elemennya semuanya merupakan indeks yang valid, mengembalikan `true` jika ya dan `false` jika tidak.                           |
| [areIntegers](areIntegers.md)               | `areIntegers` menentukan apakah semua elemen dalam array tertentu adalah bilangan bulat, mengembalikan `true` jika ya dan `false` jika tidak.                                                                |
| [areJson](areJson.md)                       | Memeriksa apakah semua elemen dalam sebuah array adalah string JSON, mengembalikan `true` hanya jika array terisi dan setiap item adalah JSON yang valid; jika tidak, mengembalikan `false`.                 |
| [areMaps](areMaps.md)                       | `areMaps` menentukan apakah sebuah array yang diberikan tidak kosong dan semua elemennya adalah instance `Map`.                                                                                              |
| [areNaNs](areNaNs.md)                       | `areNaNs` memeriksa apakah semua elemen dalam sebuah array adalah `NaN` dan mengembalikan `true` hanya jika setiap elemen adalah `NaN`.                                                                      |
| [areNullOrUndefined](areNullOrUndefined.md) | Memeriksa apakah semua elemen dalam array yang diberikan adalah `null` atau `undefined`.                                                                                                                     |
| [areNumbers](areNumbers.md)                 | `areNumbers` memeriksa apakah suatu nilai adalah array yang tidak kosong di mana semua elemennya adalah angka.                                                                                               |
| [areNumerics](areNumerics.md)               | `areNumerics` memeriksa apakah suatu nilai adalah array yang tidak kosong di mana semua elemennya numerik.                                                                                                   |
| [areObjects](areObjects.md)                 | `areObjects` memeriksa apakah array terisi yang diberikan hanya berisi objek.                                                                                                                                |
| [areOctals](areOctals.md)                   | `areOctals` menentukan apakah nilai yang diberikan adalah array tidak kosong berisi string oktal yang valid.                                                                                                 |
| [areOfType](areOfType.md)                   | Memeriksa apakah semua elemen dalam `array` yang diberikan memiliki `type` yang ditentukan.                                                                                                                  |
| [areOneOfType](areOneOfType.md)             | `areOneOfType` memeriksa apakah semua elemen dalam array yang tidak kosong termasuk salah satu dari tipe runtime yang ditentukan.                                                                            |
| [arePlainObjects](arePlainObjects.md)       | Memeriksa apakah semua elemen dari sebuah array adalah objek biasa, mengembalikan `true` hanya jika setiap elemen memenuhi syarat.                                                                           |
| [arePrimitives](arePrimitives.md)           | `arePrimitives` mengevaluasi apakah semua elemen dalam array yang diberikan dan tidak kosong adalah tipe primitif.                                                                                           |
| [arePromises](arePromises.md)               | `arePromises` menentukan apakah semua elemen dalam sebuah array merupakan instance `Promise`.                                                                                                                |
| [areRegExes](areRegExes.md)                 | `areRegExes` memeriksa apakah sebuah nilai adalah array yang terisi dan hanya berisi objek `RegExp`.                                                                                                         |
| [areSets](areSets.md)                       | Memeriksa apakah sebuah array tidak kosong yang diberikan hanya berisi instance `Set`, mengembalikan `true` jika ya dan `false` jika tidak.                                                                  |
| [areStreams](areStreams.md)                 | `areStreams` menentukan apakah sebuah nilai adalah array terisi di mana setiap elemennya adalah `Stream`.                                                                                                    |
| [areStrings](areStrings.md)                 | `areStrings` memeriksa apakah sebuah array tidak kosong dan semua elemennya adalah string, mengembalikan `true` hanya dalam kasus tersebut.                                                                  |
| [areSymbols](areSymbols.md)                 | Memeriksa apakah sebuah input adalah array terisi yang elemennya semuanya simbol, mengembalikan `true` atau `false`.                                                                                         |
| [areTrue](areTrue.md)                       | Memeriksa apakah sebuah array yang tidak kosong hanya berisi nilai boolean `true`.                                                                                                                           |
| [areUndefined](areUndefined.md)             | `areUndefined` memeriksa apakah setiap elemen dalam array yang diberikan adalah `undefined`.                                                                                                                 |
| [areValidDates](areValidDates.md)           | Menentukan apakah sebuah array tidak kosong dan seluruh isinya terdiri dari objek `Date` yang valid.                                                                                                         |
| [areWeakMaps](areWeakMaps.md)               | `areWeakMaps` memeriksa apakah sebuah nilai adalah array tidak kosong di mana setiap elemennya adalah `WeakMap`, mengembalikan `true` hanya dalam kasus tersebut dan `false` jika tidak.                     |
| [areWeakSets](areWeakSets.md)               | Memeriksa apakah sebuah input adalah array tidak kosong di mana setiap elemennya adalah `WeakSet`, dan hanya mengembalikan `true` dalam kasus tersebut.                                                      |
| [getTypeOf](getTypeOf.md)                   | `getTypeOf` mengembalikan label tipe yang rinci dan mudah dibaca manusia untuk suatu nilai, termasuk tipe numerik yang diperhalus dan jenis objek yang spesifik.                                             |
| [isArray](isArray.md)                       | `isArray` memeriksa apakah suatu nilai yang diberikan adalah array dan mengembalikan `true` jika ya, jika tidak `false`.                                                                                     |
| [isBigInt](isBigInt.md)                     | `isBigInt` memeriksa apakah suatu nilai tertentu bertipe `bigint`, mengembalikan `true` untuk primitif BigInt dan `false` jika tidak.                                                                        |
| [isBinary](isBinary.md)                     | `isBinary` menentukan apakah suatu nilai adalah string biner (opsional diawali dengan `0b`/`0B`) dan mengembalikan `true` atau `false`.                                                                      |
| [isBoolean](isBoolean.md)                   | Menentukan apakah suatu nilai yang diberikan adalah `boolean`.                                                                                                                                               |
| [isBuffer](isBuffer.md)                     | Memeriksa apakah sebuah nilai adalah `Buffer` Node.js dan mengembalikan `true` atau `false`.                                                                                                                 |
| [isDate](isDate.md)                         | `isDate` menentukan apakah nilai yang diberikan adalah `Date`, mengembalikan `true` untuk instance `Date` dan `false` jika tidak.                                                                            |
| [isDecimal](isDecimal.md)                   | `isDecimal` memeriksa apakah sebuah nilai merupakan representasi string desimal tanpa spasi di awal/akhir dan dengan format desimal yang valid.                                                              |
| [isEmpty](isEmpty.md)                       | Menentukan apakah suatu nilai kosong, mengembalikan `true` untuk `null`, `undefined`, string kosong/berisi spasi saja, array kosong, `Map`/`Set` kosong, atau objek tanpa properti enumerable milik sendiri. |
| [isError](isError.md)                       | Memeriksa apakah `value` yang diberikan merupakan instance dari `Error`.                                                                                                                                     |
| [isFalse](isFalse.md)                       | `isFalse` memeriksa apakah suatu nilai yang diberikan benar-benar sama secara ketat dengan literal boolean `false`.                                                                                          |
| [isFilledArray](isFilledArray.md)           | Memeriksa apakah `value` adalah sebuah array dengan setidaknya satu elemen, mengembalikan `true` atau `false`.                                                                                               |
| [isFinite](isFinite.md)                     | Menentukan apakah `value` yang diberikan merupakan `number` yang hingga.                                                                                                                                     |
| [isFloat](isFloat.md)                       | `isFloat` menentukan apakah `value` yang diberikan adalah bilangan floating-point hingga (sebuah `number` yang bukan bilangan bulat).                                                                        |
| [isHexadecimal](isHexadecimal.md)           | `isHexadecimal` memeriksa apakah sebuah nilai yang diberikan adalah literal string heksadesimal dengan awalan `0x`/`0X` yang wajib.                                                                          |
| [isIndexFound](isIndexFound.md)             | `isIndexFound` menentukan apakah suatu nilai yang diberikan adalah bilangan bulat tidak negatif, yang menandakan sebuah indeks ditemukan.                                                                    |
| [isInteger](isInteger.md)                   | Menentukan apakah `value` yang diberikan adalah angka bilangan bulat aman.                                                                                                                                   |
| [isMap](isMap.md)                           | Memeriksa apakah `value` yang diberikan adalah sebuah `Map`, mengembalikan `true` jika ya dan `false` jika tidak.                                                                                            |
| [isNaN](isNaN.md)                           | Menentukan apakah `value` yang diberikan adalah `NaN` bertipe `number` tanpa mengonversi string.                                                                                                             |
| [isNull](isNull.md)                         | Menentukan apakah `value` yang diberikan adalah `null`.                                                                                                                                                      |
| [isNullOrUndefined](isNullOrUndefined.md)   | Memeriksa apakah suatu nilai adalah `null` atau `undefined`.                                                                                                                                                 |
| [isNumber](isNumber.md)                     | `isNumber` memeriksa apakah sebuah nilai adalah angka yang berhingga dan bukan `NaN`.                                                                                                                        |
| [isNumeric](isNumeric.md)                   | `isNumeric` menentukan apakah `value` tertentu dianggap numerik dengan memeriksa tipe hasil resolusinya terhadap `NUMERIC_TYPES`.                                                                            |
| [isObject](isObject.md)                     | Menentukan apakah `value` yang diberikan adalah sebuah `object` (tidak termasuk `null`).                                                                                                                     |
| [isOctal](isOctal.md)                       | Menentukan apakah suatu nilai adalah string literal oktal yang valid (mis. `0o755`).                                                                                                                         |
| [isOfType](isOfType.md)                     | Menentukan apakah `value` tertentu cocok dengan string tipe yang ditentukan, menggunakan `typeof` untuk tipe primitif dan fallback untuk tipe kompleks.                                                      |
| [isOneOfType](isOneOfType.md)               | `isOneOfType` menentukan apakah `value` yang diberikan cocok dengan setidaknya satu dari string tipe yang disediakan, mengembalikan `true` jika ada kecocokan dan `false` jika tidak.                        |
| [isPlainObject](isPlainObject.md)           | Memeriksa apakah `value` yang diberikan adalah objek polos dan mengembalikan `true` jika ya, jika tidak `false`.                                                                                             |
| [isPrimitive](isPrimitive.md)               | `isPrimitive` menentukan apakah suatu nilai yang diberikan adalah primitif (`null`, `undefined`, `boolean`, `number`, `string`, `bigint`, `symbol`).                                                         |
| [isPromise](isPromise.md)                   | Menentukan apakah suatu nilai tertentu adalah `Promise`.                                                                                                                                                     |
| [isRegEx](isRegEx.md)                       | Menentukan apakah nilai yang diberikan merupakan instance `RegExp`.                                                                                                                                          |
| [isSet](isSet.md)                           | Menentukan apakah suatu nilai yang diberikan adalah `Set`.                                                                                                                                                   |
| [isStream](isStream.md)                     | `isStream` memeriksa apakah suatu nilai tertentu adalah objek stream (mirip stream Node.js, `ReadableStream`, atau `WritableStream`).                                                                        |
| [isString](isString.md)                     | `isString` menentukan apakah suatu nilai yang diberikan adalah string.                                                                                                                                       |
| [isSymbol](isSymbol.md)                     | `isSymbol` menentukan apakah suatu nilai yang diberikan bertipe `symbol`, mengembalikan `true` untuk symbol dan `false` jika tidak.                                                                          |
| [isTrue](isTrue.md)                         | `isTrue` menentukan apakah suatu nilai yang diberikan benar-benar sama dengan `true`.                                                                                                                        |
| [isUndefined](isUndefined.md)               | Memeriksa apakah suatu nilai adalah `undefined`.                                                                                                                                                             |
| [isValidDate](isValidDate.md)               | `isValidDate` memeriksa apakah sebuah nilai yang diberikan adalah objek `Date` yang valid dan mengembalikan `true` hanya untuk tanggal yang nyata dan tidak invalid.                                         |
| [isWeakMap](isWeakMap.md)                   | Menentukan apakah `value` yang diberikan merupakan instance `WeakMap`.                                                                                                                                       |
| [isWeakSet](isWeakSet.md)                   | Menentukan apakah `value` yang diberikan adalah sebuah `WeakSet` berisi objek.                                                                                                                               |

## Fungsi yang dibandingkan

| Fungsi | Deskripsi |
|--------|-----------|
