# areFloats

## Апісанне

`areFloats` правярае, ці з’яўляецца зададзены масіў запоўненым і ці ўсе яго элементы — лікі з плаваючай кропкай.

### Выкарыстанне

Выкарыстоўвайце `areFloats`, калі вы атрымліваеце `unknown[]` (напрыклад, з JSON, параметраў запыту або знешніх API) і
вам трэба пераканацца, што гэта запоўнены масіў, у якім кожны элемент — лік з плаваючай кропкай, перад выкананнем
лікавай логікі, такой як вылічэнне сярэдняга, інтэрпаляцыя або статыстычныя разлікі.

> **Падказка для карыстальнікаў TypeScript:**
>
> Выкарыстоўвайце `areFloats`, каб абараніць `unknown[]` перад тым, як разглядаць яго як `number[]`, які змяшчае толькі
> лікі з плаваючай кропкай; ён вяртае `false` для пустых масіваў і для любога элемента, які не з’яўляецца лікам з
> плаваючай кропкай.

### Перавагі

- Вяртае `true` толькі тады, калі ўваходныя даныя — гэта непусты масіў і кожны элемент з’яўляецца лікам з плаваючай
  кропкай.
- Хутка спыняецца: вяртае `false`, як толькі знаходзіцца элемент, які не з’яўляецца лікам з плаваючай кропкай.
- Дапамагае правяраць невядомыя ўваходныя даныя перад выкананнем вылічэнняў, спецыфічных для лікаў з плаваючай кропкай.

## Выкарыстанне

### Сінтаксіс

Функцыя:

- `areFloats(array)`

Параметры:

- `array`: Масіў, у якім трэба праверыць элементы на тое, ці з’яўляюцца яны лікамі з плаваючай кропкай.

### Лакальны імпарт функцыі

```ts
import { areFloats } from "@type-check/guards";

const a: unknown[] = [3.14, 2.71, 1.0];
const b: unknown[] = [3.14, 2];
const c: unknown[] = [];

console.log(areFloats(a)); // праўда
console.log(areFloats(b)); // няпраўда
console.log(areFloats(c)); // няпраўда

function sumFloats(values: unknown): number | null {
  if (!Array.isArray(values) || !areFloats(values)) return null;
  return values.reduce((acc, n) => acc + n, 0);
}

console.log(sumFloats([0.5, 1.25])); // 1.75
console.log(sumFloats([1, 2])); // null

```

### Глабальны імпарт аб’екта

Каб імпартаваць функцыі як глабальныя метады аб’екта, выкарыстоўвайце:

```ts
import "@type-check/guards/register-global-object.mjs";
```

Наступны метад будзе даступны глабальна:

- `Type.areFloats(array)`

## Аналіз функцый

Таблічны аналіз таго, які вынік атрымліваецца пры перадачы розных параметраў у функцыі, задокументаваны
тут: [areFloats](../_analysis/areFloats.md)

<br>

---

<small>Файл быў створаны 30 January 2026 at 15:56:20 (UTC) з выкарыстаннем *
*[markdown-documentation-generator](https://github.com/roland-milto/markdown-documentation-generator)** ад *
*[Roland Milto](https://roland-milto.de/)**.</small>