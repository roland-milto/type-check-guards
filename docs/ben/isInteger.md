# isInteger

## বর্ণনা

প্রদত্ত `value` একটি নিরাপদ পূর্ণসংখ্যা কি না তা নির্ধারণ করে।

### ব্যবহারের ক্ষেত্র

অবিশ্বস্ত ইনপুট (যেমন, query params, JSON payloads, environment variables) পূর্ণসংখ্যা হিসেবে ব্যবহার করার আগে যাচাই
করুন—অ্যারে ইনডেক্স, pagination, কাউন্টার, বা ডাটাবেস ID-এর জন্য।

> **TypeScript ব্যবহারকারীদের জন্য ইঙ্গিত:**
>
> অজানা ইনপুটকে সংখ্যাগত পূর্ণসংখ্যা হিসেবে ব্যবহারের আগে যাচাই করতে `isInteger` ব্যবহার করুন; এটি কেবল সেই মানগুলোর
> জন্য `true` রিটার্ন করে যেখানে `typeof value === "number"` এবং `Number.isSafeInteger(value)`।

### সুবিধাসমূহ

- টাইপ এবং সংখ্যাগত নিরাপত্তা—দুটিই যাচাই করে: ইনপুটটি সংখ্যা এবং নিরাপদ পূর্ণসংখ্যা হলে তবেই `true` রিটার্ন করে।
- সংখ্যায় জোরপূর্বক রূপান্তরের সাধারণ ফাঁদ এড়ায়: "5"-এর মতো স্ট্রিং সঠিকভাবে `false` রিটার্ন করে।
- অপূর্ণসংখ্যা এবং অনিরাপদ পূর্ণসংখ্যা প্রত্যাখ্যান করে, ফলে এটি ID, কাউন্টার, এবং অ্যারে ইনডেক্সিং-এর জন্য উপযোগী।

## ব্যবহার

### সিনট্যাক্স

ফাংশন:

- `isInteger(value)`

প্যারামিটার:

- `value`: পূর্ণসংখ্যা অবস্থা যাচাই করার জন্য মানটি।

### লোকাল ফাংশন ইমপোর্ট

```ts
import { isInteger } from "@type-check/guards";

const a = isInteger(5);      // সত্য
const b = isInteger(-100);   // সত্য
const c = isInteger("5");    // মিথ্যা
const d = isInteger(5.5);    // মিথ্যা
const e = isInteger(null);   // মিথ্যা

```

### গ্লোবাল অবজেক্ট ইমপোর্ট

ফাংশনগুলোকে গ্লোবাল অবজেক্ট মেথড হিসেবে ইমপোর্ট করতে ব্যবহার করুন:

```ts
import "@type-check/guards/register-global-object.mjs";
```

তাহলে নিম্নলিখিত মেথডটি গ্লোবালি উপলব্ধ হবে:

- `Type.isInteger(value)`

## ফাংশন বিশ্লেষণ

ফাংশনগুলোতে বিভিন্ন প্যারামিটার দিলে কোন আউটপুট হয় তার একটি সারণিভিত্তিক বিশ্লেষণ এখানে নথিভুক্ত
আছে: [isInteger](../_analysis/isInteger.md)

<br>

---

<small>**[Roland Milto](https://roland-milto.de/)** কর্তৃক *
*[markdown-documentation-generator](https://github.com/roland-milto/markdown-documentation-generator)** ব্যবহার করে ৩১
জানুয়ারী, ২০২৬ এ ১২:৪৯:০৯ AM (UTC) তারিখে ফাইলটি তৈরি করা হয়েছে।</small>