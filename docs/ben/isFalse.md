# isFalse

## বর্ণনা

`isFalse` পরীক্ষা করে কোনো প্রদত্ত মান বুলিয়ান লিটারাল `false`-এর সাথে কঠোরভাবে সমান কি না।

### ব্যবহারের ক্ষেত্র

অজানা ডেটা (যেমন JSON, কুয়েরি প্যারামস, বা ব্যবহারকারীর ইনপুট থেকে) যাচাই করুন, যেখানে কেবল স্পষ্ট বুলিয়ান মান `false`
-কেই বৈধ ফ্ল্যাগ হিসেবে গণ্য করা উচিত, এবং বাকি সবকিছু প্রত্যাখ্যান করা উচিত।

> **TypeScript ব্যবহারকারীদের জন্য ইঙ্গিত:**
>
> যখন আপনাকে কেবল লিটারাল `false` গ্রহণ করতে হবে এবং অন্য সব falsy মান প্রত্যাখ্যান করতে হবে, তখন `isFalse` ব্যবহার
> করুন; এটি কেবল `value === false` হলে `true` ফেরত দেয়।

### সুবিধাসমূহ

- কোনো ধরনের কোয়ের্শন ছাড়াই বুলিয়ান লিটারাল `false`-এর জন্য কঠোর যাচাই প্রদান করে।
- `false`-কে `0`, `""`, `null`, এবং `undefined`-এর মতো অন্যান্য falsy মান থেকে আলাদা করতে সাহায্য করে।
- অজানা ইনপুট যাচাই করার সময় উদ্দেশ্যকে স্পষ্ট করে পাঠযোগ্যতা উন্নত করে।

## ব্যবহার

### সিনট্যাক্স

ফাংশন:

- `isFalse(value)`

প্যারামিটার:

- `value`: যে মানটি যাচাই করা হবে।

### লোকাল ফাংশন ইমপোর্ট

```ts
import { isFalse } from "@type-check/guards";

const input: unknown = false;

if (isFalse(input)) {
  // এখানে input ঠিক false-ই
  console.log("Got false");
} else {
  console.log("Not false");
}

```

### গ্লোবাল অবজেক্ট ইমপোর্ট

ফাংশনগুলোকে গ্লোবাল অবজেক্ট মেথড হিসেবে ইমপোর্ট করতে ব্যবহার করুন:

```ts
import "@type-check/guards/register-global-object.mjs";
```

তাহলে নিম্নলিখিত মেথডটি গ্লোবালি উপলব্ধ হবে:

- `Type.isFalse(value)`

## ফাংশন বিশ্লেষণ

ফাংশনগুলোতে বিভিন্ন প্যারামিটার দিলে কোন আউটপুট হয় তার একটি সারণিভিত্তিক বিশ্লেষণ এখানে নথিভুক্ত
আছে: [isFalse](../_analysis/isFalse.md)

<br>

---

<small>**[Roland Milto](https://roland-milto.de/)** কর্তৃক *
*[markdown-documentation-generator](https://github.com/roland-milto/markdown-documentation-generator)** ব্যবহার করে ৩১
জানুয়ারী, ২০২৬ এ ৪:২০:৫৪ PM (UTC) তারিখে ফাইলটি তৈরি করা হয়েছে।</small>