# Tham chiếu hàm

## Các hàm

| Hàm                                         | Mô tả                                                                                                                                                                                                           |
|---------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| [areArrays](areArrays.md)                   | `areArrays` xác định liệu một giá trị có phải là một mảng hai chiều đã được điền (không rỗng) mà các phần tử của nó đều là mảng hay không.                                                                      |
| [areBigInts](areBigInts.md)                 | `areBigInts` xác định liệu một giá trị có phải là một mảng không rỗng chỉ chứa các giá trị `bigint` hay không.                                                                                                  |
| [areBinaries](areBinaries.md)               | Kiểm tra xem giá trị được cung cấp có phải là một mảng không rỗng gồm các chuỗi nhị phân hợp lệ hay không và chỉ trả về `true` nếu tất cả các mục đều vượt qua xác thực.                                        |
| [areBooleans](areBooleans.md)               | `areBooleans` kiểm tra xem một mảng không rỗng đã cho có chỉ chứa các giá trị boolean hay không, trả về `true` nếu có và `false` nếu không.                                                                     |
| [areBuffers](areBuffers.md)                 | `areBuffers` kiểm tra liệu giá trị được cung cấp có phải là một mảng không rỗng, đã được điền, trong đó mọi phần tử đều là một `Buffer` hay không; nếu đúng thì trả về `true`, nếu không thì trả về `false`.    |
| [areDates](areDates.md)                     | `areDates` xác định liệu một mảng cho trước có được điền dữ liệu và chỉ chứa các đối tượng `Date` hay không, chỉ trả về `true` khi tất cả phần tử đều là ngày hợp lệ.                                           |
| [areDecimals](areDecimals.md)               | Kiểm tra xem tất cả phần tử trong một mảng có phải là số thập phân và mảng có được điền (không rỗng) hay không, trả về `true` hoặc `false`.                                                                     |
| [areEqual](areEqual.md)                     | `areEqual` kiểm tra liệu tất cả phần tử trong một mảng có bằng với một giá trị mong đợi cho trước hay không, chỉ trả về `true` đối với các mảng không rỗng mà mọi mục đều khớp.                                 |
| [areErrors](areErrors.md)                   | Kiểm tra xem một mảng có không rỗng và chỉ chứa các đối tượng `Error` hay không, trả về `true` hoặc `false`.                                                                                                    |
| [areFalse](areFalse.md)                     | `areFalse` kiểm tra liệu tất cả các phần tử trong một mảng được cung cấp có строго là giá trị boolean `false` hay không.                                                                                        |
| [areFilledArrays](areFilledArrays.md)       | `areFilledArrays` kiểm tra liệu một mảng hai chiều có không rỗng và tất cả các mảng con của nó đều không rỗng hay không.                                                                                        |
| [areFinite](areFinite.md)                   | `areFinite` kiểm tra xem một giá trị có phải là một mảng không rỗng mà các phần tử đều là số hữu hạn hay không, trả về `true` nếu đúng và `false` nếu không.                                                    |
| [areFloats](areFloats.md)                   | `areFloats` kiểm tra liệu một mảng cho trước có được điền (không rỗng) và tất cả các phần tử của nó đều là float hay không.                                                                                     |
| [areHexadecimals](areHexadecimals.md)       | Kiểm tra liệu tất cả phần tử trong một mảng có phải là chuỗi thập lục phân hay không, chỉ trả về `true` cho các mảng không rỗng mà mọi mục đều hợp lệ.                                                          |
| [areIndexesFound](areIndexesFound.md)       | `areIndexesFound` kiểm tra xem một giá trị có phải là một mảng không rỗng mà các phần tử của nó đều là các chỉ mục hợp lệ hay không, trả về `true` nếu đúng và `false` nếu không.                               |
| [areIntegers](areIntegers.md)               | `areIntegers` xác định liệu tất cả các phần tử trong một mảng cho trước có phải là số nguyên hay không, trả về `true` nếu đúng và `false` nếu không.                                                            |
| [areJson](areJson.md)                       | Kiểm tra liệu tất cả phần tử của một mảng có phải là chuỗi JSON hay không, chỉ trả về `true` nếu mảng có phần tử và mọi mục đều là JSON hợp lệ; nếu không trả về `false`.                                       |
| [areMaps](areMaps.md)                       | `areMaps` xác định liệu một mảng cho trước có không rỗng và tất cả các phần tử của nó đều là các thể hiện `Map` hay không.                                                                                      |
| [areNaNs](areNaNs.md)                       | `areNaNs` kiểm tra liệu tất cả các phần tử trong một mảng có phải là `NaN` hay không và chỉ trả về `true` nếu mọi phần tử đều là `NaN`.                                                                         |
| [areNullOrUndefined](areNullOrUndefined.md) | Kiểm tra xem tất cả phần tử trong mảng đã cho có phải là `null` hoặc `undefined` hay không.                                                                                                                     |
| [areNumbers](areNumbers.md)                 | `areNumbers` kiểm tra liệu một giá trị có phải là một mảng không rỗng mà trong đó tất cả các phần tử đều là số hay không.                                                                                       |
| [areNumerics](areNumerics.md)               | `areNumerics` kiểm tra liệu một giá trị có phải là mảng không rỗng mà tất cả phần tử đều là số hay không.                                                                                                       |
| [areObjects](areObjects.md)                 | `areObjects` kiểm tra xem một mảng có phần tử được cung cấp có chỉ chứa các đối tượng hay không.                                                                                                                |
| [areOctals](areOctals.md)                   | `areOctals` xác định liệu giá trị được cung cấp có phải là một mảng không rỗng gồm các chuỗi bát phân hợp lệ hay không.                                                                                         |
| [areOfType](areOfType.md)                   | Kiểm tra xem tất cả phần tử trong `array` đã cho có thuộc `type` được chỉ định hay không.                                                                                                                       |
| [areOneOfType](areOneOfType.md)             | `areOneOfType` kiểm tra liệu tất cả các phần tử trong một mảng không rỗng có thuộc một trong các kiểu runtime được chỉ định hay không.                                                                          |
| [arePlainObjects](arePlainObjects.md)       | Kiểm tra liệu tất cả phần tử của một mảng có phải là đối tượng thuần hay không, chỉ trả về `true` nếu mọi phần tử đều đạt điều kiện.                                                                            |
| [arePrimitives](arePrimitives.md)           | `arePrimitives` đánh giá liệu tất cả phần tử trong một mảng được cung cấp (không rỗng) có phải là các kiểu nguyên thủy hay không.                                                                               |
| [arePromises](arePromises.md)               | `arePromises` xác định liệu tất cả các phần tử trong một mảng có phải là các thể hiện `Promise` hay không.                                                                                                      |
| [areRegExes](areRegExes.md)                 | `areRegExes` kiểm tra xem một giá trị có phải là một mảng đã được điền (không rỗng) chỉ chứa các đối tượng `RegExp` hay không.                                                                                  |
| [areSets](areSets.md)                       | Kiểm tra xem một mảng không rỗng cho trước có chỉ chứa các thể hiện của `Set` hay không; trả về `true` nếu có và `false` nếu không.                                                                             |
| [areStreams](areStreams.md)                 | `areStreams` xác định liệu một giá trị có phải là một mảng có phần tử, trong đó mọi phần tử đều là một `Stream` hay không.                                                                                      |
| [areStrings](areStrings.md)                 | `areStrings` kiểm tra liệu một mảng có không rỗng và tất cả các phần tử của nó đều là chuỗi hay không, chỉ trả về `true` trong trường hợp đó.                                                                   |
| [areSymbols](areSymbols.md)                 | Kiểm tra xem một đầu vào có phải là mảng đã được điền mà các phần tử đều là symbol hay không, trả về `true` hoặc `false`.                                                                                       |
| [areTrue](areTrue.md)                       | Kiểm tra xem một mảng không rỗng có chỉ chứa các giá trị boolean `true` hay không.                                                                                                                              |
| [areUndefined](areUndefined.md)             | `areUndefined` kiểm tra liệu mọi phần tử trong một mảng được cung cấp có phải là `undefined` hay không.                                                                                                         |
| [areValidDates](areValidDates.md)           | Xác định liệu một mảng có khác rỗng và hoàn toàn bao gồm các đối tượng `Date` hợp lệ hay không.                                                                                                                 |
| [areWeakMaps](areWeakMaps.md)               | `areWeakMaps` kiểm tra liệu một giá trị có phải là một mảng không rỗng mà trong đó mọi phần tử đều là `WeakMap` hay không, chỉ trả về `true` trong trường hợp đó và `false` trong các trường hợp còn lại.       |
| [areWeakSets](areWeakSets.md)               | Kiểm tra xem đầu vào có phải là một mảng không rỗng mà mọi phần tử đều là `WeakSet` hay không, và chỉ trả về `true` trong trường hợp đó.                                                                        |
| [getTypeOf](getTypeOf.md)                   | `getTypeOf` trả về một nhãn kiểu chi tiết, dễ đọc cho con người cho một giá trị cho trước, bao gồm các kiểu số được tinh chỉnh và các loại đối tượng cụ thể.                                                    |
| [isArray](isArray.md)                       | `isArray` kiểm tra xem một giá trị cho trước có phải là mảng hay không và trả về `true` nếu đúng, ngược lại là `false`.                                                                                         |
| [isBigInt](isBigInt.md)                     | `isBigInt` kiểm tra liệu một giá trị đã cho có thuộc kiểu `bigint` hay không, trả về `true` cho các giá trị nguyên thủy BigInt và `false` trong các trường hợp khác.                                            |
| [isBinary](isBinary.md)                     | `isBinary` xác định liệu một giá trị có phải là chuỗi nhị phân (tùy chọn có tiền tố `0b`/`0B`) hay không và trả về `true` hoặc `false`.                                                                         |
| [isBoolean](isBoolean.md)                   | Xác định liệu một giá trị đã cho có phải là `boolean` hay không.                                                                                                                                                |
| [isBuffer](isBuffer.md)                     | Kiểm tra xem một giá trị có phải là `Buffer` của Node.js hay không và trả về `true` hoặc `false`.                                                                                                               |
| [isDate](isDate.md)                         | `isDate` xác định liệu một giá trị được cung cấp có phải là `Date` hay không, trả về `true` cho các thể hiện `Date` và `false` trong các trường hợp khác.                                                       |
| [isDecimal](isDecimal.md)                   | `isDecimal` kiểm tra liệu một giá trị có phải là biểu diễn chuỗi thập phân không, không có khoảng trắng ở đầu/cuối và có định dạng thập phân hợp lệ.                                                            |
| [isEmpty](isEmpty.md)                       | Xác định liệu một giá trị cho trước có rỗng hay không, trả về `true` cho `null`, `undefined`, chuỗi rỗng/chỉ có khoảng trắng, mảng rỗng, `Map`/`Set` rỗng, hoặc đối tượng không có thuộc tính enumerable riêng. |
| [isError](isError.md)                       | Kiểm tra xem `value` đã cho có phải là một thể hiện của `Error` hay không.                                                                                                                                      |
| [isFalse](isFalse.md)                       | `isFalse` kiểm tra xem một giá trị đã cho có bằng nghiêm ngặt với literal boolean `false` hay không.                                                                                                            |
| [isFilledArray](isFilledArray.md)           | Kiểm tra xem `value` có phải là một mảng có ít nhất một phần tử hay không, trả về `true` hoặc `false`.                                                                                                          |
| [isFinite](isFinite.md)                     | Xác định liệu `value` đã cho có phải là một `number` hữu hạn hay không.                                                                                                                                         |
| [isFloat](isFloat.md)                       | `isFloat` xác định liệu `value` đã cho có phải là một số dấu phẩy động hữu hạn hay không (một `number` không phải số nguyên).                                                                                   |
| [isHexadecimal](isHexadecimal.md)           | `isHexadecimal` kiểm tra liệu một giá trị đã cho có phải là literal chuỗi thập lục phân với tiền tố bắt buộc `0x`/`0X` hay không.                                                                               |
| [isIndexFound](isIndexFound.md)             | `isIndexFound` xác định liệu một giá trị cho trước có phải là số nguyên không âm hay không, cho biết rằng đã tìm thấy một chỉ mục.                                                                              |
| [isInteger](isInteger.md)                   | Xác định liệu `value` đã cho có phải là một số nguyên an toàn hay không.                                                                                                                                        |
| [isMap](isMap.md)                           | Kiểm tra xem `value` đã cho có phải là một `Map` hay không, trả về `true` nếu đúng và `false` nếu không.                                                                                                        |
| [isNaN](isNaN.md)                           | Xác định liệu `value` được cung cấp có phải là `NaN` thuộc kiểu `number` hay không mà không chuyển đổi chuỗi.                                                                                                   |
| [isNull](isNull.md)                         | Xác định liệu `value` được cung cấp có phải là `null` hay không.                                                                                                                                                |
| [isNullOrUndefined](isNullOrUndefined.md)   | Kiểm tra xem một giá trị cho trước có phải là `null` hoặc `undefined` hay không.                                                                                                                                |
| [isNumber](isNumber.md)                     | `isNumber` kiểm tra liệu một giá trị có phải là một số hữu hạn và không phải `NaN` hay không.                                                                                                                   |
| [isNumeric](isNumeric.md)                   | `isNumeric` xác định liệu một `value` nhất định có được coi là kiểu số hay không bằng cách kiểm tra kiểu đã được phân giải của nó so với `NUMERIC_TYPES`.                                                       |
| [isObject](isObject.md)                     | Xác định liệu `value` đã cho có phải là một `object` hay không (loại trừ `null`).                                                                                                                               |
| [isOctal](isOctal.md)                       | Xác định liệu một giá trị có phải là chuỗi literal bát phân hợp lệ hay không (ví dụ: `0o755`).                                                                                                                  |
| [isOfType](isOfType.md)                     | Xác định liệu `value` đã cho có khớp với một chuỗi kiểu được chỉ định hay không, dùng `typeof` cho các kiểu nguyên thủy và cơ chế dự phòng cho các kiểu phức tạp.                                               |
| [isOneOfType](isOneOfType.md)               | `isOneOfType` xác định liệu `value` đã cho có khớp với ít nhất một trong các chuỗi kiểu được cung cấp hay không, trả về `true` nếu tìm thấy bất kỳ kiểu nào khớp và `false` nếu không.                          |
| [isPlainObject](isPlainObject.md)           | Kiểm tra xem `value` đã cho có phải là một đối tượng thuần hay không và trả về `true` nếu đúng, nếu không thì `false`.                                                                                          |
| [isPrimitive](isPrimitive.md)               | `isPrimitive` xác định liệu một giá trị đã cho có phải là primitive (`null`, `undefined`, `boolean`, `number`, `string`, `bigint`, `symbol`) hay không.                                                         |
| [isPromise](isPromise.md)                   | Xác định liệu một giá trị đã cho có phải là `Promise` hay không.                                                                                                                                                |
| [isRegEx](isRegEx.md)                       | Xác định liệu một giá trị được cung cấp có phải là một instance của `RegExp` hay không.                                                                                                                         |
| [isSet](isSet.md)                           | Xác định liệu một giá trị đã cho có phải là `Set` hay không.                                                                                                                                                    |
| [isStream](isStream.md)                     | `isStream` kiểm tra xem một giá trị cho trước có phải là đối tượng stream hay không (giống stream của Node.js, `ReadableStream`, hoặc `WritableStream`).                                                        |
| [isString](isString.md)                     | `isString` xác định liệu một giá trị cho trước có phải là chuỗi hay không.                                                                                                                                      |
| [isSymbol](isSymbol.md)                     | `isSymbol` xác định liệu một giá trị cho trước có thuộc kiểu `symbol` hay không, trả về `true` cho symbol và `false` trong các trường hợp khác.                                                                 |
| [isTrue](isTrue.md)                         | `isTrue` xác định liệu một giá trị đã cho có bằng nghiêm ngặt với `true` hay không.                                                                                                                             |
| [isUndefined](isUndefined.md)               | Kiểm tra xem một giá trị cho trước có phải là `undefined` hay không.                                                                                                                                            |
| [isValidDate](isValidDate.md)               | `isValidDate` kiểm tra liệu một giá trị đã cho có phải là một đối tượng `Date` hợp lệ hay không và chỉ trả về `true` đối với các ngày thực, không bị vô hiệu.                                                   |
| [isWeakMap](isWeakMap.md)                   | Xác định liệu `value` đã cho có phải là một thể hiện `WeakMap` hay không.                                                                                                                                       |
| [isWeakSet](isWeakSet.md)                   | Xác định liệu `value` đã cho có phải là một `WeakSet` của các đối tượng hay không.                                                                                                                              |

## Các hàm đã so sánh

| Các hàm | Mô tả |
|---------|-------|
