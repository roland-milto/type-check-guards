# Riferimenti alle funzioni

## Funzioni

| Funzione                                    | Descrizione                                                                                                                                                                                                  |
|---------------------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| [areArrays](areArrays.md)                   | `areArrays` determina se un valore è un array bidimensionale pieno i cui elementi sono tutti array.                                                                                                          |
| [areBigInts](areBigInts.md)                 | `areBigInts` determina se un valore è un array non vuoto contenente solo valori `bigint`.                                                                                                                    |
| [areBinaries](areBinaries.md)               | Verifica se il valore fornito è un array non vuoto di stringhe binarie valide e restituisce `true` solo se tutti gli elementi superano la convalida.                                                         |
| [areBooleans](areBooleans.md)               | `areBooleans` verifica se un dato array non vuoto contiene solo valori booleani, restituendo `true` in tal caso e `false` altrimenti.                                                                        |
| [areBuffers](areBuffers.md)                 | `areBuffers` verifica se il valore fornito è un array non vuoto e popolato in cui ogni elemento è un `Buffer`, restituendo `true` in tal caso e `false` altrimenti.                                          |
| [areDates](areDates.md)                     | `areDates` determina se un dato array è valorizzato e contiene solo oggetti `Date`, restituendo `true` solo quando tutti gli elementi sono date valide.                                                      |
| [areDecimals](areDecimals.md)               | Verifica se tutti gli elementi di un array sono numeri decimali e se l'array è non vuoto, restituendo `true` o `false`.                                                                                      |
| [areEqual](areEqual.md)                     | `areEqual` verifica se tutti gli elementi di un array sono uguali a un determinato valore previsto, restituendo `true` solo per array non vuoti in cui ogni elemento corrisponde.                            |
| [areErrors](areErrors.md)                   | Verifica se un array è non vuoto e contiene solo oggetti `Error`, restituendo `true` o `false`.                                                                                                              |
| [areFalse](areFalse.md)                     | `areFalse` verifica se tutti gli elementi in un array fornito sono rigorosamente il booleano `false`.                                                                                                        |
| [areFilledArrays](areFilledArrays.md)       | `areFilledArrays` verifica se un array bidimensionale non è vuoto e se tutti i suoi sottoarray non sono vuoti.                                                                                               |
| [areFinite](areFinite.md)                   | `areFinite` verifica se un valore è un array non vuoto i cui elementi sono tutti numeri finiti, restituendo `true` in tal caso e `false` altrimenti.                                                         |
| [areFloats](areFloats.md)                   | `areFloats` verifica se un dato array è valorizzato e se tutti i suoi elementi sono float.                                                                                                                   |
| [areHexadecimals](areHexadecimals.md)       | Verifica se tutti gli elementi in un array sono stringhe esadecimali, restituendo `true` solo per array non vuoti in cui ogni elemento è valido.                                                             |
| [areIndexesFound](areIndexesFound.md)       | `areIndexesFound` verifica se un valore è un array non vuoto i cui elementi sono tutti indici validi, restituendo `true` se lo sono e `false` altrimenti.                                                    |
| [areIntegers](areIntegers.md)               | `areIntegers` determina se tutti gli elementi in un dato array sono interi, restituendo `true` se lo sono e `false` in caso contrario.                                                                       |
| [areJson](areJson.md)                       | Verifica se tutti gli elementi di un array sono stringhe JSON, restituendo `true` solo se l'array è popolato e ogni elemento è JSON valido; altrimenti restituisce `false`.                                  |
| [areMaps](areMaps.md)                       | `areMaps` determina se un dato array è non vuoto e se tutti i suoi elementi sono istanze di `Map`.                                                                                                           |
| [areNaNs](areNaNs.md)                       | `areNaNs` verifica se tutti gli elementi in un array sono `NaN` e restituisce `true` solo se ogni elemento è `NaN`.                                                                                          |
| [areNullOrUndefined](areNullOrUndefined.md) | Verifica se tutti gli elementi nell'array fornito sono `null` o `undefined`.                                                                                                                                 |
| [areNumbers](areNumbers.md)                 | `areNumbers` verifica se un valore è un array non vuoto in cui tutti gli elementi sono numeri.                                                                                                               |
| [areNumerics](areNumerics.md)               | `areNumerics` verifica se un valore è un array non vuoto in cui tutti gli elementi sono numerici.                                                                                                            |
| [areObjects](areObjects.md)                 | `areObjects` verifica se un array non vuoto fornito contiene solo oggetti.                                                                                                                                   |
| [areOctals](areOctals.md)                   | `areOctals` determina se il valore fornito è un array non vuoto di stringhe ottali valide.                                                                                                                   |
| [areOfType](areOfType.md)                   | Verifica se tutti gli elementi nell'`array` fornito sono del `type` specificato.                                                                                                                             |
| [areOneOfType](areOneOfType.md)             | `areOneOfType` verifica se tutti gli elementi in un array non vuoto appartengono a uno dei tipi runtime specificati.                                                                                         |
| [arePlainObjects](arePlainObjects.md)       | Verifica se tutti gli elementi di un array sono oggetti semplici, restituendo `true` solo se ogni elemento soddisfa i requisiti.                                                                             |
| [arePrimitives](arePrimitives.md)           | `arePrimitives` valuta se tutti gli elementi in un array fornito e non vuoto sono tipi primitivi.                                                                                                            |
| [arePromises](arePromises.md)               | `arePromises` determina se tutti gli elementi in un array sono istanze di `Promise`.                                                                                                                         |
| [areRegExes](areRegExes.md)                 | `areRegExes` verifica se un valore è un array valorizzato che contiene solo oggetti `RegExp`.                                                                                                                |
| [areSets](areSets.md)                       | Verifica se un dato array non vuoto contiene solo istanze di `Set`, restituendo `true` in tal caso e `false` altrimenti.                                                                                     |
| [areStreams](areStreams.md)                 | `areStreams` determina se un valore è un array non vuoto in cui ogni elemento è uno `Stream`.                                                                                                                |
| [areStrings](areStrings.md)                 | `areStrings` verifica se un array è non vuoto e se tutti i suoi elementi sono stringhe, restituendo `true` solo in quel caso.                                                                                |
| [areSymbols](areSymbols.md)                 | Verifica se un input è un array riempito i cui elementi sono tutti simboli, restituendo `true` o `false`.                                                                                                    |
| [areTrue](areTrue.md)                       | Verifica se un array non vuoto contiene solo valori booleani `true`.                                                                                                                                         |
| [areUndefined](areUndefined.md)             | `areUndefined` verifica se ogni elemento in un array fornito è `undefined`.                                                                                                                                  |
| [areValidDates](areValidDates.md)           | Determina se un array è non vuoto ed è composto interamente da oggetti `Date` validi.                                                                                                                        |
| [areWeakMaps](areWeakMaps.md)               | `areWeakMaps` verifica se un valore è un array non vuoto in cui ogni elemento è un `WeakMap`, restituendo `true` solo in quel caso e `false` altrimenti.                                                     |
| [areWeakSets](areWeakSets.md)               | Verifica se un input è un array non vuoto in cui ogni elemento è un `WeakSet`, restituendo `true` solo in quel caso.                                                                                         |
| [getTypeOf](getTypeOf.md)                   | `getTypeOf` restituisce un’etichetta di tipo dettagliata e leggibile dall’uomo per un dato valore, includendo tipi numerici raffinati e specifiche categorie di oggetti.                                     |
| [isArray](isArray.md)                       | `isArray` verifica se un determinato valore è un array e restituisce `true` se lo è, altrimenti `false`.                                                                                                     |
| [isBigInt](isBigInt.md)                     | `isBigInt` verifica se un determinato valore è di tipo `bigint`, restituendo `true` per i primitivi BigInt e `false` altrimenti.                                                                             |
| [isBinary](isBinary.md)                     | `isBinary` determina se un valore è una stringa binaria (facoltativamente con prefisso `0b`/`0B`) e restituisce `true` o `false`.                                                                            |
| [isBoolean](isBoolean.md)                   | Determina se un determinato valore è un `boolean`.                                                                                                                                                           |
| [isBuffer](isBuffer.md)                     | Verifica se un valore è un `Buffer` di Node.js e restituisce `true` o `false`.                                                                                                                               |
| [isDate](isDate.md)                         | `isDate` determina se un valore fornito è un `Date`, restituendo `true` per le istanze di `Date` e `false` altrimenti.                                                                                       |
| [isDecimal](isDecimal.md)                   | `isDecimal` verifica se un valore è una rappresentazione in stringa di un numero decimale senza spazi bianchi iniziali/finali e con un formato decimale valido.                                              |
| [isEmpty](isEmpty.md)                       | Determina se un determinato valore è vuoto, restituendo `true` per `null`, `undefined`, stringhe vuote/composte solo da spazi, array vuoti, `Map`/`Set` vuoti o oggetti senza proprietà enumerabili proprie. |
| [isError](isError.md)                       | Verifica se un determinato `value` è un'istanza di `Error`.                                                                                                                                                  |
| [isFalse](isFalse.md)                       | `isFalse` verifica se un determinato valore è strettamente uguale al letterale booleano `false`.                                                                                                             |
| [isFilledArray](isFilledArray.md)           | Verifica se `value` è un array con almeno un elemento, restituendo `true` o `false`.                                                                                                                         |
| [isFinite](isFinite.md)                     | Determina se un dato `value` è un `number` finito.                                                                                                                                                           |
| [isFloat](isFloat.md)                       | `isFloat` determina se un dato `value` è un numero in virgola mobile finito (un `number` che non è un intero).                                                                                               |
| [isHexadecimal](isHexadecimal.md)           | `isHexadecimal` verifica se un determinato valore è un letterale di stringa esadecimale con prefisso `0x`/`0X` obbligatorio.                                                                                 |
| [isIndexFound](isIndexFound.md)             | `isIndexFound` determina se un dato valore è un intero non negativo, indicando che è stato trovato un indice.                                                                                                |
| [isInteger](isInteger.md)                   | Determina se un dato `value` è un numero intero sicuro.                                                                                                                                                      |
| [isMap](isMap.md)                           | Verifica se il `value` fornito è una `Map`, restituendo `true` se lo è e `false` altrimenti.                                                                                                                 |
| [isNaN](isNaN.md)                           | Determina se il `value` fornito è un `NaN` di tipo `number` senza convertire le stringhe.                                                                                                                    |
| [isNull](isNull.md)                         | Determina se il `value` fornito è `null`.                                                                                                                                                                    |
| [isNullOrUndefined](isNullOrUndefined.md)   | Verifica se un determinato valore è `null` o `undefined`.                                                                                                                                                    |
| [isNumber](isNumber.md)                     | `isNumber` verifica se un valore è un numero finito e non `NaN`.                                                                                                                                             |
| [isNumeric](isNumeric.md)                   | `isNumeric` determina se un dato `value` è considerato numerico verificando il suo tipo risolto rispetto a `NUMERIC_TYPES`.                                                                                  |
| [isObject](isObject.md)                     | Determina se un dato `value` è un `object` (escludendo `null`).                                                                                                                                              |
| [isOctal](isOctal.md)                       | Determina se un valore è una stringa di letterale ottale valida (ad es. `0o755`).                                                                                                                            |
| [isOfType](isOfType.md)                     | Determina se un determinato `value` corrisponde a una stringa di tipo specificata, usando `typeof` per i primitivi e un fallback per i tipi complessi.                                                       |
| [isOneOfType](isOneOfType.md)               | `isOneOfType` determina se un determinato `value` corrisponde ad almeno una delle stringhe di tipo fornite, restituendo `true` se viene trovata una corrispondenza e `false` altrimenti.                     |
| [isPlainObject](isPlainObject.md)           | Verifica se un dato `value` è un oggetto semplice e restituisce `true` se lo è, altrimenti `false`.                                                                                                          |
| [isPrimitive](isPrimitive.md)               | `isPrimitive` determina se un determinato valore è un primitivo (`null`, `undefined`, `boolean`, `number`, `string`, `bigint`, `symbol`).                                                                    |
| [isPromise](isPromise.md)                   | Determina se un dato valore è una `Promise`.                                                                                                                                                                 |
| [isRegEx](isRegEx.md)                       | Determina se un valore fornito è un'istanza di `RegExp`.                                                                                                                                                     |
| [isSet](isSet.md)                           | Determina se un determinato valore è un `Set`.                                                                                                                                                               |
| [isStream](isStream.md)                     | `isStream` verifica se un determinato valore è un oggetto stream (simile a stream di Node.js, `ReadableStream` o `WritableStream`).                                                                          |
| [isString](isString.md)                     | `isString` determina se un determinato valore è una stringa.                                                                                                                                                 |
| [isSymbol](isSymbol.md)                     | `isSymbol` determina se un determinato valore è di tipo `symbol`, restituendo `true` per i symbol e `false` altrimenti.                                                                                      |
| [isTrue](isTrue.md)                         | `isTrue` determina se un determinato valore è strettamente uguale a `true`.                                                                                                                                  |
| [isUndefined](isUndefined.md)               | Verifica se un determinato valore è `undefined`.                                                                                                                                                             |
| [isValidDate](isValidDate.md)               | `isValidDate` verifica se un determinato valore è un oggetto `Date` valido e restituisce `true` solo per date reali e non invalide.                                                                          |
| [isWeakMap](isWeakMap.md)                   | Determina se un dato `value` è un’istanza di `WeakMap`.                                                                                                                                                      |
| [isWeakSet](isWeakSet.md)                   | Determina se il `value` fornito è un `WeakSet` di oggetti.                                                                                                                                                   |

## Funzioni confrontate

| Funzioni | Descrizione |
|----------|-------------|
