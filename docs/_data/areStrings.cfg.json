{
	"sourceLanguage": "eng",
	"referenceFileHash": "9bfd5950de934048e3300746eef3544188001add0e57f1cd088957f168cd31a3",
	"aiPromptHash": "9f5ec9e4ef2b88305876eee7c9d77766b651784d11b77ecf8ef81bd005620fa5",
	"translatedTo": {
		"eng": "`areStrings` checks whether an array is non-empty and all its elements are strings, returning `true` only in that case.",
		"ara": "تتحقق `areStrings` مما إذا كانت المصفوفة غير فارغة وكانت جميع عناصرها سلاسل نصية، وتُرجع `true` فقط في هذه الحالة.",
		"afr": "`areStrings` kontroleer of 'n skikking nie-leeg is en al sy elemente strings is, en gee slegs in daardie geval `true` terug.",
		"arz": "`areStrings` بتشيّك إذا كانت الـ array مش فاضية و كل عناصرها strings، و بترجّع `true` بس في الحالة دي.",
		"bel": "`areStrings` правярае, ці з'яўляецца масіў непустым і ці ўсе яго элементы — радкі, вяртаючы `true` толькі ў гэтым выпадку.",
		"ben": "`areStrings` পরীক্ষা করে একটি অ্যারে অ-খালি কি না এবং তার সব উপাদান স্ট্রিং কি না; কেবল সেই ক্ষেত্রেই `true` রিটার্ন করে।",
		"bho": "`areStrings` जाँचेला कि ऐरे गैर-खाली बा आ ओकर सभे तत्व स्ट्रिंग बा कि ना, आ खाली ओही हालत में `true` लौटावेला।",
		"bos": "`areStrings` provjerava da li je niz neprazan i da su svi njegovi elementi stringovi, vraćajući `true` samo u tom slučaju.",
		"cat": "`areStrings` comprova si un array no és buit i si tots els seus elements són cadenes, retornant `true` només en aquest cas.",
		"bul": "`areStrings` проверява дали масивът е непразен и дали всички негови елементи са низове, като връща `true` само в този случай.",
		"cjy": "`areStrings` 检查一个数组是否非空并且所有元素都是字符串，只有在这种情况下才返回 `true`。",
		"ces": "`areStrings` kontroluje, zda je pole neprázdné a všechny jeho prvky jsou řetězce; `true` vrací pouze v tomto případě.",
		"cmn": "`areStrings` 用于检查一个数组是否为非空且其所有元素都是字符串；仅在这种情况下返回 `true`。",
		"ckb": "`areStrings` پشکنین دەکات کە ئایا ئارایەکە نەنەوەیە و هەموو توخمەکانی سترینگن، و تەنها لەو حاڵەتەدا `true` دەگەڕێنێتەوە.",
		"deu": "`areStrings` prüft, ob ein Array nicht leer ist und alle seine Elemente Zeichenketten sind, und gibt nur in diesem Fall `true` zurück.",
		"cym": "Mae `areStrings` yn gwirio a yw arae yn ddi-wag ac a yw ei holl elfennau yn linynnau, gan ddychwelyd `true` dim ond yn yr achos hwnnw.",
		"dan": "`areStrings` kontrollerer, om et array er ikke-tomt, og om alle dets elementer er strenge, og returnerer kun `true` i det tilfælde.",
		"ell": "Το `areStrings` ελέγχει αν ένας πίνακας είναι μη κενός και αν όλα τα στοιχεία του είναι συμβολοσειρές, επιστρέφοντας `true` μόνο σε αυτή την περίπτωση.",
		"est": "`areStrings` kontrollib, kas massiiv on mittetühi ja kõik selle elemendid on stringid, tagastades `true` ainult sel juhul.",
		"fas": "`areStrings` بررسی می‌کند آیا یک آرایه غیرخالی است و همهٔ عناصر آن رشته هستند یا نه، و فقط در همان حالت `true` برمی‌گرداند.",
		"fra": "`areStrings` vérifie si un tableau est non vide et si tous ses éléments sont des chaînes de caractères, en renvoyant `true` uniquement dans ce cas.",
		"fin": "`areStrings` tarkistaa, onko taulukko ei-tyhjä ja ovatko kaikki sen alkiot merkkijonoja, ja palauttaa `true` vain siinä tapauksessa.",
		"hak": "`areStrings` 會檢查一個陣列係毋係非空，且佢个全部元素都係字串；只有符合這兩項條件正會回傳 `true`。",
		"gsw": "`areStrings` prüeft, öb es Array nöd läär isch und alli sini Element Strings sind, und git nume i dem Fall `true` zrugg.",
		"gle": "Seiceálann `areStrings` an bhfuil eagar neamhfholamh agus an bhfuil a chuid eilimintí go léir ina dteaghráin, agus ní thugann sé `true` ar ais ach sa chás sin.",
		"heb": "`areStrings` בודקת האם מערך אינו ריק וכל רכיביו הם מחרוזות, ומחזירה `true` רק במקרה זה.",
		"hin": "`areStrings` जांचता है कि कोई array गैर-खाली है और उसके सभी तत्व strings हैं या नहीं; केवल उसी स्थिति में `true` लौटाता है।",
		"hrv": "`areStrings` provjerava je li niz neprazan i jesu li svi njegovi elementi nizovi znakova te vraća `true` samo u tom slučaju.",
		"hsn": "`areStrings` check kardā hai ki array non-empty hai ate us dē sāre elements strings han, te `true` sirf usī hālat vich wapas kardā hai.",
		"ind": "`areStrings` memeriksa apakah sebuah array tidak kosong dan semua elemennya adalah string, mengembalikan `true` hanya dalam kasus tersebut.",
		"ita": "`areStrings` verifica se un array è non vuoto e se tutti i suoi elementi sono stringhe, restituendo `true` solo in quel caso.",
		"hun": "Az `areStrings` ellenőrzi, hogy egy tömb nem üres-e, és minden eleme string-e; csak ebben az esetben ad vissza `true` értéket.",
		"isl": "`areStrings` athugar hvort fylki sé ekki tómt og að öll stök þess séu strengir, og skilar `true` aðeins í því tilviki.",
		"jav": "`areStrings` mriksa apa sawijining array ora kosong lan kabeh unsuré iku string, mung mbalekake `true` yen mung ing kahanan kuwi.",
		"kat": "`areStrings` ამოწმებს, არის თუ არა მასივი არაცარიელი და მისი ყველა ელემენტი სტრინგი, და `true`-ს აბრუნებს მხოლოდ ამ შემთხვევაში.",
		"jpn": "`areStrings` は、配列が空でなく、かつ全要素が文字列であるかを確認し、その場合にのみ `true` を返します。",
		"kor": "`areStrings`는 배열이 비어 있지 않고 모든 요소가 문자열인지 확인하며, 그 경우에만 `true`를 반환합니다.",
		"lav": "`areStrings` pārbauda, vai masīvs ir netukšs un visi tā elementi ir virknes, atgriežot `true` tikai šajā gadījumā.",
		"lit": "`areStrings` patikrina, ar masyvas yra netuščias ir ar visi jo elementai yra eilutės, ir grąžina `true` tik tokiu atveju.",
		"kur": "`areStrings` پشکنین دەکات کە ئایا ئارایەکە نەنەوەیە و هەموو توخمەکانی شتڕینگن، و تەنها لەو حاڵەتەدا `true` دەگەڕێنێتەوە.",
		"mar": "`areStrings` तपासते की अ‍ॅरे न-रिकामी आहे का आणि त्यातील सर्व घटक स्ट्रिंग्स आहेत का; फक्त त्या स्थितीतच `true` परत करते.",
		"mkd": "`areStrings` проверува дали една низа е непразна и дали сите нејзини елементи се низи (strings), враќајќи `true` само во тој случај.",
		"mya": "`areStrings` သည် array တစ်ခု မအလွတ်ဖြစ်ပြီး ၎င်း၏ အင်္ဂါရပ်အားလုံးသည် string များဖြစ်ကြောင်း စစ်ဆေးကာ ထိုအခြေအနေတွင်သာ `true` ကို ပြန်ပေးသည်။",
		"nan": "`areStrings` 會檢查一个陣列敢是無空而且內底攏是字串，只有佇這種情形才會回傳 `true`。",
		"nep": "`areStrings` ले कुनै एरे गैर-खाली छ र त्यसका सबै तत्वहरू स्ट्रिङ हुन् कि होइनन् भनेर जाँच गर्छ, र त्यो अवस्थामा मात्र `true` फर्काउँछ।",
		"nno": "`areStrings` sjekkar om ein tabell er ikkje-tom og om alle elementa er strengar, og returnerer `true` berre i det tilfellet.",
		"nob": "`areStrings` sjekker om en array er ikke-tom og om alle elementene er strenger, og returnerer `true` kun i det tilfellet.",
		"nld": "`areStrings` controleert of een array niet leeg is en of al zijn elementen strings zijn, en retourneert alleen in dat geval `true`.",
		"orm": "`areStrings` array tokko duwwaa hin taanee fi elementoonni isaa hundi string ta’uu isaa ni sakatta’a; haala sana keessatti qofa `true` deebisa.",
		"nor": "`areStrings` sjekker om en array er ikke-tom og om alle elementene er strenger, og returnerer `true` kun i det tilfellet.",
		"pan": "`areStrings` ਚੈੱਕ ਕਰਦਾ ਹੈ ਕਿ ਕੀ ਇੱਕ ਐਰੇ ਗੈਰ-ਖਾਲੀ ਹੈ ਅਤੇ ਉਸਦੇ ਸਾਰੇ ਐਲਿਮੈਂਟ ਸਟਰਿੰਗ ਹਨ; ਸਿਰਫ਼ ਉਸ ਹਾਲਤ ਵਿੱਚ ਹੀ `true` ਵਾਪਸ ਕਰਦਾ ਹੈ।",
		"pcm": "`areStrings` dey check whether array no empty and all im elements na strings, e go return `true` only for dat case.",
		"pes": "`areStrings` بررسی می‌کند که آیا یک آرایه غیرخالی است و همهٔ عناصر آن رشته هستند یا نه، و فقط در همان حالت `true` برمی‌گرداند.",
		"pol": "`areStrings` sprawdza, czy tablica jest niepusta i czy wszystkie jej elementy są ciągami znaków, zwracając `true` tylko w takim przypadku.",
		"por": "`areStrings` verifica se um array não está vazio e se todos os seus elementos são strings, retornando `true` apenas nesse caso.",
		"pus": "`areStrings` ګوري چې اراې ناتشه ده او ټول عناصر يې سټرېنګونه دي؛ يوازې په هماغه حالت کې `true` ستنوي.",
		"rus": "`areStrings` проверяет, что массив непустой и все его элементы — строки, возвращая `true` только в этом случае.",
		"ron": "`areStrings` verifică dacă un tablou este ne-gol și dacă toate elementele sale sunt șiruri, returnând `true` doar în acest caz.",
		"slk": "`areStrings` kontroluje, či je pole neprázdne a či sú všetky jeho prvky reťazce; `true` vráti iba v takom prípade.",
		"spa": "`areStrings` comprueba si un arreglo no está vacío y si todos sus elementos son cadenas, devolviendo `true` solo en ese caso.",
		"slv": "`areStrings` preveri, ali je polje neprazno in ali so vsi njegovi elementi nizi, ter vrne `true` samo v tem primeru.",
		"sqi": "`areStrings` kontrollon nëse një varg është jo-bosh dhe nëse të gjithë elementët e tij janë vargje (strings), duke kthyer `true` vetëm në atë rast.",
		"swa": "`areStrings` hukagua kama safu si tupu na vipengele vyake vyote ni kamba, na hurudisha `true` tu katika hali hiyo.",
		"srp": "`areStrings` проверава да ли је низ непразан и да ли су сви његови елементи стрингови, враћајући `true` само у том случају.",
		"swe": "`areStrings` kontrollerar om en array är icke-tom och att alla dess element är strängar, och returnerar `true` endast i det fallet.",
		"tel": "`areStrings` ఒక అరే ఖాళీ కాకుండా ఉందా మరియు దాని అన్ని అంశాలు స్ట్రింగ్‌లేనా అని తనిఖీ చేస్తుంది; ఆ సందర్భంలో మాత్రమే `true` ను తిరిగి ఇస్తుంది.",
		"tam": "`areStrings` ஒரு வரிசை காலியல்லாததா மற்றும் அதன் அனைத்து உறுப்புகளும் சரங்களா என்பதைச் சரிபார்த்து, அந்த நிலையில் மட்டும் `true` திருப்புகிறது.",
		"tgl": "Sinusuri ng `areStrings` kung ang isang array ay hindi walang laman at lahat ng mga elemento nito ay mga string, at nagbabalik lamang ng `true` sa kasong iyon.",
		"tur": "`areStrings`, bir dizinin boş olmadığını ve tüm öğelerinin string olduğunu kontrol eder; yalnızca bu durumda `true` döndürür.",
		"tha": "`areStrings` ตรวจสอบว่าอาร์เรย์ไม่ว่างและองค์ประกอบทั้งหมดเป็นสตริง โดยจะคืนค่า `true` เฉพาะในกรณีนั้นเท่านั้น",
		"ukr": "`areStrings` перевіряє, чи масив є непорожнім і чи всі його елементи є рядками, повертаючи `true` лише в цьому випадку.",
		"uig": "`areStrings` بىر سانلار گۇرۇپپىسىنىڭ بوش ئەمەسلىكىنى ۋە ئۇنىڭ بارلىق ئېلېمېنتلىرىنىڭ تىزىم (string) ئىكەنلىكىنى تەكشۈرىدۇ؛ پەقەت شۇ ھالەتتەلا `true` قايتۇرىدۇ.",
		"urd": "`areStrings` چیک کرتا ہے کہ آیا کوئی array غیر خالی ہے اور اس کے تمام عناصر strings ہیں، اور صرف اسی صورت میں `true` واپس کرتا ہے۔",
		"vie": "`areStrings` kiểm tra liệu một mảng có không rỗng và tất cả các phần tử của nó đều là chuỗi hay không, chỉ trả về `true` trong trường hợp đó.",
		"uzb": "`areStrings` massiv bo‘sh emasligini va uning barcha elementlari satr ekanini tekshiradi; faqat shu holatda `true` qaytaradi.",
		"wuu": "`areStrings` 检查一个数组是不是非空，并且里向所有元素侪是字符串；只有符合这两点才返回 `true`。",
		"zho": "`areStrings` 用于检查一个数组是否为非空且其所有元素都是字符串；仅在这种情况下返回 `true`。",
		"yue": "`areStrings` 會檢查一個陣列係咪非空，而且所有元素都係字串；只有喺呢種情況先會回傳 `true`。",
		"zha": "`areStrings` roxnaengz gyoengq daih gwnz raeuz mbouj empty laengz gyoengq daih roengz ndei mbouj string, laengz ndaej `true` gwnz raeuz ndei mbouj gwnz daengz.",
		"zsm": "`areStrings` menyemak sama ada sesuatu tatasusunan tidak kosong dan semua elemennya ialah rentetan, memulangkan `true` hanya dalam keadaan itu."
	},
	"subject": []
}