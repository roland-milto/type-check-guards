{
	"sourceLanguage": "eng",
	"referenceFileHash": "8c09c46ef993efe152f4b66c497dcd0ad135d76db3b1883611f3c4dc745d5361",
	"aiPromptHash": "9f5ec9e4ef2b88305876eee7c9d77766b651784d11b77ecf8ef81bd005620fa5",
	"translatedTo": {
		"eng": "`isArray` checks whether a given value is an array and returns `true` if it is, otherwise `false`.",
		"arz": "`isArray` بيشيّك إذا كانت قيمة معيّنة Array وبيرجّع `true` لو هي كده، وإلا `false`.",
		"afr": "`isArray` kontroleer of 'n gegewe waarde 'n skikking is en gee `true` terug as dit is, anders `false`.",
		"ara": "تتحقق `isArray` مما إذا كانت قيمة معيّنة مصفوفة وتعيد `true` إذا كانت كذلك، وإلا فتعيد `false`.",
		"ben": "`isArray` পরীক্ষা করে প্রদত্ত মানটি অ্যারে কি না এবং অ্যারে হলে `true`, নইলে `false` ফেরত দেয়।",
		"bel": "`isArray` правярае, ці з'яўляецца зададзенае значэнне масівам, і вяртае `true`, калі так, у адваротным выпадку — `false`.",
		"bho": "`isArray` जांचेला कि दिहल गइल मान array बा कि ना आ अगर बा त `true` लौटावेला, नाहीं त `false`।",
		"bos": "`isArray` provjerava da li je data vrijednost niz i vraća `true` ako jeste, u suprotnom `false`.",
		"bul": "`isArray` проверява дали дадена стойност е масив и връща `true`, ако е така, в противен случай `false`.",
		"cat": "`isArray` comprova si un valor donat és un array i retorna `true` si ho és; altrament, `false`.",
		"ces": "`isArray` kontroluje, zda je daná hodnota pole, a vrátí `true`, pokud ano, jinak `false`.",
		"cjy": "`isArray` 检查给定个值系唔系数组；系就返回 `true`，唔系就返回 `false`。",
		"cmn": "`isArray` 用于检查给定值是否为数组；如果是则返回 `true`，否则返回 `false`。",
		"ckb": "`isArray` پشکنین دەکات کە ئایا بەهایەکی دیاریکراو ئارایە یان نا و ئەگەر ئارای بوو `true` دەگەڕێنێتەوە، ئەگەر نا `false`.",
		"dan": "`isArray` kontrollerer, om en given værdi er et array, og returnerer `true`, hvis den er det, ellers `false`.",
		"cym": "Mae `isArray` yn gwirio a yw gwerth penodol yn arae ac yn dychwelyd `true` os ydyw, fel arall `false`.",
		"deu": "`isArray` prüft, ob ein gegebener Wert ein Array ist, und gibt `true` zurück, wenn dies der Fall ist, andernfalls `false`.",
		"ell": "Το `isArray` ελέγχει αν μια δεδομένη τιμή είναι πίνακας και επιστρέφει `true` αν είναι, αλλιώς `false`.",
		"est": "`isArray` kontrollib, kas antud väärtus on massiiv, ja tagastab `true`, kui on, vastasel juhul `false`.",
		"fas": "`isArray` بررسی می‌کند که آیا یک مقدارِ داده‌شده آرایه است یا نه و اگر آرایه باشد `true` و در غیر این صورت `false` برمی‌گرداند.",
		"fin": "`isArray` tarkistaa, onko annettu arvo taulukko, ja palauttaa `true`, jos on, muuten `false`.",
		"fra": "`isArray` vérifie si une valeur donnée est un tableau et renvoie `true` si c’est le cas, sinon `false`.",
		"gle": "Seiceálann `isArray` an bhfuil luach tugtha ina eagar agus filleann sé `true` má tá, murach sin `false`.",
		"gsw": "`isArray` prüeft, öb en gegebene Wert es Array isch, und git `true` zrugg, wänn ja, susch `false`.",
		"hak": "`isArray` kiem-cha chi̍t ê gîng-thi̍t value sī-m̄-sī array, sī ê chêng-hông thòi-chhut `true`, m̄-sī thì thòi-chhut `false`.",
		"heb": "`isArray` בודק האם ערך נתון הוא מערך ומחזיר `true` אם כן, אחרת `false`.",
		"hin": "`isArray` जाँचता है कि दिया गया मान एक ऐरे है या नहीं और यदि है तो `true` लौटाता है, अन्यथा `false`।",
		"hrv": "`isArray` provjerava je li zadana vrijednost polje i vraća `true` ako jest, inače `false`.",
		"hsn": "`isArray` check kare hai ki diya gaya value array hai ki nai, aur agar hai to `true` return kare hai, nai to `false`.",
		"hun": "Az `isArray` ellenőrzi, hogy egy adott érték tömb-e, és `true`-t ad vissza, ha igen, különben `false`-t.",
		"ind": "`isArray` memeriksa apakah suatu nilai yang diberikan adalah array dan mengembalikan `true` jika ya, jika tidak `false`.",
		"isl": "`isArray` athugar hvort tiltekið gildi sé fylki og skilar `true` ef svo er, annars `false`.",
		"ita": "`isArray` verifica se un determinato valore è un array e restituisce `true` se lo è, altrimenti `false`.",
		"jav": "`isArray` mriksa apa sawijining nilai iku array lan mbalekake `true` yen iya, yen ora `false`.",
		"jpn": "`isArray` は指定された値が配列かどうかを確認し、配列であれば `true`、そうでなければ `false` を返します。",
		"kat": "`isArray` ამოწმებს, არის თუ არა მოცემული მნიშვნელობა მასივი და თუ არის, აბრუნებს `true`-ს, წინააღმდეგ შემთხვევაში `false`-ს.",
		"kor": "`isArray`는 주어진 값이 배열인지 확인하고, 배열이면 `true`를 반환하며 그렇지 않으면 `false`를 반환합니다.",
		"lav": "`isArray` pārbauda, vai dotā vērtība ir masīvs, un atgriež `true`, ja tā ir, pretējā gadījumā `false`.",
		"kur": "`isArray` kontrol dike ka nirxek diyarkirî rêz e an na û heke rêz be `true` vedigerîne, wekî din `false`.",
		"lit": "`isArray` patikrina, ar pateikta reikšmė yra masyvas, ir grąžina `true`, jei taip, kitu atveju `false`.",
		"mar": "`isArray` दिलेले मूल्य array आहे का ते तपासते आणि असल्यास `true`, अन्यथा `false` परत करते.",
		"mkd": "`isArray` проверува дали дадена вредност е низа и враќа `true` ако е, во спротивно `false`.",
		"mya": "`isArray` သည် ပေးထားသော တန်ဖိုးသည် array ဟုတ်မဟုတ်ကို စစ်ဆေးပြီး ဟုတ်ပါက `true` ကို ပြန်ပေးသည်၊ မဟုတ်ပါက `false` ကို ပြန်ပေးသည်။",
		"nan": "`isArray` 會檢查指定的值是否為陣列；若是則回傳 `true`，否則回傳 `false`。",
		"nep": "`isArray` ले दिइएको मान array हो कि होइन भनेर जाँच गर्छ र array भए `true` फर्काउँछ, नभए `false`।",
		"nld": "`isArray` controleert of een gegeven waarde een array is en retourneert `true` als dat zo is, anders `false`.",
		"nno": "`isArray` sjekkar om ein gitt verdi er ein array og returnerer `true` dersom han er det, elles `false`.",
		"nob": "`isArray` sjekker om en gitt verdi er en array og returnerer `true` hvis den er det, ellers `false`.",
		"nor": "`isArray` sjekker om en gitt verdi er en array og returnerer `true` hvis den er det, ellers `false`.",
		"pan": "`isArray` ਜਾਂਚਦਾ ਹੈ ਕਿ ਦਿੱਤਾ ਗਿਆ ਮੁੱਲ ਐਰੇ ਹੈ ਜਾਂ ਨਹੀਂ ਅਤੇ ਜੇ ਹੈ ਤਾਂ `true` ਵਾਪਸ ਕਰਦਾ ਹੈ, ਨਹੀਂ ਤਾਂ `false`।",
		"pcm": "`isArray` dey check whether value wey you give am na array, and e go return `true` if na array; if no be array, e go return `false`.",
		"orm": "`isArray` gatiin kenname array ta'uu isaa ni sakatta'a; yoo array ta'e `true` deebisa, yoo hin taane `false`.",
		"pes": "`isArray` بررسی می‌کند آیا مقدار داده‌شده یک آرایه است و اگر باشد `true` و در غیر این صورت `false` برمی‌گرداند.",
		"por": "`isArray` verifica se um determinado valor é um array e retorna `true` se for, caso contrário `false`.",
		"pol": "`isArray` sprawdza, czy podana wartość jest tablicą, i zwraca `true`, jeśli tak jest, w przeciwnym razie `false`.",
		"ron": "`isArray` verifică dacă o valoare dată este un tablou și returnează `true` dacă este, altfel `false`.",
		"rus": "`isArray` проверяет, является ли заданное значение массивом, и возвращает `true`, если это так, иначе `false`.",
		"pus": "`isArray` ګوري چې ورکړل شوی ارزښت اراي دی که نه، او که اراي وي `true` بېرته ورکوي، کنه `false`.",
		"slk": "`isArray` kontroluje, či je daná hodnota pole, a vráti `true`, ak ním je, inak `false`.",
		"slv": "`isArray` preveri, ali je podana vrednost polje, in vrne `true`, če je, sicer `false`.",
		"spa": "`isArray` comprueba si un valor dado es un array y devuelve `true` si lo es; de lo contrario, `false`.",
		"sqi": "`isArray` kontrollon nëse një vlerë e dhënë është një varg dhe kthen `true` nëse është, përndryshe `false`.",
		"srp": "`isArray` проверава да ли је дата вредност низ и враћа `true` ако јесте, у супротном `false`.",
		"swa": "`isArray` hukagua kama thamani iliyotolewa ni safu (array) na hurejesha `true` ikiwa ni hivyo, vinginevyo `false`.",
		"swe": "`isArray` kontrollerar om ett givet värde är en array och returnerar `true` om det är det, annars `false`.",
		"tam": "`isArray` கொடுக்கப்பட்ட மதிப்பு ஒரு வரிசையா என்பதைச் சரிபார்த்து, அது வரிசையாக இருந்தால் `true`-ஐ, இல்லையெனில் `false`-ஐ திருப்புகிறது.",
		"tel": "`isArray` ఇచ్చిన విలువ ఒక అరేనా కాదా అని తనిఖీ చేసి, అరే అయితే `true` లేకపోతే `false` ను తిరిగి ఇస్తుంది.",
		"tgl": "Sinusuri ng `isArray` kung ang ibinigay na value ay isang array at nagbabalik ng `true` kung oo, kung hindi ay `false`.",
		"tha": "`isArray` ตรวจสอบว่าค่าที่กำหนดเป็นอาร์เรย์หรือไม่ และคืนค่า `true` หากเป็นอาร์เรย์ มิฉะนั้นคืนค่า `false`",
		"tur": "`isArray`, verilen bir değerin dizi olup olmadığını kontrol eder ve öyleyse `true`, değilse `false` döndürür.",
		"ukr": "`isArray` перевіряє, чи є задане значення масивом, і повертає `true`, якщо так, інакше `false`.",
		"uig": "`isArray` بېرىلگەن قىممەتنىڭ تىزىملىك (array) ئىكەنلىكىنى تەكشۈرىدۇ ۋە ئەگەر شۇنداق بولسا `true` نى، بولمىسا `false` نى قايتۇرىدۇ.",
		"urd": "`isArray` چیک کرتا ہے کہ آیا دی گئی ویلیو ایک اَرے ہے اور اگر ہے تو `true` واپس کرتا ہے، ورنہ `false`۔",
		"uzb": "`isArray` berilgan qiymat massiv ekanligini tekshiradi va agar shunday bo‘lsa `true`, aks holda `false` qaytaradi.",
		"vie": "`isArray` kiểm tra xem một giá trị cho trước có phải là mảng hay không và trả về `true` nếu đúng, ngược lại là `false`.",
		"wuu": "`isArray` 检查给定个值是不是数组；是就返回 `true`，勿是就返回 `false`。",
		"yue": "`isArray` 會檢查指定嘅值係咪陣列；如果係就回傳 `true`，否則回傳 `false`。",
		"zho": "`isArray` 用于检查给定值是否为数组；如果是则返回 `true`，否则返回 `false`。",
		"zha": "`isArray` check gijz a given value miz array boux, laeuz raeuz `true` yawj it miz, aeu mbouj miz, raeuz `false`.",
		"zsm": "`isArray` menyemak sama ada nilai yang diberikan ialah tatasusunan dan mengembalikan `true` jika ya, jika tidak `false`."
	},
	"subject": []
}