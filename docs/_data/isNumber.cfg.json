{
	"sourceLanguage": "eng",
	"referenceFileHash": "0fb5b57792c34ce5e8997383a2847f39ff0612e0663041514a01b49040f0f8a3",
	"aiPromptHash": "9f5ec9e4ef2b88305876eee7c9d77766b651784d11b77ecf8ef81bd005620fa5",
	"translatedTo": {
		"eng": "`isNumber` checks whether a value is a finite, non-`NaN` number.",
		"deu": "`isNumber` prüft, ob ein Wert eine endliche, nicht-`NaN` Zahl ist.",
		"pol": "`isNumber` sprawdza, czy wartość jest skończoną liczbą, która nie jest `NaN`.",
		"ara": "تتحقق `isNumber` مما إذا كانت القيمة رقمًا منتهيًا وغير `NaN`.",
		"afr": "`isNumber` kontroleer of ’n waarde ’n eindige, nie-`NaN` getal is.",
		"arz": "`isNumber` بيشيّك إذا كانت القيمة رقم محدود (finite) ومش `NaN`.",
		"bel": "`isNumber` правярае, ці з'яўляецца значэнне канечным лікам, не роўным `NaN`.",
		"bho": "`isNumber` जाँचेला कि कउनो मान सीमित (finite), non-`NaN` नंबर बा कि ना।",
		"ben": "`isNumber` পরীক্ষা করে কোনো মান সসীম, `NaN`-নয় এমন সংখ্যা কি না।",
		"bos": "`isNumber` provjerava da li je vrijednost konačan broj koji nije `NaN`.",
		"cat": "`isNumber` comprova si un valor és un nombre finit i no `NaN`.",
		"bul": "`isNumber` проверява дали дадена стойност е крайно число, различно от `NaN`.",
		"cjy": "`isNumber` 用来检查一个值是不是有限、非 `NaN` 嘞数字。",
		"ces": "`isNumber` kontroluje, zda je hodnota konečné číslo, které není `NaN`.",
		"cmn": "`isNumber` 用于检查某个值是否为有限且非 `NaN` 的数字。",
		"ckb": "`isNumber` پشکنین دەکات کە ئایا بەهایەک ژمارەیەکی سنووردارە و `NaN` نییە.",
		"cym": "Mae `isNumber` yn gwirio a yw gwerth yn rhif terfynol, nad yw'n `NaN`.",
		"dan": "`isNumber` kontrollerer, om en værdi er et endeligt tal, der ikke er `NaN`.",
		"ell": "Το `isNumber` ελέγχει αν μια τιμή είναι πεπερασμένος αριθμός που δεν είναι `NaN`.",
		"est": "`isNumber` kontrollib, kas väärtus on lõplik, mitte-`NaN` number.",
		"fas": "`isNumber` بررسی می‌کند که آیا یک مقدار، عددی متناهی و غیرِ `NaN` است یا نه.",
		"fra": "`isNumber` vérifie si une valeur est un nombre fini, non `NaN`.",
		"fin": "`isNumber` tarkistaa, onko arvo äärellinen, ei-`NaN`-numero.",
		"gle": "Seiceálann `isNumber` an bhfuil luach ina uimhir chríochnaithe, neamh-`NaN`.",
		"gsw": "`isNumber` prüeft, öb en Wert e endli, nid-`NaN` Zahle isch.",
		"hak": "`isNumber` 檢查一個值係毋係有限、非 `NaN` 个數字。",
		"hin": "`isNumber` जाँचता है कि कोई मान सीमित (finite), non-`NaN` संख्या है या नहीं।",
		"hrv": "`isNumber` provjerava je li vrijednost konačan broj koji nije `NaN`.",
		"heb": "`isNumber` בודק האם ערך הוא מספר סופי שאינו `NaN`.",
		"hun": "Az `isNumber` ellenőrzi, hogy egy érték véges, nem `NaN` szám-e.",
		"ind": "`isNumber` memeriksa apakah sebuah nilai adalah angka yang berhingga dan bukan `NaN`.",
		"hsn": "`isNumber` चेक करदा ऐ के कोई वैल्यू फाइनाइट, गैर-`NaN` नंबर ऐ के नहीं।",
		"isl": "`isNumber` athugar hvort gildi sé endanleg tala sem er ekki `NaN`.",
		"ita": "`isNumber` verifica se un valore è un numero finito e non `NaN`.",
		"jav": "`isNumber` mriksa apa sawijining nilai iku angka sing finite lan dudu `NaN`.",
		"jpn": "`isNumber`は、値が有限で`NaN`ではない数値かどうかをチェックします。",
		"kat": "`isNumber` ამოწმებს, არის თუ არა მნიშვნელობა საბოლოო, არა-`NaN` რიცხვი.",
		"kor": "`isNumber`는 값이 유한한(non-`NaN`) 숫자인지 확인합니다.",
		"lav": "`isNumber` pārbauda, vai vērtība ir galīgs, ne-`NaN` skaitlis.",
		"kur": "`isNumber` kontrol dike ka nirx hejmarekî finite, ne-`NaN` e an na.",
		"lit": "`isNumber` patikrina, ar reikšmė yra baigtinis, ne `NaN` skaičius.",
		"mar": "`isNumber` एखादे मूल्य finite, non-`NaN` संख्या आहे का ते तपासते.",
		"mya": "`isNumber` သည် တန်ဖိုးတစ်ခုသည် အကန့်အသတ်ရှိပြီး `NaN` မဟုတ်သော နံပါတ်ဖြစ်/မဖြစ်ကို စစ်ဆေးသည်။",
		"mkd": "`isNumber` проверува дали вредноста е конечен број што не е `NaN`.",
		"nan": "`isNumber` 檢查一個值敢是有限、非 `NaN` 个數字。",
		"nld": "`isNumber` controleert of een waarde een eindig, niet-`NaN` getal is.",
		"nno": "`isNumber` sjekkar om ein verdi er eit endeleg tal som ikkje er `NaN`.",
		"nep": "`isNumber` ले कुनै मान सीमित (finite), `NaN` नभएको नम्बर हो कि होइन भनेर जाँच गर्छ।",
		"nob": "`isNumber` sjekker om en verdi er et endelig tall som ikke er `NaN`.",
		"nor": "`isNumber` sjekker om en verdi er et endelig, ikke-`NaN` tall.",
		"orm": "`isNumber` gatiin tokko lakkoofsa xumura-qabu, `NaN` hin taane ta'uu isaa ni sakatta'a.",
		"pcm": "`isNumber` dey check whether value na finite number wey no be `NaN`.",
		"pan": "`isNumber` ਚੈਕ ਕਰਦਾ ਹੈ ਕਿ ਕੋਈ ਮੁੱਲ finite, non-`NaN` ਨੰਬਰ ਹੈ ਜਾਂ ਨਹੀਂ।",
		"pes": "`isNumber` بررسی می‌کند آیا یک مقدار، عددی متناهی و غیرِ `NaN` است یا نه.",
		"por": "`isNumber` verifica se um valor é um número finito e não `NaN`.",
		"ron": "`isNumber` verifică dacă o valoare este un număr finit, non-`NaN`.",
		"pus": "`isNumber` ګوري چې يو ارزښت محدود (finite) او غير-`NaN` عدد دی که نه.",
		"rus": "`isNumber` проверяет, является ли значение конечным числом, не равным `NaN`.",
		"slk": "`isNumber` kontroluje, či je hodnota konečné číslo, ktoré nie je `NaN`.",
		"slv": "`isNumber` preveri, ali je vrednost končno število, ki ni `NaN`.",
		"spa": "`isNumber` comprueba si un valor es un número finito y no `NaN`.",
		"sqi": "`isNumber` kontrollon nëse një vlerë është një numër i fundëm, jo-`NaN`.",
		"swe": "`isNumber` kontrollerar om ett värde är ett ändligt tal som inte är `NaN`.",
		"srp": "`isNumber` проверава да ли је вредност коначан број који није `NaN`.",
		"swa": "`isNumber` hukagua kama thamani ni nambari yenye kikomo, isiyo-`NaN`.",
		"tgl": "Sinusuri ng `isNumber` kung ang isang value ay isang finite, hindi-`NaN` na number.",
		"tel": "`isNumber` ఒక విలువ పరిమితమైన (finite), `NaN` కాని సంఖ్యా కాదా అని తనిఖీ చేస్తుంది.",
		"tam": "`isNumber` ஒரு மதிப்பு முடிவுற்ற, `NaN` அல்லாத எண் தானா என்பதைச் சரிபார்க்கிறது.",
		"tur": "`isNumber`, bir değerin sonlu ve `NaN` olmayan bir sayı olup olmadığını kontrol eder.",
		"tha": "`isNumber` ตรวจสอบว่าค่าเป็นตัวเลขที่มีขอบเขตจำกัดและไม่ใช่ `NaN` หรือไม่.",
		"uig": "`isNumber` بىر قىممەتنىڭ چەكلىك، `NaN` بولمىغان سان ئىكەنلىكىنى تەكشۈرىدۇ.",
		"ukr": "`isNumber` перевіряє, чи є значення скінченним числом, відмінним від `NaN`.",
		"urd": "`isNumber` چیک کرتا ہے کہ آیا کوئی ویلیو محدود (finite)، غیر-`NaN` نمبر ہے۔",
		"uzb": "`isNumber` qiymat cheklangan (finite), `NaN` bo‘lmagan son ekanligini tekshiradi.",
		"vie": "`isNumber` kiểm tra liệu một giá trị có phải là một số hữu hạn và không phải `NaN` hay không.",
		"wuu": "`isNumber` 检查一个值是不是有限、非 `NaN` 个数字。",
		"yue": "`isNumber` 會檢查一個值係咪有限、非 `NaN` 嘅數字。",
		"zho": "`isNumber` 用于检查一个值是否为有限且非 `NaN` 的数字。",
		"zsm": "`isNumber` menyemak sama ada sesuatu nilai ialah nombor terhingga yang bukan `NaN`.",
		"zha": "`isNumber` cek ndaej vaenz mbouj ndaej, gijyauz sojmiz laengz sojmiz finite, mbouj rox `NaN`."
	},
	"subject": []
}