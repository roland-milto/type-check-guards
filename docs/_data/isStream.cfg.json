{
	"sourceLanguage": "eng",
	"referenceFileHash": "f4da4b8cdbb43a3f459772c40494725f0a6e1688ff2cd8fb4c8d6b8bba4a8f7c",
	"aiPromptHash": "9f5ec9e4ef2b88305876eee7c9d77766b651784d11b77ecf8ef81bd005620fa5",
	"translatedTo": {
		"eng": "`isStream` checks whether a given value is a stream object (Node.js stream-like, `ReadableStream`, or `WritableStream`).",
		"afr": "`isStream` kontroleer of ’n gegewe waarde ’n stroomvoorwerp is (Node.js-stroomagtig, `ReadableStream`, of `WritableStream`).",
		"ara": "تتحقق `isStream` مما إذا كانت قيمة معيّنة كائن تدفّق (شبيه بتدفّقات Node.js، أو `ReadableStream`، أو `WritableStream`).",
		"arz": "`isStream` بيشيّك إذا كانت قيمة معيّنة عبارة عن كائن stream (شبيه بـ Node.js stream، أو `ReadableStream`، أو `WritableStream`).",
		"ben": "`isStream` পরীক্ষা করে প্রদত্ত কোনো মান স্ট্রিম অবজেক্ট কি না (Node.js স্ট্রিম-সদৃশ, `ReadableStream`, বা `WritableStream`)।",
		"bel": "`isStream` правярае, ці з’яўляецца зададзенае значэнне stream-аб’ектам (stream-падобным у Node.js, `ReadableStream` або `WritableStream`).",
		"bho": "`isStream` जाँचेला कि दिहल गइल मान stream ऑब्जेक्ट बा कि ना (Node.js stream-जइसन, `ReadableStream`, भा `WritableStream`)।",
		"bos": "`isStream` provjerava da li je data vrijednost stream objekat (Node.js nalik streamu, `ReadableStream` ili `WritableStream`).",
		"cat": "`isStream` comprova si un valor determinat és un objecte stream (semblant a stream de Node.js, `ReadableStream` o `WritableStream`).",
		"bul": "`isStream` проверява дали дадена стойност е обект поток (подобен на Node.js поток, `ReadableStream` или `WritableStream`).",
		"ces": "`isStream` kontroluje, zda je daná hodnota stream objekt (podobný streamům v Node.js, `ReadableStream` nebo `WritableStream`).",
		"cmn": "`isStream` 用于检查给定值是否为流对象（Node.js 类流、`ReadableStream` 或 `WritableStream`）。",
		"cjy": "`isStream` 會檢查指定个值係毋係 stream 物件（Node.js 類 stream、`ReadableStream`，抑係 `WritableStream`）。",
		"ckb": "`isStream` پشکنین دەکات کە ئایا بەهایەکی دیاریکراو شتێکی ستریمە (هاوشێوەی ستریمی Node.js، `ReadableStream`، یان `WritableStream`).",
		"dan": "`isStream` kontrollerer, om en given værdi er et stream-objekt (Node.js stream-lignende, `ReadableStream` eller `WritableStream`).",
		"cym": "Mae `isStream` yn gwirio a yw gwerth penodol yn wrthrych ffrwd (tebyg i ffrwd Node.js, `ReadableStream`, neu `WritableStream`).",
		"deu": "`isStream` prüft, ob ein gegebener Wert ein Stream-Objekt ist (Node.js-Stream-ähnlich, `ReadableStream` oder `WritableStream`).",
		"ell": "Το `isStream` ελέγχει αν μια δεδομένη τιμή είναι αντικείμενο stream (τύπου Node.js stream-like, `ReadableStream` ή `WritableStream`).",
		"est": "`isStream` kontrollib, kas antud väärtus on voogobjekt (Node.js-i voogudega sarnane, `ReadableStream` või `WritableStream`).",
		"fin": "`isStream` tarkistaa, onko annettu arvo stream-objekti (Node.js:n stream-tyyppinen, `ReadableStream` tai `WritableStream`).",
		"fas": "`isStream` بررسی می‌کند که آیا یک مقدارِ داده‌شده یک شیء استریم است (شبیه به استریم در Node.js، `ReadableStream` یا `WritableStream`).",
		"fra": "`isStream` vérifie si une valeur donnée est un objet flux (de type flux Node.js, `ReadableStream` ou `WritableStream`).",
		"gle": "Seiceálann `isStream` an bhfuil luach ar leith ina réad srutha (cosúil le sruth Node.js, `ReadableStream`, nó `WritableStream`).",
		"gsw": "`isStream` prüeft, öb en gegebene Wert es Stream-Objäkt isch (Node.js-Stream-ähnlig, `ReadableStream` oder `WritableStream`).",
		"hak": "`isStream` 會檢查指定个值係毋係串流物件（Node.js 類串流、`ReadableStream`，抑係 `WritableStream`）。",
		"hrv": "`isStream` provjerava je li zadana vrijednost stream objekt (Node.js stream-like, `ReadableStream` ili `WritableStream`).",
		"heb": "`isStream` בודק האם ערך נתון הוא אובייקט stream (דמוי-Stream ב-Node.js, `ReadableStream`, או `WritableStream`).",
		"hsn": "`isStream` चेक करथे के दिहे गे value एक stream ऑब्जेक्ट आय का (Node.js stream-जइसन, `ReadableStream`, या `WritableStream`)।",
		"ind": "`isStream` memeriksa apakah suatu nilai tertentu adalah objek stream (mirip stream Node.js, `ReadableStream`, atau `WritableStream`).",
		"isl": "`isStream` athugar hvort tiltekið gildi sé straumhlutur (Node.js straumlíkur, `ReadableStream` eða `WritableStream`).",
		"ita": "`isStream` verifica se un determinato valore è un oggetto stream (simile a stream di Node.js, `ReadableStream` o `WritableStream`).",
		"jav": "`isStream` mriksa apa sawijining nilai iku obyek stream (kaya stream ing Node.js, `ReadableStream`, utawa `WritableStream`).",
		"jpn": "`isStream` は、指定された値がストリームオブジェクト（Node.js のストリーム風、`ReadableStream`、または `WritableStream`）かどうかを確認します。",
		"kat": "`isStream` ამოწმებს, არის თუ არა მოცემული მნიშვნელობა სტრიმის ობიექტი (Node.js-ის სტრიმის მსგავსი, `ReadableStream` ან `WritableStream`).",
		"kor": "`isStream`은 주어진 값이 스트림 객체(Node.js 스트림 유사 객체, `ReadableStream` 또는 `WritableStream`)인지 확인합니다.",
		"hin": "`isStream` जाँचता है कि दिया गया मान एक stream object है या नहीं (Node.js stream-जैसा, `ReadableStream`, या `WritableStream`)।",
		"hun": "Az `isStream` ellenőrzi, hogy egy adott érték stream objektum-e (Node.js stream-szerű, `ReadableStream` vagy `WritableStream`).",
		"lav": "`isStream` pārbauda, vai dotā vērtība ir straumes objekts (Node.js straumei līdzīgs, `ReadableStream` vai `WritableStream`).",
		"lit": "`isStream` patikrina, ar pateikta reikšmė yra srauto objektas (į Node.js srautą panašus, `ReadableStream` arba `WritableStream`).",
		"kur": "`isStream` kontrol dike ka nirxek dayîn stream e (stream-şêwaz ên Node.js, `ReadableStream`, an `WritableStream`).",
		"mar": "`isStream` दिलेले मूल्य stream ऑब्जेक्ट आहे का (Node.js stream-सदृश, `ReadableStream`, किंवा `WritableStream`) हे तपासते.",
		"mkd": "`isStream` проверува дали дадена вредност е стрим-објект (Node.js стрим-сличен, `ReadableStream` или `WritableStream`).",
		"mya": "`isStream` သည် ပေးထားသော တန်ဖိုးသည် stream အရာဝတ္ထု (Node.js stream-like, `ReadableStream`, သို့မဟုတ် `WritableStream`) ဟုတ်မဟုတ်ကို စစ်ဆေးသည်။",
		"nld": "`isStream` controleert of een gegeven waarde een streamobject is (Node.js stream-achtig, `ReadableStream` of `WritableStream`).",
		"nep": "`isStream` ले दिइएको मान stream वस्तु (Node.js stream-जस्तो, `ReadableStream`, वा `WritableStream`) हो कि होइन भनेर जाँच गर्छ।",
		"nan": "`isStream` kiám-cha chi̍t ê gí-bēng ê chhi̍t sī-m̄-sī stream object (Node.js stream-lêng-kiānn, `ReadableStream`, á-sī `WritableStream`).",
		"nob": "`isStream` sjekker om en gitt verdi er et strømobjekt (Node.js-strømlignende, `ReadableStream` eller `WritableStream`).",
		"nno": "`isStream` sjekkar om ein gitt verdi er eit straumobjekt (Node.js-straumliknande, `ReadableStream` eller `WritableStream`).",
		"nor": "`isStream` sjekker om en gitt verdi er et strømobjekt (Node.js-strømlignende, `ReadableStream` eller `WritableStream`).",
		"pcm": "`isStream` dey check whether one value na stream object (Node.js stream-like, `ReadableStream`, or `WritableStream`).",
		"orm": "`isStream` gatiin kenname stream object ta'uu isaa (akka stream Node.js fakkaatu, `ReadableStream`, yookaan `WritableStream`) ni mirkaneessa.",
		"pan": "`isStream` ਜਾਂਚਦਾ ਹੈ ਕਿ ਦਿੱਤੀ ਗਈ ਵੈਲਿਊ ਇੱਕ ਸਟ੍ਰੀਮ ਆਬਜੈਕਟ ਹੈ ਜਾਂ ਨਹੀਂ (Node.js ਸਟ੍ਰੀਮ-ਵਰਗੀ, `ReadableStream`, ਜਾਂ `WritableStream`)।",
		"pes": "`isStream` بررسی می‌کند که آیا یک مقدار داده‌شده یک شیء استریم است (شبیه استریم در Node.js، `ReadableStream` یا `WritableStream`).",
		"por": "`isStream` verifica se um determinado valor é um objeto de stream (semelhante a stream do Node.js, `ReadableStream` ou `WritableStream`).",
		"pol": "`isStream` sprawdza, czy podana wartość jest obiektem strumienia (podobnym do strumieni Node.js, `ReadableStream` lub `WritableStream`).",
		"pus": "`isStream` ګوري چې ورکړل شوی ارزښت يو stream څيز دی که نه (Node.js stream-ته ورته، `ReadableStream`، يا `WritableStream`).",
		"rus": "`isStream` проверяет, является ли заданное значение объектом потока (stream-подобным в Node.js, `ReadableStream` или `WritableStream`).",
		"ron": "`isStream` verifică dacă o valoare dată este un obiect stream (de tip stream-like Node.js, `ReadableStream` sau `WritableStream`).",
		"slk": "`isStream` kontroluje, či je daná hodnota objekt streamu (podobný streamu v Node.js, `ReadableStream` alebo `WritableStream`).",
		"spa": "`isStream` comprueba si un valor dado es un objeto stream (similar a streams de Node.js, `ReadableStream` o `WritableStream`).",
		"slv": "`isStream` preveri, ali je dana vrednost objekt stream (Node.js streamu podoben, `ReadableStream` ali `WritableStream`).",
		"sqi": "`isStream` kontrollon nëse një vlerë e dhënë është një objekt stream (i ngjashëm me stream në Node.js, `ReadableStream`, ose `WritableStream`).",
		"swa": "`isStream` hukagua kama thamani iliyotolewa ni kitu cha stream (kinachofanana na stream cha Node.js, `ReadableStream`, au `WritableStream`).",
		"srp": "`isStream` проверава да ли је дата вредност стрим објекат (Node.js налик стриму, `ReadableStream` или `WritableStream`).",
		"swe": "`isStream` kontrollerar om ett givet värde är ett strömobjekt (Node.js-strömliknande, `ReadableStream` eller `WritableStream`).",
		"tel": "`isStream` ఇచ్చిన విలువ స్ట్రీమ్ ఆబ్జెక్ట్‌నా (Node.js స్ట్రీమ్‌లాంటి, `ReadableStream`, లేదా `WritableStream`) అని తనిఖీ చేస్తుంది.",
		"tam": "`isStream` கொடுக்கப்பட்ட மதிப்பு ஒரு stream பொருளா (Node.js stream போன்றது, `ReadableStream`, அல்லது `WritableStream`) என்பதைச் சரிபார்க்கிறது.",
		"tgl": "Sinusuri ng `isStream` kung ang ibinigay na value ay isang stream object (Node.js stream-like, `ReadableStream`, o `WritableStream`).",
		"tha": "`isStream` ตรวจสอบว่าค่าที่กำหนดเป็นอ็อบเจ็กต์สตรีมหรือไม่ (สตรีมแบบ Node.js, `ReadableStream` หรือ `WritableStream`).",
		"tur": "`isStream`, verilen bir değerin bir akış nesnesi (Node.js akış-benzeri, `ReadableStream` veya `WritableStream`) olup olmadığını kontrol eder.",
		"uig": "`isStream` بېرىلگەن قىممەتنىڭ ئېقىم ئوبيېكتى (Node.js ئېقىمغا ئوخشاش، `ReadableStream` ياكى `WritableStream`) ئىكەنلىكىنى تەكشۈرىدۇ.",
		"ukr": "`isStream` перевіряє, чи є задане значення об’єктом потоку (схожим на потік Node.js, `ReadableStream` або `WritableStream`).",
		"urd": "`isStream` یہ چیک کرتا ہے کہ آیا دی گئی قدر ایک stream آبجیکٹ ہے (Node.js stream-like، `ReadableStream`، یا `WritableStream`)۔",
		"vie": "`isStream` kiểm tra xem một giá trị cho trước có phải là đối tượng stream hay không (giống stream của Node.js, `ReadableStream`, hoặc `WritableStream`).",
		"uzb": "`isStream` berilgan qiymat stream obyekti (Node.js streamga o‘xshash, `ReadableStream` yoki `WritableStream`) ekanligini tekshiradi.",
		"wuu": "`isStream` 用来检查给定个值是不是 stream 对象（Node.js 类 stream、`ReadableStream` 或 `WritableStream`）。",
		"zho": "`isStream` 用于检查给定值是否为流对象（Node.js 类流、`ReadableStream` 或 `WritableStream`）。",
		"yue": "`isStream` 會檢查指定嘅值係咪 stream 物件（Node.js 類 stream、`ReadableStream` 或 `WritableStream`）。",
		"zha": "`isStream` roxq raeuz gij aen daengz rox, yaek aen stream object (Node.js stream-like, `ReadableStream`, ro `WritableStream`).",
		"zsm": "`isStream` menyemak sama ada sesuatu nilai yang diberikan ialah objek aliran (seperti aliran Node.js, `ReadableStream`, atau `WritableStream`)."
	},
	"subject": []
}