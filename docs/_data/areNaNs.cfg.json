{
	"sourceLanguage": "eng",
	"referenceFileHash": "c693fcba86c7ecb3bb98854cf2394d29bbecb8185775315a755542a9671c62b7",
	"aiPromptHash": "9f5ec9e4ef2b88305876eee7c9d77766b651784d11b77ecf8ef81bd005620fa5",
	"translatedTo": {
		"eng": "`areNaNs` checks whether all elements in an array are `NaN` and returns `true` only if every element is `NaN`.",
		"arz": "`areNaNs` بتشيّك إذا كان كل العناصر في المصفوفة `NaN` وبتطلع `true` بس لو كل عنصر `NaN`.",
		"afr": "`areNaNs` kontroleer of alle elemente in 'n skikking `NaN` is en gee `true` slegs terug as elke element `NaN` is.",
		"ara": "تتحقق `areNaNs` مما إذا كانت جميع العناصر في مصفوفة هي `NaN` وتعيد `true` فقط إذا كان كل عنصر `NaN`.",
		"bel": "`areNaNs` правярае, ці ўсе элементы ў масіве з'яўляюцца `NaN`, і вяртае `true` толькі калі кожны элемент — `NaN`.",
		"bho": "`areNaNs` जाँचेला कि ऐरे में सब तत्व `NaN` बा कि ना आ `true` खाली तबे लौटावेला जब हर तत्व `NaN` होखे।",
		"ben": "`areNaNs` যাচাই করে একটি অ্যারের সব উপাদান `NaN` কি না এবং শুধুমাত্র তখনই `true` ফেরত দেয় যখন প্রতিটি উপাদান `NaN`।",
		"bos": "`areNaNs` provjerava da li su svi elementi u nizu `NaN` i vraća `true` samo ako je svaki element `NaN`.",
		"bul": "`areNaNs` проверява дали всички елементи в масив са `NaN` и връща `true` само ако всеки елемент е `NaN`.",
		"cat": "`areNaNs` comprova si tots els elements d'un array són `NaN` i retorna `true` només si cada element és `NaN`.",
		"ces": "`areNaNs` kontroluje, zda jsou všechny prvky v poli `NaN`, a vrací `true` pouze tehdy, když je každý prvek `NaN`.",
		"cjy": "`areNaNs` 检查一个数组里向是不是所有元素都系 `NaN`，只有当每个元素都系 `NaN` 个辰光才返回 `true`。",
		"cmn": "`areNaNs` 用于检查数组中的所有元素是否都是 `NaN`，并且仅当每个元素都是 `NaN` 时才返回 `true`。",
		"ckb": "`areNaNs` دەکاتەوە کە ئایا هەموو ئەندامەکانی ئارایەکە `NaN`ن و تەنها ئەگەر هەموو ئەندامەکان `NaN` بن `true` دەگەڕێنێتەوە.",
		"dan": "`areNaNs` kontrollerer, om alle elementer i et array er `NaN`, og returnerer `true` kun hvis hvert element er `NaN`.",
		"cym": "Mae `areNaNs` yn gwirio a yw pob elfen mewn arae yn `NaN` ac yn dychwelyd `true` dim ond os yw pob elfen yn `NaN`.",
		"deu": "`areNaNs` prüft, ob alle Elemente in einem Array `NaN` sind, und gibt nur dann `true` zurück, wenn jedes Element `NaN` ist.",
		"fas": "`areNaNs` بررسی می‌کند که آیا همهٔ عناصر یک آرایه `NaN` هستند و فقط در صورتی `true` برمی‌گرداند که هر عنصر `NaN` باشد.",
		"est": "`areNaNs` kontrollib, kas kõik massiivi elemendid on `NaN`, ja tagastab `true` ainult siis, kui iga element on `NaN`.",
		"ell": "Το `areNaNs` ελέγχει αν όλα τα στοιχεία σε έναν πίνακα είναι `NaN` και επιστρέφει `true` μόνο αν κάθε στοιχείο είναι `NaN`.",
		"fra": "`areNaNs` vérifie si tous les éléments d’un tableau sont `NaN` et renvoie `true` uniquement si chaque élément est `NaN`.",
		"fin": "`areNaNs` tarkistaa, ovatko kaikki taulukon alkiot `NaN`, ja palauttaa `true` vain, jos jokainen alkio on `NaN`.",
		"gle": "Seiceálann `areNaNs` an bhfuil gach eilimint in eagar `NaN` agus filleann sé `true` amháin má tá gach eilimint `NaN`.",
		"gsw": "`areNaNs` prüeft, öb alli Element i enem Array `NaN` sind, und git nume denn `true` zrugg, wänn jedes Element `NaN` isch.",
		"hak": "`areNaNs` 會檢查陣列內底係毋係全部元素都係 `NaN`，淨係當每一個元素都係 `NaN` 正會回傳 `true`。",
		"heb": "`areNaNs` בודקת האם כל הרכיבים במערך הם `NaN` ומחזירה `true` רק אם כל רכיב הוא `NaN`.",
		"hin": "`areNaNs` जाँचता है कि क्या किसी ऐरे के सभी तत्व `NaN` हैं और केवल तभी `true` लौटाता है जब हर तत्व `NaN` हो।",
		"hrv": "`areNaNs` provjerava jesu li svi elementi u nizu `NaN` i vraća `true` samo ako je svaki element `NaN`.",
		"hsn": "`areNaNs` चैक करदा ऐ के ऐरे दे सारे एलिमैंट `NaN` नें या नहीं, अते `true` सिरिफ ओह्दो वक़्त वापस करदा ऐ जदों हर इक एलिमैंट `NaN` होवे।",
		"ind": "`areNaNs` memeriksa apakah semua elemen dalam sebuah array adalah `NaN` dan mengembalikan `true` hanya jika setiap elemen adalah `NaN`.",
		"hun": "Az `areNaNs` ellenőrzi, hogy egy tömb minden eleme `NaN`-e, és csak akkor ad vissza `true` értéket, ha minden elem `NaN`.",
		"jav": "`areNaNs` mriksa apa kabeh unsur ing sawijining array iku `NaN` lan mbalekake `true` mung yen saben unsur iku `NaN`.",
		"ita": "`areNaNs` verifica se tutti gli elementi in un array sono `NaN` e restituisce `true` solo se ogni elemento è `NaN`.",
		"isl": "`areNaNs` athugar hvort öll stök í fylki séu `NaN` og skilar `true` aðeins ef hvert stak er `NaN`.",
		"jpn": "`areNaNs` は配列内のすべての要素が `NaN` かどうかを確認し、すべての要素が `NaN` の場合にのみ `true` を返します。",
		"kor": "`areNaNs`는 배열의 모든 요소가 `NaN`인지 확인하며, 모든 요소가 `NaN`일 때에만 `true`를 반환합니다.",
		"kat": "`areNaNs` ამოწმებს, არის თუ არა მასივში ყველა ელემენტი `NaN` და აბრუნებს `true`-ს მხოლოდ იმ შემთხვევაში, თუ ყოველი ელემენტი არის `NaN`.",
		"lav": "`areNaNs` pārbauda, vai visi masīva elementi ir `NaN`, un atgriež `true` tikai tad, ja katrs elements ir `NaN`.",
		"lit": "`areNaNs` patikrina, ar visi masyvo elementai yra `NaN`, ir grąžina `true` tik jei kiekvienas elementas yra `NaN`.",
		"kur": "`areNaNs` kontrol dike ka hemû hêmanên di arrayekê de `NaN` in û tenê heke her hêman `NaN` be `true` vedigerîne.",
		"mar": "`areNaNs` अ‍ॅरेमधील सर्व घटक `NaN` आहेत का हे तपासते आणि प्रत्येक घटक `NaN` असेल तेव्हाच `true` परत करते.",
		"mkd": "`areNaNs` проверува дали сите елементи во низа се `NaN` и враќа `true` само ако секој елемент е `NaN`.",
		"mya": "`areNaNs` သည် အာရေးတစ်ခုအတွင်းရှိ အရာဝတ္ထုအားလုံးက `NaN` ဖြစ်/မဖြစ်ကို စစ်ဆေးပြီး အရာဝတ္ထုတိုင်းက `NaN` ဖြစ်တဲ့အခါမှသာ `true` ကို ပြန်ပေးသည်။",
		"nan": "`areNaNs` kiám-cha array lāi ê só͘-ū iû-sò͘ sī-m̄-sī lóng sī `NaN`, chiah tī múi ê iû-sò͘ lóng sī `NaN` ê sî-chūn chiah thèng-sī `true`.",
		"nld": "`areNaNs` controleert of alle elementen in een array `NaN` zijn en retourneert `true` alleen als elk element `NaN` is.",
		"nep": "`areNaNs` ले एरेका सबै तत्वहरू `NaN` छन् कि छैनन् भनेर जाँच गर्छ र हरेक तत्व `NaN` भएमा मात्र `true` फर्काउँछ।",
		"nno": "`areNaNs` sjekkar om alle element i eit array er `NaN` og returnerer `true` berre dersom kvart element er `NaN`.",
		"nob": "`areNaNs` sjekker om alle elementer i en array er `NaN` og returnerer `true` bare hvis hvert element er `NaN`.",
		"nor": "`areNaNs` sjekker om alle elementer i en array er `NaN` og returnerer `true` bare hvis hvert element er `NaN`.",
		"orm": "`areNaNs` elementoonni array keessa jiran hundi `NaN` ta’uu isaanii sakatta’a; yoo elementiin hundi `NaN` ta’e qofa `true` deebisa.",
		"pcm": "`areNaNs` dey check whether all elements for an array be `NaN` and e dey return `true` only if every element be `NaN`.",
		"pan": "`areNaNs` ਚੈਕ ਕਰਦਾ ਹੈ ਕਿ ਕੀ ਐਰੇ ਦੇ ਸਾਰੇ ਐਲਿਮੈਂਟ `NaN` ਹਨ ਅਤੇ ਸਿਰਫ਼ ਓਦੋਂ ਹੀ `true` ਵਾਪਸ ਕਰਦਾ ਹੈ ਜਦੋਂ ਹਰ ਐਲਿਮੈਂਟ `NaN` ਹੋਵੇ।",
		"pes": "`areNaNs` بررسی می‌کند آیا همهٔ عناصرِ یک آرایه `NaN` هستند و فقط در صورتی `true` برمی‌گرداند که هر عنصر `NaN` باشد.",
		"pol": "`areNaNs` sprawdza, czy wszystkie elementy w tablicy są `NaN`, i zwraca `true` tylko wtedy, gdy każdy element jest `NaN`.",
		"por": "`areNaNs` verifica se todos os elementos em um array são `NaN` e retorna `true` somente se cada elemento for `NaN`.",
		"ron": "`areNaNs` verifică dacă toate elementele dintr-un tablou sunt `NaN` și întoarce `true` numai dacă fiecare element este `NaN`.",
		"rus": "`areNaNs` проверяет, являются ли все элементы массива `NaN`, и возвращает `true` только если каждый элемент — `NaN`.",
		"pus": "`areNaNs` जांचदा ऐ कि ऐरे दे सारे अनसर `NaN` नें अते `true` सिरिफ ओह्दो वक़्त वापस करदा ऐ जदों हर इक अनसर `NaN` होवे।",
		"slk": "`areNaNs` kontroluje, či sú všetky prvky v poli `NaN`, a vráti `true` iba vtedy, ak je každý prvok `NaN`.",
		"slv": "`areNaNs` preveri, ali so vsi elementi v tabeli `NaN`, in vrne `true` samo, če je vsak element `NaN`.",
		"spa": "`areNaNs` comprueba si todos los elementos de un arreglo son `NaN` y devuelve `true` solo si cada elemento es `NaN`.",
		"sqi": "`areNaNs` kontrollon nëse të gjithë elementët në një varg janë `NaN` dhe kthen `true` vetëm nëse çdo element është `NaN`.",
		"srp": "`areNaNs` проверава да ли су сви елементи у низу `NaN` и враћа `true` само ако је сваки елемент `NaN`.",
		"swa": "`areNaNs` hukagua kama vipengele vyote katika safu ni `NaN` na hurejesha `true` tu ikiwa kila kipengele ni `NaN`.",
		"swe": "`areNaNs` kontrollerar om alla element i en array är `NaN` och returnerar `true` endast om varje element är `NaN`.",
		"tam": "`areNaNs` என்பது ஒரு வரிசையில் உள்ள அனைத்து உருப்படிகளும் `NaN` ஆக உள்ளனவா என்பதைச் சரிபார்த்து, ஒவ்வொரு உருப்படியும் `NaN` ஆக இருந்தால் மட்டுமே `true` ஐத் திருப்பும்.",
		"tel": "`areNaNs` oka array lo unna anni elements `NaN` āni check chēsi, prathi element `NaN` ayinappude mātramē `true` return chestundi.",
		"tgl": "Sinusuri ng `areNaNs` kung lahat ng elemento sa isang array ay `NaN` at nagbabalik ng `true` lamang kung bawat elemento ay `NaN`.",
		"tha": "`areNaNs` ตรวจสอบว่าองค์ประกอบทั้งหมดในอาร์เรย์เป็น `NaN` หรือไม่ และจะคืนค่า `true` ก็ต่อเมื่อทุกองค์ประกอบเป็น `NaN` เท่านั้น.",
		"tur": "`areNaNs`, bir dizideki tüm öğelerin `NaN` olup olmadığını kontrol eder ve yalnızca her öğe `NaN` ise `true` döndürür.",
		"ukr": "`areNaNs` перевіряє, чи всі елементи в масиві є `NaN`, і повертає `true` лише якщо кожен елемент є `NaN`.",
		"urd": "`areNaNs` چیک کرتا ہے کہ آیا ایک array کے تمام عناصر `NaN` ہیں اور صرف اسی صورت میں `true` واپس کرتا ہے جب ہر عنصر `NaN` ہو۔",
		"uig": "`areNaNs` ئارايىدىكى بارلىق ئېلېمېنتلارنىڭ `NaN` ئىكەنلىكىنى تەكشۈرىدۇ ۋە پەقەت ھەر بىر ئېلېمېنت `NaN` بولغاندا ئاندىن `true` قايتۇرىدۇ.",
		"vie": "`areNaNs` kiểm tra liệu tất cả các phần tử trong một mảng có phải là `NaN` hay không và chỉ trả về `true` nếu mọi phần tử đều là `NaN`.",
		"uzb": "`areNaNs` massivdagi barcha elementlar `NaN` ekanini tekshiradi va faqat har bir element `NaN` bo‘lsa `true` qaytaradi.",
		"wuu": "`areNaNs` 用来检查数组里向是不是所有元素侪是 `NaN`，只有当每个元素侪是 `NaN` 个辰光才返回 `true`。",
		"zho": "`areNaNs` 用于检查数组中的所有元素是否都是 `NaN`，并且仅当每个元素都是 `NaN` 时才返回 `true`。",
		"yue": "`areNaNs` 會檢查陣列入面係咪所有元素都係 `NaN`，只有喺每個元素都係 `NaN` 嘅情況下先回傳 `true`。",
		"zha": "`areNaNs` raeujndei raemx gwnz gwnz ndei mbouj doengz `NaN` laux ndei array, laux deih rox `true` deih rox gwnz gwnz ndei mbouj doengz `NaN`.",
		"zsm": "`areNaNs` menyemak sama ada semua elemen dalam tatasusunan ialah `NaN` dan mengembalikan `true` hanya jika setiap elemen ialah `NaN`."
	},
	"subject": []
}