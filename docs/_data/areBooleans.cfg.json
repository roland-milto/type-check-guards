{
	"sourceLanguage": "eng",
	"referenceFileHash": "396590f9caaf119f7eb4a2bedcdbbef9d264b37594f8efc1ab526ae8bf129686",
	"aiPromptHash": "9f5ec9e4ef2b88305876eee7c9d77766b651784d11b77ecf8ef81bd005620fa5",
	"translatedTo": {
		"eng": "`areBooleans` checks whether a given non-empty array contains only boolean values, returning `true` if it does and `false` otherwise.",
		"ara": "يتحقق `areBooleans` مما إذا كانت مصفوفة غير فارغة معيّنة تحتوي فقط على قيم منطقية، ويعيد `true` إذا كان الأمر كذلك و`false` خلاف ذلك.",
		"arz": "`areBooleans` بتتأكد إن الـ array اللي مش فاضية فيها قيم boolean بس، وبتِرجّع `true` لو كده و`false` غير كده.",
		"afr": "`areBooleans` kontroleer of 'n gegewe nie-leë skikking slegs boolean-waardes bevat, en gee `true` terug as dit wel doen en `false` andersins.",
		"bel": "`areBooleans` правярае, ці змяшчае зададзены непусты масіў толькі булевыя значэнні, вяртаючы `true`, калі так, і `false` у адваротным выпадку.",
		"bho": "`areBooleans` जाँचेला कि दिहल गइल non-empty array में खाली boolean मान बा कि ना, आ अगर बा त `true` लौटावेला, नाहीं त `false`।",
		"ben": "`areBooleans` যাচাই করে একটি প্রদত্ত খালি নয় এমন অ্যারেতে শুধুমাত্র boolean মান আছে কি না; থাকলে `true` এবং না থাকলে `false` ফেরত দেয়।",
		"bul": "`areBooleans` проверява дали даден непразен масив съдържа само булеви стойности, като връща `true`, ако е така, и `false` в противен случай.",
		"bos": "`areBooleans` provjerava da li dati neprazan niz sadrži samo booleanske vrijednosti, vraćajući `true` ako je tako i `false` u suprotnom.",
		"cat": "`areBooleans` comprova si un array no buit determinat conté només valors booleans, retornant `true` si és així i `false` en cas contrari.",
		"ces": "`areBooleans` kontroluje, zda dané neprázdné pole obsahuje pouze boolean hodnoty; vrací `true`, pokud ano, jinak `false`.",
		"cjy": "`areBooleans` 用来检查给定嗰非空数组里向是不是全系布尔值；如果系就返回 `true`，否则返回 `false`。",
		"cmn": "`areBooleans` 用于检查给定的非空数组是否只包含布尔值；若是则返回 `true`，否则返回 `false`。",
		"ckb": "`areBooleans` دەپشکنێت کە ئایا ئارایەیەکی دیاریکراوی بەتاڵ نەبوو تەنها لە بەهاکانی boolean پێکهاتووە، ئەگەر وابێت `true` دەگەڕێنێتەوە و ئەگەر نا `false`.",
		"cym": "Mae `areBooleans` yn gwirio a yw arae benodol nad yw'n wag yn cynnwys gwerthoedd boolean yn unig, gan ddychwelyd `true` os yw'n gwneud hynny a `false` fel arall.",
		"dan": "`areBooleans` kontrollerer, om et givent ikke-tomt array kun indeholder booleske værdier, og returnerer `true`, hvis det gør, og ellers `false`.",
		"deu": "`areBooleans` prüft, ob ein gegebenes nicht leeres Array ausschließlich Boolean-Werte enthält, und gibt `true` zurück, wenn dies der Fall ist, andernfalls `false`.",
		"ell": "Το `areBooleans` ελέγχει αν ένας δεδομένος μη κενός πίνακας περιέχει μόνο τιμές boolean, επιστρέφοντας `true` αν ισχύει και `false` διαφορετικά.",
		"fas": "`areBooleans` بررسی می‌کند آیا یک آرایهٔ غیرخالیِ داده‌شده فقط شامل مقادیر بولی است یا نه؛ اگر باشد `true` و در غیر این صورت `false` برمی‌گرداند.",
		"est": "`areBooleans` kontrollib, kas antud mittetühi massiiv sisaldab ainult tõeväärtusi, tagastades `true`, kui sisaldab, ja `false` muul juhul.",
		"fin": "`areBooleans` tarkistaa, sisältääkö annettu ei-tyhjä taulukko vain boolean-arvoja, palauttaen `true`, jos näin on, ja `false` muussa tapauksessa.",
		"fra": "`areBooleans` vérifie si un tableau non vide donné ne contient que des valeurs booléennes, en renvoyant `true` si c’est le cas et `false` sinon.",
		"gle": "Seiceálann `areBooleans` an bhfuil booleáin amháin in eagar neamhfholamh ar leith, ag filleadh `true` má tá agus `false` murach sin.",
		"gsw": "`areBooleans` prüeft, öb es gägebs nöd-läers Array nume Boolean-Wärt enthaltet, und git `true` zrugg, wänn das so isch, und susch `false`.",
		"hak": "`areBooleans` 檢查一個指定个毋係空个陣列係毋係淨包含 boolean 值；係个話回傳 `true`，毋係个話回傳 `false`。",
		"heb": "`areBooleans` בודק האם מערך נתון שאינו ריק מכיל רק ערכים בוליאניים, ומחזיר `true` אם כן ו-`false` אחרת.",
		"hin": "`areBooleans` जाँचता है कि दिया गया non-empty array केवल boolean मानों को ही शामिल करता है या नहीं; ऐसा होने पर `true` और अन्यथा `false` लौटाता है।",
		"hrv": "`areBooleans` provjerava sadrži li zadani neprazan niz samo booleove vrijednosti, vraćajući `true` ako sadrži, a `false` u suprotnom.",
		"hsn": "`areBooleans` चेक करदा ऐ के दित्ता گیا गैर-खाली ऐरे सिरिफ बूलियन वैल्यूआں تے मुश्तमिल ऐ या नئیں، जे होवे तां `true` واپس करदा ऐ वरना `false`।",
		"ind": "`areBooleans` memeriksa apakah sebuah array tidak kosong yang diberikan hanya berisi nilai boolean, mengembalikan `true` jika ya dan `false` jika tidak.",
		"hun": "Az `areBooleans` ellenőrzi, hogy egy adott nem üres tömb kizárólag logikai (boolean) értékeket tartalmaz-e; ha igen, `true`-t ad vissza, egyébként `false`-t.",
		"ita": "`areBooleans` verifica se un dato array non vuoto contiene solo valori booleani, restituendo `true` in tal caso e `false` altrimenti.",
		"isl": "`areBooleans` athugar hvort tiltekið ótómt fylki innihaldi eingöngu boolean gildi og skilar `true` ef svo er, annars `false`.",
		"jav": "`areBooleans` mriksa apa sawijining array sing ora kosong mung ngemot nilai boolean, mbalekake `true` yen iya lan `false` yen ora.",
		"kor": "`areBooleans`는 주어진 비어 있지 않은 배열이 불리언 값만 포함하는지 확인하며, 그렇다면 `true`를 반환하고 그렇지 않으면 `false`를 반환합니다.",
		"jpn": "`areBooleans` は、与えられた空でない配列がブール値のみを含むかどうかを確認し、含む場合は `true`、そうでない場合は `false` を返します。",
		"kat": "`areBooleans` ამოწმებს, შეიცავს თუ არა მოცემული არაცარიელი მასივი მხოლოდ ბულურ მნიშვნელობებს; თუ შეიცავს, აბრუნებს `true`-ს, წინააღმდეგ შემთხვევაში — `false`-ს.",
		"lit": "`areBooleans` patikrina, ar pateiktame netuščame masyve yra tik loginės reikšmės, grąžindamas `true`, jei taip, ir `false` kitu atveju.",
		"lav": "`areBooleans` pārbauda, vai dotais netukšs masīvs satur tikai būla vērtības, atgriežot `true`, ja tā ir, un `false` pretējā gadījumā.",
		"kur": "`areBooleans` kontrol dike ka arrayek nevala ya dayîn tenê nirxên booleyan dihewîne an na; heke dihewîne `true` vedigerîne û wekî din `false`.",
		"mkd": "`areBooleans` проверува дали дадена непразна низа содржи само булови вредности, враќајќи `true` ако е така и `false` во спротивно.",
		"mar": "`areBooleans` दिलेल्या नॉन-एम्प्टी array मध्ये फक्त boolean मूल्ये आहेत का ते तपासते; असल्यास `true` परत करते, अन्यथा `false`.",
		"nep": "`areBooleans` ले दिइएको गैर-खाली array मा केवल boolean मानहरू मात्र छन् कि छैनन् भनेर जाँच गर्छ; भएमा `true` र नभएमा `false` फर्काउँछ।",
		"mya": "`areBooleans` သည် ပေးထားသော အလွတ်မဟုတ်သော array တစ်ခုတွင် boolean တန်ဖိုးများသာ ပါဝင်နေသလားကို စစ်ဆေးပြီး ပါဝင်ပါက `true` ကို ပြန်ပေးကာ မပါဝင်ပါက `false` ကို ပြန်ပေးသည်။",
		"nan": "`areBooleans` kiám-cha chi̍t ê gí-bêng ê bô-khong array sī-m̄-sī kan-na ū boolean chiàu, nā sī tio̍h tńg `true`，bô sī tńg `false`.",
		"nld": "`areBooleans` controleert of een gegeven niet-lege array uitsluitend boolean-waarden bevat, en retourneert `true` als dat zo is en anders `false`.",
		"nno": "`areBooleans` sjekkar om ein gitt ikkje-tom array berre inneheld boolske verdiar, og returnerer `true` dersom han gjer det og `false` elles.",
		"nob": "`areBooleans` sjekker om en gitt ikke-tom matrise kun inneholder boolske verdier, og returnerer `true` hvis den gjør det og `false` ellers.",
		"nor": "`areBooleans` sjekker om en gitt ikke-tom matrise kun inneholder boolske verdier, og returnerer `true` hvis den gjør det og `false` ellers.",
		"pcm": "`areBooleans` dey check whether one given non-empty array get only boolean values, e go return `true` if na so, and `false` if no be so.",
		"orm": "`areBooleans` tarree duwwaa hin taane kenname keessaa gatiiwwan boolean qofa akka jiran ni mirkaneessa; yoo akkas taʼe `true` deebisa, yoo hin taʼin `false` deebisa.",
		"pan": "`areBooleans` ਜਾਂਚਦਾ ਹੈ ਕਿ ਦਿੱਤਾ ਗਿਆ non-empty ਐਰੇ ਸਿਰਫ਼ boolean ਮੁੱਲਾਂ ਤੋਂ ਬਣਿਆ ਹੈ ਜਾਂ ਨਹੀਂ; ਜੇ ਹੈ ਤਾਂ `true` ਵਾਪਸ ਕਰਦਾ ਹੈ, ਨਹੀਂ ਤਾਂ `false`।",
		"pol": "`areBooleans` sprawdza, czy dana niepusta tablica zawiera wyłącznie wartości typu boolean, zwracając `true`, jeśli tak jest, w przeciwnym razie `false`.",
		"pes": "`areBooleans` بررسی می‌کند آیا یک آرایهٔ غیرخالیِ داده‌شده فقط شامل مقادیر بولی است یا نه؛ اگر باشد `true` و در غیر این صورت `false` برمی‌گرداند.",
		"por": "`areBooleans` verifica se um determinado array não vazio contém apenas valores booleanos, retornando `true` se contiver e `false` caso contrário.",
		"ron": "`areBooleans` verifică dacă un tablou nevid dat conține doar valori booleene, returnând `true` dacă da și `false` în caz contrar.",
		"rus": "`areBooleans` проверяет, содержит ли заданный непустой массив только булевы значения, возвращая `true`, если это так, и `false` в противном случае.",
		"pus": "`areBooleans` ګوري چې ورکړل شوی غير-خالي array يواځې boolean ارزښتونه لري که نه؛ که ولري `true` واپس کوي او که نه `false`.",
		"slk": "`areBooleans` kontroluje, či dané neprázdne pole obsahuje iba boolean hodnoty; vráti `true`, ak áno, inak vráti `false`.",
		"spa": "`areBooleans` comprueba si un arreglo no vacío dado contiene únicamente valores booleanos, devolviendo `true` si es así y `false` en caso contrario.",
		"slv": "`areBooleans` preveri, ali dan neprazen seznam vsebuje samo logične vrednosti; vrne `true`, če jih, sicer pa `false`.",
		"srp": "`areBooleans` проверава да ли дати непразан низ садржи само булове вредности, враћајући `true` ако је тако и `false` у супротном.",
		"sqi": "`areBooleans` kontrollon nëse një varg i dhënë jo-bosh përmban vetëm vlera boolean, duke kthyer `true` nëse po dhe `false` përndryshe.",
		"swa": "`areBooleans` hukagua kama safu fulani isiyo tupu ina thamani za boolean pekee, ikirejesha `true` ikiwa ndivyo na `false` vinginevyo.",
		"swe": "`areBooleans` kontrollerar om en given icke-tom array endast innehåller boolean-värden och returnerar `true` om den gör det, annars `false`.",
		"tam": "`areBooleans` என்பது கொடுக்கப்பட்ட காலியல்லாத array-ல் boolean மதிப்புகள் மட்டுமே உள்ளனவா என்பதைச் சரிபார்த்து, இருந்தால் `true` ஐவும் இல்லையெனில் `false` ஐவும் திருப்பும்.",
		"tel": "`areBooleans` ఇచ్చిన ఖాళీ కాని అర్రేలో బూలియన్ విలువలు మాత్రమే ఉన్నాయా అని తనిఖీ చేస్తుంది; ఉంటే `true`, లేకపోతే `false` ను తిరిగి ఇస్తుంది.",
		"tgl": "Sinusuri ng `areBooleans` kung ang ibinigay na hindi bakanteng array ay naglalaman lamang ng mga boolean na value, at nagbabalik ng `true` kung oo at `false` kung hindi.",
		"tur": "`areBooleans`, verilen boş olmayan bir dizinin yalnızca boolean değerler içerip içermediğini kontrol eder; içeriyorsa `true`, aksi halde `false` döndürür.",
		"tha": "`areBooleans` ตรวจสอบว่าอาร์เรย์ที่ไม่ว่างเปล่าที่กำหนดมีเฉพาะค่าบูลีนเท่านั้น โดยคืนค่า `true` หากเป็นเช่นนั้น และคืนค่า `false` หากไม่ใช่",
		"ukr": "`areBooleans` перевіряє, чи містить заданий непорожній масив лише булеві значення, повертаючи `true`, якщо так, і `false` в іншому випадку.",
		"uig": "`areBooleans` بېرىلگەن بوش بولمىغان سانلار گۇرۇپپىسىنىڭ پەقەت بولېن قىممەتلەرنىلا ئۆز ئىچىگە ئالغان-ئالمىغانلىقىنى تەكشۈرىدۇ؛ ئەگەر شۇنداق بولسا `true` قايتۇرىدۇ، بولمىسا `false` قايتۇرىدۇ.",
		"urd": "`areBooleans` یہ چیک کرتا ہے کہ آیا دی گئی غیر خالی array میں صرف boolean قدریں موجود ہیں؛ اگر ہیں تو `true` واپس کرتا ہے ورنہ `false`۔",
		"vie": "`areBooleans` kiểm tra xem một mảng không rỗng đã cho có chỉ chứa các giá trị boolean hay không, trả về `true` nếu có và `false` nếu không.",
		"uzb": "`areBooleans` berilgan bo‘sh bo‘lmagan massiv faqat boolean qiymatlardan iboratligini tekshiradi; shunday bo‘lsa `true`, aks holda `false` qaytaradi.",
		"wuu": "`areBooleans` 用来检查给定个非空数组里向是不是只包含布尔值；如果是就返回 `true`，否则返回 `false`。",
		"zho": "`areBooleans` 用于检查给定的非空数组是否只包含布尔值；若是则返回 `true`，否则返回 `false`。",
		"yue": "`areBooleans` 會檢查指定嘅非空陣列係咪只包含 boolean 值；如果係就回傳 `true`，否則回傳 `false`。",
		"zha": "`areBooleans` caeuq ndaej mbouj rox, gwnz ndei raeuj non-empty array daengz booleans laengz, gyaeuj `true` haenh ndaej, gyaeuj `false` haenh mbouj ndaej.",
		"zsm": "`areBooleans` menyemak sama ada tatasusunan tidak kosong yang diberikan mengandungi hanya nilai boolean, mengembalikan `true` jika ya dan `false` jika tidak."
	},
	"subject": []
}