{
	"sourceLanguage": "eng",
	"referenceFileHash": "b0c2de089423106d0697570d1119b445c121a4fcfef3bffe03f5a4e9fe40e06b",
	"aiPromptHash": "9f5ec9e4ef2b88305876eee7c9d77766b651784d11b77ecf8ef81bd005620fa5",
	"translatedTo": {
		"eng": "`isFloat` determines whether a given `value` is a finite floating-point number (a `number` that is not an integer).",
		"afr": "`isFloat` bepaal of 'n gegewe `value` 'n eindige drywende-punt getal is (’n `number` wat nie ’n heelgetal is nie).",
		"ara": "تحدد `isFloat` ما إذا كانت `value` المعطاة عددًا عشريًا منتهيًا (قيمة من نوع `number` ليست عددًا صحيحًا).",
		"arz": "`isFloat` بيحدّد إذا كانت `value` المعطاة رقم عشري محدود (يعني `number` مش عدد صحيح).",
		"bel": "`isFloat` вызначае, ці з’яўляецца зададзенае `value` скончаным лікам з плаваючай коскай (тыпу `number`, які не з’яўляецца цэлым).",
		"ben": "`isFloat` নির্ধারণ করে প্রদত্ত `value` একটি সসীম ভাসমান-বিন্দু সংখ্যা কি না (অর্থাৎ একটি `number` যা পূর্ণসংখ্যা নয়)।",
		"bul": "`isFloat` определя дали дадена `value` е крайно число с плаваща запетая (тип `number`, което не е цяло число).",
		"bos": "`isFloat` određuje da li je zadana `value` konačan broj s pomičnim zarezom (tj. `number` koji nije cijeli broj).",
		"bho": "`isFloat` तय करेला कि दिहल गइल `value` सीमित फ्लोटिंग-पॉइंट संख्या बा कि ना (अइसन `number` जे पूर्णांक ना होखे)।",
		"ces": "`isFloat` určuje, zda je daná `value` konečné číslo s plovoucí desetinnou čárkou (tj. `number`, které není celé číslo).",
		"cat": "`isFloat` determina si un `value` donat és un nombre de coma flotant finit (un `number` que no és un enter).",
		"cjy": "`isFloat` 用来判断给定个 `value` 是不是有限个浮点数（一个非整数个 `number`）。",
		"cmn": "`isFloat` 用于判断给定的 `value` 是否为有限的浮点数（不是整数的 `number`）。",
		"cym": "Mae `isFloat` yn penderfynu a yw `value` penodol yn rhif pwynt arnawf terfynol (sef `number` nad yw'n gyfanrif).",
		"ckb": "`isFloat` دیاری دەکات ئایا `value` ـێکی دیاریکراو ژمارەیەکی فڵۆتی سنووردارە (ژمارەیەک کە ژمارە تەواو نییە) یان نا.",
		"dan": "`isFloat` afgør, om en given `value` er et endeligt flydende tal (et `number`, der ikke er et heltal).",
		"deu": "`isFloat` bestimmt, ob ein gegebener `value` eine endliche Gleitkommazahl ist (eine `number`, die keine Ganzzahl ist).",
		"est": "`isFloat` määrab, kas antud `value` on lõplik ujukomaarv (st `number`, mis ei ole täisarv).",
		"ell": "Το `isFloat` καθορίζει αν μια δεδομένη `value` είναι πεπερασμένος αριθμός κινητής υποδιαστολής (ένας `number` που δεν είναι ακέραιος).",
		"fas": "`isFloat` تعیین می‌کند آیا `value` داده‌شده یک عدد ممیز شناورِ متناهی است (یک `number` که عدد صحیح نیست).",
		"fra": "`isFloat` détermine si une `value` donnée est un nombre à virgule flottante fini (un `number` qui n’est pas un entier).",
		"fin": "`isFloat` määrittää, onko annettu `value` äärellinen liukuluku (eli `number`, joka ei ole kokonaisluku).",
		"gle": "Cinneann `isFloat` an bhfuil `value` tugtha ina uimhir snámhphointe chríochta (uimhir `number` nach slánuimhir í).",
		"hak": "`isFloat` 用來判斷指定个 `value` 係毋係有限个浮點數（一个毋係整數个 `number`）。",
		"gsw": "`isFloat` bestimmt, öb e gegebnigi `value` e endligi Gleitkommazahl isch (es `number`, wo kei Ganzzahl isch).",
		"heb": "`isFloat` קובעת האם `value` נתון הוא מספר נקודה צפה סופי (כלומר `number` שאינו מספר שלם).",
		"hrv": "`isFloat` određuje je li zadana `value` konačan broj s pomičnim zarezom (tj. `number` koji nije cijeli broj).",
		"hin": "`isFloat` निर्धारित करता है कि दिया गया `value` एक सीमित फ्लोटिंग-पॉइंट संख्या है या नहीं (एक `number` जो पूर्णांक नहीं है)।",
		"hsn": "`isFloat` एह तय करदा ऐ कि दित्ती गई `value` finite floating-point number ऐ (इक `number` जेहड़ा integer न होवे)।",
		"ind": "`isFloat` menentukan apakah `value` yang diberikan adalah bilangan floating-point hingga (sebuah `number` yang bukan bilangan bulat).",
		"hun": "Az `isFloat` meghatározza, hogy egy adott `value` véges lebegőpontos szám-e (olyan `number`, amely nem egész szám).",
		"isl": "`isFloat` ákvarðar hvort gefið `value` sé endanleg fleytitala (tala (`number`) sem er ekki heiltala).",
		"jav": "`isFloat` nemtokake apa `value` tartamtu iku angka floating-point sing winates (yaiku `number` sing dudu bilangan bulat).",
		"ita": "`isFloat` determina se un dato `value` è un numero in virgola mobile finito (un `number` che non è un intero).",
		"jpn": "`isFloat` は、与えられた `value` が有限の浮動小数点数（整数ではない `number`）かどうかを判定します。",
		"kat": "`isFloat` განსაზღვრავს, არის თუ არა მოცემული `value` საბოლოო მცურავი წერტილის რიცხვი (ანუ `number`, რომელიც არ არის მთელი რიცხვი).",
		"kor": "`isFloat`는 주어진 `value`가 유한한 부동소수점 숫자(정수가 아닌 `number`)인지 여부를 판단합니다.",
		"lav": "`isFloat` nosaka, vai dotā `value` ir galīgs peldošā komata skaitlis (t. i., `number`, kas nav vesels skaitlis).",
		"lit": "`isFloat` nustato, ar pateikta `value` yra baigtinis slankiojo kablelio skaičius (t. y. `number`, kuris nėra sveikasis skaičius).",
		"kur": "`isFloat` diyar dike ka `value`-eke dayîn hejmareke floating-point a finite ye (hejmarek `number` ku integer nîne).",
		"mar": "`isFloat` दिलेले `value` हे मर्यादित फ्लोटिंग-पॉइंट संख्या आहे का (म्हणजे पूर्णांक नसलेली `number`) हे ठरवते.",
		"mkd": "`isFloat` утврдува дали дадената `value` е конечен број со подвижна запирка (еден `number` што не е цел број).",
		"mya": "`isFloat` သည် ပေးထားသော `value` သည် အကန့်အသတ်ရှိသော floating-point ကိန်းဂဏန်း (ကိန်းပြည့်မဟုတ်သော `number`) ဟုတ်မဟုတ်ကို သတ်မှတ်ပေးသည်။",
		"nan": "`isFloat` 會判斷指定个 `value` 是毋是有限个浮點數（一个毋是整數个 `number`）。",
		"nep": "`isFloat` ले दिइएको `value` सीमित फ्लोटिङ-पोइन्ट संख्या (पूर्णांक नभएको `number`) हो कि होइन भनेर निर्धारण गर्छ।",
		"nld": "`isFloat` bepaalt of een gegeven `value` een eindig drijvendekommagetal is (een `number` die geen integer is).",
		"nno": "`isFloat` avgjer om ein gitt `value` er eit endeleg flyttal (eit `number` som ikkje er eit heiltal).",
		"nob": "`isFloat` avgjør om en gitt `value` er et endelig flyttall (et `number` som ikke er et heltall).",
		"nor": "`isFloat` avgjør om en gitt `value` er et endelig flyttall (et `number` som ikke er et heltall).",
		"pcm": "`isFloat` dey determine whether given `value` na finite floating-point number (one `number` wey no be integer).",
		"pan": "`isFloat` ਇਹ ਨਿਰਧਾਰਤ ਕਰਦਾ ਹੈ ਕਿ ਦਿੱਤਾ ਗਿਆ `value` ਇੱਕ ਸੀਮਤ ਫਲੋਟਿੰਗ-ਪੌਇੰਟ ਨੰਬਰ ਹੈ ਜਾਂ ਨਹੀਂ (ਇੱਕ `number` ਜੋ ਪੂਰਨ ਅੰਕ ਨਹੀਂ ਹੈ)।",
		"orm": "`isFloat` `value` kenname lakkoofsa floating-point finite ta'uu isaa (jechuunis `number` ta'ee lakkoofsa guutuu hin taane) murteessa.",
		"pes": "`isFloat` تعیین می‌کند آیا `value` داده‌شده یک عدد ممیز شناورِ متناهی است (یک `number` که عدد صحیح نیست).",
		"por": "`isFloat` determina se um determinado `value` é um número de ponto flutuante finito (um `number` que não é um inteiro).",
		"pol": "`isFloat` określa, czy podana `value` jest skończoną liczbą zmiennoprzecinkową (tj. `number`, która nie jest liczbą całkowitą).",
		"pus": "`isFloat` ټاکي چې ورکړل شوی `value` يو محدود اعشاري (floating-point) عدد دی که نه (يو `number` چې صحیح عدد نه وي).",
		"ron": "`isFloat` determină dacă un `value` dat este un număr finit în virgulă mobilă (un `number` care nu este un întreg).",
		"rus": "`isFloat` определяет, является ли заданное `value` конечным числом с плавающей запятой (тип `number`, которое не является целым).",
		"slk": "`isFloat` určuje, či je daná `value` konečné číslo s pohyblivou desatinnou čiarkou (t. j. `number`, ktorý nie je celé číslo).",
		"slv": "`isFloat` določi, ali je podana `value` končno število s plavajočo vejico (tj. `number`, ki ni celo število).",
		"spa": "`isFloat` determina si un `value` dado es un número de punto flotante finito (un `number` que no es un entero).",
		"sqi": "`isFloat` përcakton nëse një `value` e dhënë është një numër me presje dhjetore i fundëm (një `number` që nuk është numër i plotë).",
		"srp": "`isFloat` одређује да ли је дати `value` коначан број са покретним зарезом (тј. `number` који није цео број).",
		"swa": "`isFloat` huamua kama `value` iliyotolewa ni nambari ya nukta inayoelea yenye kikomo (yaani `number` ambayo si nambari kamili).",
		"swe": "`isFloat` avgör om ett givet `value` är ett ändligt flyttal (ett `number` som inte är ett heltal).",
		"tam": "`isFloat` என்பது கொடுக்கப்பட்ட `value` ஒரு முடிவுற்ற மிதவை-புள்ளி எண் (முழு எண் அல்லாத `number`) தானா என்பதை நிர்ணயிக்கிறது.",
		"tel": "`isFloat` ఇచ్చిన `value` పరిమితమైన ఫ్లోటింగ్-పాయింట్ సంఖ్యా (పూర్ణసంఖ్య కాని `number`) కాదా అని నిర్ణయిస్తుంది.",
		"tgl": "Tinutukoy ng `isFloat` kung ang ibinigay na `value` ay isang finite na floating-point na numero (isang `number` na hindi integer).",
		"tha": "`isFloat` ใช้กำหนดว่า `value` ที่กำหนดเป็นเลขทศนิยมแบบจำนวนจำกัดหรือไม่ (เป็น `number` ที่ไม่ใช่จำนวนเต็ม).",
		"tur": "`isFloat`, verilen `value` değerinin sonlu bir kayan noktalı sayı (tamsayı olmayan bir `number`) olup olmadığını belirler.",
		"uig": "`isFloat` بېرىلگەن `value` نىڭ چەكلىك لەيلىمە نۇقتىلىق سان (پۈتۈن سان بولمىغان `number`) ئىكەنلىكىنى بەلگىلەيدۇ.",
		"ukr": "`isFloat` визначає, чи є задане `value` скінченним числом з плаваючою комою (тобто `number`, який не є цілим числом).",
		"urd": "`isFloat` یہ طے کرتا ہے کہ دیا گیا `value` ایک محدود فلوٹنگ پوائنٹ نمبر ہے یا نہیں (یعنی ایسا `number` جو صحیح عدد نہ ہو)۔",
		"vie": "`isFloat` xác định liệu `value` đã cho có phải là một số dấu phẩy động hữu hạn hay không (một `number` không phải số nguyên).",
		"uzb": "`isFloat` berilgan `value` chekli suzuvchi nuqtali son (butun son bo‘lmagan `number`) ekanligini aniqlaydi.",
		"wuu": "`isFloat` 用来判断给定个 `value` 是不是有限个浮点数（一个不是整数个 `number`）。",
		"yue": "`isFloat` 用嚟判斷指定嘅 `value` 係咪有限嘅浮點數（即係唔係整數嘅 `number`）。",
		"zho": "`isFloat` 用于判断给定的 `value` 是否为有限的浮点数（即不是整数的 `number`）。",
		"zsm": "`isFloat` menentukan sama ada `value` yang diberikan ialah nombor titik apungan terhingga (sebuah `number` yang bukan integer).",
		"zha": "`isFloat` raeuj dingq, `value` rox finite floating-point number (gwnz `number` mbouj rox integer) raeuj."
	},
	"subject": []
}