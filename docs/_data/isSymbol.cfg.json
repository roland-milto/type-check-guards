{
	"sourceLanguage": "eng",
	"referenceFileHash": "9c35451086f3e937cc09f0281a3999d4484e3ac01c693129940a472190656ecd",
	"aiPromptHash": "9f5ec9e4ef2b88305876eee7c9d77766b651784d11b77ecf8ef81bd005620fa5",
	"translatedTo": {
		"eng": "`isSymbol` determines whether a given value is of type `symbol`, returning `true` for symbols and `false` otherwise.",
		"arz": "`isSymbol` بيحدد إذا كانت قيمة معيّنة من نوع `symbol`، وبيُرجّع `true` للـ symbols و`false` لغير كده.",
		"ara": "تحدّد `isSymbol` ما إذا كانت قيمة معيّنة من النوع `symbol`، وتُرجع `true` للرموز و`false` خلاف ذلك.",
		"afr": "`isSymbol` bepaal of 'n gegewe waarde van tipe `symbol` is, en gee `true` vir simbole en `false` andersins terug.",
		"bho": "`isSymbol` तय करेला कि दिहल मान `symbol` टाइप के बा कि ना, symbols खातिर `true` आ नाहीं त `false` लौटावेला।",
		"bel": "`isSymbol` вызначае, ці мае зададзенае значэнне тып `symbol`, вяртаючы `true` для сімвалаў і `false` у іншых выпадках.",
		"ben": "`isSymbol` নির্ধারণ করে কোনো প্রদত্ত মান `symbol` টাইপের কি না; symbol হলে `true` এবং অন্যথায় `false` ফেরত দেয়।",
		"cat": "`isSymbol` determina si un valor determinat és del tipus `symbol`, retornant `true` per als símbols i `false` en cas contrari.",
		"bul": "`isSymbol` определя дали дадена стойност е от тип `symbol`, като връща `true` за символи и `false` в противен случай.",
		"bos": "`isSymbol` određuje da li je data vrijednost tipa `symbol`, vraćajući `true` za symbole i `false` u suprotnom.",
		"cjy": "`isSymbol` 用来判断给定个值係唔係 `symbol` 类型：係 symbol 就返回 `true`，唔係就返回 `false`。",
		"ces": "`isSymbol` určuje, zda je daná hodnota typu `symbol`; pro symboly vrací `true` a jinak `false`.",
		"cmn": "`isSymbol` 用于判断给定值是否为 `symbol` 类型：对于 symbol 返回 `true`，否则返回 `false`。",
		"ckb": "`isSymbol` دیاری دەکات کە ئایا بەهایەکی دیاریکراو لە جۆری `symbol` ـە، بۆ symbol ـەکان `true` دەگەڕێنێتەوە و بۆ ئەوانەی تر `false`.",
		"dan": "`isSymbol` afgør, om en given værdi er af typen `symbol`, og returnerer `true` for symboler og ellers `false`.",
		"deu": "`isSymbol` bestimmt, ob ein gegebener Wert vom Typ `symbol` ist, und gibt für Symbole `true` und andernfalls `false` zurück.",
		"cym": "Mae `isSymbol` yn penderfynu a yw gwerth penodol o'r math `symbol`, gan ddychwelyd `true` ar gyfer symbolau a `false` fel arall.",
		"est": "`isSymbol` määrab, kas antud väärtus on tüüpi `symbol`, tagastades sümbolite korral `true` ja muudel juhtudel `false`.",
		"ell": "Το `isSymbol` καθορίζει αν μια δεδομένη τιμή είναι τύπου `symbol`, επιστρέφοντας `true` για σύμβολα και `false` διαφορετικά.",
		"fas": "`isSymbol` تعیین می‌کند آیا یک مقدار داده‌شده از نوع `symbol` است یا نه؛ برای symbolها مقدار `true` و در غیر این صورت `false` برمی‌گرداند.",
		"fra": "`isSymbol` détermine si une valeur donnée est de type `symbol`, en renvoyant `true` pour les symboles et `false` sinon.",
		"fin": "`isSymbol` määrittää, onko annettu arvo tyyppiä `symbol`, palauttaen `true` symboleille ja `false` muussa tapauksessa.",
		"gle": "Cinneann `isSymbol` an bhfuil luach ar leith den chineál `symbol`, ag filleadh `true` do shiombailí agus `false` murach sin.",
		"gsw": "`isSymbol` bestimmt, öb e gegebene Wärt vom Typ `symbol` isch, und git für Symbols `true` zrugg und susch `false`.",
		"hak": "`isSymbol` 用來判斷一個指定个值係毋係 `symbol` 型別；係 symbol 就回傳 `true`，毋係就回傳 `false`。",
		"heb": "`isSymbol` קובע האם ערך נתון הוא מטיפוס `symbol`, ומחזיר `true` עבור סמלים ו-`false` אחרת.",
		"hin": "`isSymbol` यह निर्धारित करता है कि दिया गया मान `symbol` प्रकार का है या नहीं; symbols के लिए `true` और अन्यथा `false` लौटाता है।",
		"hrv": "`isSymbol` određuje je li zadana vrijednost tipa `symbol`, vraćajući `true` za simbole i `false` u suprotnom.",
		"hun": "Az `isSymbol` meghatározza, hogy egy adott érték `symbol` típusú-e, szimbólumok esetén `true`-t, egyébként `false`-t ad vissza.",
		"hsn": "`isSymbol` طے کردا ہے کہ دِتّی گئی ویلیو `symbol` ٹائپ دی ہے یا نہیں، symbols لئی `true` تے ہور صورت وچ `false` واپس کردا ہے۔",
		"ind": "`isSymbol` menentukan apakah suatu nilai yang diberikan bertipe `symbol`, mengembalikan `true` untuk symbol dan `false` jika tidak.",
		"ita": "`isSymbol` determina se un determinato valore è di tipo `symbol`, restituendo `true` per i symbol e `false` altrimenti.",
		"isl": "`isSymbol` ákvarðar hvort tiltekið gildi sé af gerðinni `symbol` og skilar `true` fyrir tákn og `false` annars.",
		"jav": "`isSymbol` nemtokake apa sawijining nilai kalebu jinis `symbol`, mbalekake `true` kanggo simbol lan `false` kanggo liyane.",
		"kor": "`isSymbol`은 주어진 값이 `symbol` 타입인지 여부를 판단하며, 심볼이면 `true`를 반환하고 그렇지 않으면 `false`를 반환합니다.",
		"jpn": "`isSymbol` は、指定された値が `symbol` 型かどうかを判定し、シンボルであれば `true`、それ以外は `false` を返します。",
		"kat": "`isSymbol` განსაზღვრავს, არის თუ არა მოცემული მნიშვნელობა `symbol` ტიპის, აბრუნებს `true`-ს symbol-ებისთვის და `false`-ს სხვა შემთხვევებში.",
		"kur": "`isSymbol` diyar dike ka nirxek hatî dayîn ji cureya `symbol` e an na, ji bo symbolan `true` vedigerîne û ji bo yên din `false`.",
		"lav": "`isSymbol` nosaka, vai dotā vērtība ir tipa `symbol`, atgriežot `true` simboliem un `false` citos gadījumos.",
		"lit": "`isSymbol` nustato, ar pateikta reikšmė yra tipo `symbol`, grąžindamas `true` simboliams ir `false` kitu atveju.",
		"mar": "`isSymbol` दिलेले मूल्य `symbol` प्रकाराचे आहे का हे ठरवते; symbols साठी `true` आणि अन्यथा `false` परत करते.",
		"mkd": "`isSymbol` утврдува дали дадена вредност е од тип `symbol`, враќајќи `true` за симболи и `false` во спротивно.",
		"nep": "`isSymbol` ले दिइएको मान `symbol` प्रकारको हो कि होइन भनेर निर्धारण गर्छ; symbol भए `true` र अन्यथा `false` फर्काउँछ।",
		"nan": "`isSymbol` 用來判斷指定个值敢是 `symbol` 型別；若是 symbol 就回傳 `true`，無是就回傳 `false`。",
		"mya": "`isSymbol` သည် ပေးထားသော တန်ဖိုးသည် `symbol` အမျိုးအစား ဟုတ်မဟုတ်ကို သတ်မှတ်ပြီး symbol ဖြစ်ပါက `true` ကို ပြန်ပေးကာ မဟုတ်ပါက `false` ကို ပြန်ပေးသည်။",
		"nno": "`isSymbol` avgjer om ein gitt verdi er av typen `symbol`, og returnerer `true` for symbol og `false` elles.",
		"nld": "`isSymbol` bepaalt of een gegeven waarde van het type `symbol` is, en retourneert `true` voor symbolen en anders `false`.",
		"nob": "`isSymbol` avgjør om en gitt verdi er av typen `symbol`, og returnerer `true` for symboler og `false` ellers.",
		"nor": "`isSymbol` avgjør om en gitt verdi er av typen `symbol`, og returnerer `true` for symboler og `false` ellers.",
		"orm": "`isSymbol` gatiin kenname gosa `symbol` ta'uu isaa murteessa; symbol irratti `true` deebisa, kan biraa irratti immoo `false` deebisa.",
		"pcm": "`isSymbol` dey determine whether one value na type `symbol`, e go return `true` for symbols and `false` if no be so.",
		"pan": "`isSymbol` ਇਹ ਨਿਰਧਾਰਤ ਕਰਦਾ ਹੈ ਕਿ ਦਿੱਤਾ ਗਿਆ ਮੁੱਲ `symbol` ਕਿਸਮ ਦਾ ਹੈ ਜਾਂ ਨਹੀਂ; symbols ਲਈ `true` ਅਤੇ ਹੋਰਥਾਂ `false` ਵਾਪਸ ਕਰਦਾ ਹੈ।",
		"pes": "`isSymbol` تعیین می‌کند آیا یک مقدار داده‌شده از نوع `symbol` است یا نه؛ برای symbolها `true` و در غیر این صورت `false` برمی‌گرداند.",
		"por": "`isSymbol` determina se um determinado valor é do tipo `symbol`, retornando `true` para símbolos e `false` caso contrário.",
		"pol": "`isSymbol` określa, czy dana wartość jest typu `symbol`, zwracając `true` dla symboli i `false` w przeciwnym razie.",
		"pus": "`isSymbol` ټاکي چې ورکړل شوی ارزښت د `symbol` ډول دی که نه؛ د symbol لپاره `true` او بل ډول لپاره `false` بېرته ورکوي.",
		"ron": "`isSymbol` determină dacă o valoare dată este de tip `symbol`, returnând `true` pentru simboluri și `false` în caz contrar.",
		"rus": "`isSymbol` определяет, является ли заданное значение типом `symbol`, возвращая `true` для symbol и `false` в противном случае.",
		"slv": "`isSymbol` določi, ali je dana vrednost tipa `symbol`, pri čemer vrne `true` za simbole in `false` sicer.",
		"spa": "`isSymbol` determina si un valor dado es de tipo `symbol`, devolviendo `true` para símbolos y `false` en caso contrario.",
		"slk": "`isSymbol` určuje, či je daná hodnota typu `symbol`, pričom vracia `true` pre symboly a `false` v opačnom prípade.",
		"sqi": "`isSymbol` përcakton nëse një vlerë e dhënë është e tipit `symbol`, duke kthyer `true` për simbolet dhe `false` përndryshe.",
		"swa": "`isSymbol` huamua kama thamani iliyotolewa ni ya aina `symbol`, ikirudisha `true` kwa symbols na `false` vinginevyo.",
		"srp": "`isSymbol` одређује да ли је дата вредност типа `symbol`, враћајући `true` за симболе и `false` у супротном.",
		"swe": "`isSymbol` avgör om ett givet värde är av typen `symbol` och returnerar `true` för symboler och `false` annars.",
		"tam": "`isSymbol` என்பது கொடுக்கப்பட்ட மதிப்பு `symbol` வகையா என்பதை நிர்ணயித்து, symbols-க்கு `true` மற்றும் இல்லையெனில் `false` என்பதைத் திருப்பி அளிக்கிறது.",
		"tel": "`isSymbol` ఇచ్చిన విలువ `symbol` రకానికి చెందినదో లేదో నిర్ణయిస్తుంది; సింబల్స్‌కు `true` ను, లేకపోతే `false` ను తిరిగి ఇస్తుంది.",
		"tgl": "Tinutukoy ng `isSymbol` kung ang ibinigay na value ay may type na `symbol`, na nagbabalik ng `true` para sa mga symbol at `false` kung hindi.",
		"tha": "`isSymbol` ใช้กำหนดว่าค่าที่กำหนดเป็นชนิด `symbol` หรือไม่ โดยคืนค่า `true` สำหรับสัญลักษณ์ และ `false` ในกรณีอื่น",
		"tur": "`isSymbol`, verilen bir değerin `symbol` türünde olup olmadığını belirler; semboller için `true`, aksi halde `false` döndürür.",
		"uig": "`isSymbol` بېرىلگەن قىممەتنىڭ `symbol` تىپىدا ياكى ئەمەسلىكىنى بەلگىلەيدۇ؛ symbol بولسا `true` قايتۇرىدۇ، بولمىسا `false` قايتۇرىدۇ.",
		"ukr": "`isSymbol` визначає, чи має задане значення тип `symbol`, повертаючи `true` для символів і `false` в інших випадках.",
		"vie": "`isSymbol` xác định liệu một giá trị cho trước có thuộc kiểu `symbol` hay không, trả về `true` cho symbol và `false` trong các trường hợp khác.",
		"urd": "`isSymbol` یہ طے کرتا ہے کہ دی گئی قدر کی قسم `symbol` ہے یا نہیں؛ symbols کے لیے `true` اور بصورتِ دیگر `false` واپس کرتا ہے۔",
		"uzb": "`isSymbol` berilgan qiymat `symbol` turiga mansubligini aniqlaydi: symbol bo‘lsa `true`, aks holda `false` qaytaradi.",
		"wuu": "`isSymbol` 用来判断给定个值是不是 `symbol` 类型：是 symbol 就返回 `true`，否则返回 `false`。",
		"yue": "`isSymbol` 用嚟判斷指定值係咪 `symbol` 類型；如果係 symbol 就回傳 `true`，否則回傳 `false`。",
		"zha": "`isSymbol` raeuj gwnz ndei gwnz value aen type `symbol` rox, symbol de return `true`, mbouj aen de return `false`.",
		"zho": "`isSymbol` 用于判断给定值是否为 `symbol` 类型；对于 symbol 返回 `true`，否则返回 `false`。",
		"zsm": "`isSymbol` menentukan sama ada sesuatu nilai yang diberikan adalah daripada jenis `symbol`, mengembalikan `true` untuk simbol dan `false` jika sebaliknya."
	},
	"subject": []
}