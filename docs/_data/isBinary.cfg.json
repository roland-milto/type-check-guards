{
	"sourceLanguage": "eng",
	"referenceFileHash": "4a8fa4070d3ec2a7028ce41cc7071fb09bd398125de95974aea97060ad2e6c49",
	"aiPromptHash": "9f5ec9e4ef2b88305876eee7c9d77766b651784d11b77ecf8ef81bd005620fa5",
	"translatedTo": {
		"eng": "`isBinary` determines whether a value is a binary string (optionally prefixed with `0b`/`0B`) and returns `true` or `false`.",
		"ara": "تحدد `isBinary` ما إذا كانت القيمة سلسلة ثنائية (مع إمكانية وجود بادئة `0b`/`0B`) وتعيد `true` أو `false`.",
		"afr": "`isBinary` bepaal of 'n waarde 'n binêre string is (opsioneel met `0b`/`0B` as voorvoegsel) en gee `true` of `false` terug.",
		"arz": "`isBinary` بيحدد إذا كانت القيمة سترينج باينري (وممكن تبقى مسبوقة بـ `0b`/`0B`) وبيُرجّع `true` أو `false`.",
		"bel": "`isBinary` вызначае, ці з'яўляецца значэнне бінарным радком (з неабавязковым прэфіксам `0b`/`0B`) і вяртае `true` або `false`.",
		"ben": "`isBinary` নির্ধারণ করে কোনো মান বাইনারি স্ট্রিং কি না (ঐচ্ছিকভাবে `0b`/`0B` প্রিফিক্সসহ) এবং `true` বা `false` রিটার্ন করে।",
		"bho": "`isBinary` तय करेला कि कउनो मान बाइनरी स्ट्रिंग बा कि ना (वैकल्पिक रूप से `0b`/`0B` प्रीफिक्स सहित) आ `true` भा `false` लौटावेला।",
		"bos": "`isBinary` određuje da li je vrijednost binarni string (opcionalno s prefiksom `0b`/`0B`) i vraća `true` ili `false`.",
		"bul": "`isBinary` определя дали дадена стойност е двоичен низ (по избор с префикс `0b`/`0B`) и връща `true` или `false`.",
		"cat": "`isBinary` determina si un valor és una cadena binària (opcionalment amb el prefix `0b`/`0B`) i retorna `true` o `false`.",
		"ces": "`isBinary` určuje, zda je hodnota binární řetězec (volitelně s předponou `0b`/`0B`) a vrací `true` nebo `false`.",
		"cjy": "`isBinary` 用嚟判断一个值系咪二进制字符串（可选带 `0b`/`0B` 前缀），并返回 `true` 或 `false`。",
		"cmn": "`isBinary` 用于判断一个值是否为二进制字符串（可选带 `0b`/`0B` 前缀），并返回 `true` 或 `false`。",
		"dan": "`isBinary` afgør, om en værdi er en binær streng (valgfrit med præfiks `0b`/`0B`) og returnerer `true` eller `false`.",
		"cym": "Mae `isBinary` yn penderfynu a yw gwerth yn llinyn deuaidd (gyda rhagddodiad `0b`/`0B` yn ddewisol) ac yn dychwelyd `true` neu `false`.",
		"ckb": "`isBinary` دیاری دەکات کە ئایا بەهایەک ڕیزەنووسی دووانەیە (بە دڵخوازانە پێشگرەوەی `0b`/`0B` هەبێت) و `true` یان `false` دەگەڕێنێتەوە.",
		"deu": "`isBinary` bestimmt, ob ein Wert eine Binärzeichenfolge ist (optional mit `0b`/`0B` vorangestellt) und gibt `true` oder `false` zurück.",
		"ell": "Το `isBinary` καθορίζει αν μια τιμή είναι δυαδική συμβολοσειρά (προαιρετικά με πρόθεμα `0b`/`0B`) και επιστρέφει `true` ή `false`.",
		"est": "`isBinary` määrab, kas väärtus on binaarstring (valikuliselt `0b`/`0B` prefiksiga), ja tagastab `true` või `false`.",
		"fas": "`isBinary` تعیین می‌کند آیا یک مقدار، رشتهٔ دودویی (با پیشوند اختیاری `0b`/`0B`) است یا نه و `true` یا `false` برمی‌گرداند.",
		"fin": "`isBinary` määrittää, onko arvo binaarimerkkijono (valinnaisesti `0b`/`0B`-etuliitteellä) ja palauttaa `true` tai `false`.",
		"fra": "`isBinary` détermine si une valeur est une chaîne binaire (éventuellement préfixée par `0b`/`0B`) et renvoie `true` ou `false`.",
		"hak": "`isBinary` 判斷一個值係毋係二進位字串（做得選擇性加 `0b`/`0B` 前綴），並回傳 `true` 或 `false`。",
		"gsw": "`isBinary` bestimmt, öb e Wert e Binär-String isch (optional mit `0b`/`0B`-Präfix) und git `true` oder `false` zrugg.",
		"gle": "Cinneann `isBinary` an teaghrán dénártha é luach (le réimír `0b`/`0B` go roghnach) agus filleann sé `true` nó `false`.",
		"hin": "`isBinary` यह निर्धारित करता है कि कोई मान बाइनरी स्ट्रिंग है या नहीं (वैकल्पिक रूप से `0b`/`0B` प्रीफ़िक्स के साथ) और `true` या `false` लौटाता है।",
		"heb": "`isBinary` קובע האם ערך הוא מחרוזת בינארית (עם קידומת אופציונלית `0b`/`0B`) ומחזיר `true` או `false`.",
		"hrv": "`isBinary` određuje je li vrijednost binarni niz znakova (po želji s prefiksom `0b`/`0B`) i vraća `true` ili `false`.",
		"ind": "`isBinary` menentukan apakah suatu nilai adalah string biner (opsional diawali dengan `0b`/`0B`) dan mengembalikan `true` atau `false`.",
		"hsn": "`isBinary` ایہ طے کردا اے کہ کوئی value بائنری سٹرنگ اے (اختیاری طور تے `0b`/`0B` prefix دے نال) تے `true` یا `false` واپس کردا اے۔",
		"hun": "Az `isBinary` meghatározza, hogy egy érték bináris karakterlánc-e (opcionálisan `0b`/`0B` előtaggal), és `true` vagy `false` értéket ad vissza.",
		"ita": "`isBinary` determina se un valore è una stringa binaria (facoltativamente con prefisso `0b`/`0B`) e restituisce `true` o `false`.",
		"isl": "`isBinary` ákvarðar hvort gildi sé tvíundarstrengur (valkvætt með `0b`/`0B` forskeyti) og skilar `true` eða `false`.",
		"jav": "`isBinary` nemtokaké apa sawijining nilai iku string biner (opsional nganggo prefiks `0b`/`0B`) lan mbalèkaké `true` utawa `false`.",
		"jpn": "`isBinary` は、値が（任意で `0b`/`0B` が付いた）2進文字列かどうかを判定し、`true` または `false` を返します。",
		"kor": "`isBinary`는 값이 이진 문자열인지(`0b`/`0B` 접두사는 선택 사항) 판별하고 `true` 또는 `false`를 반환합니다.",
		"kat": "`isBinary` განსაზღვრავს, არის თუ არა მნიშვნელობა ბინარული სტრიქონი (`0b`/`0B` პრეფიქსით სურვილისამებრ) და აბრუნებს `true` ან `false`.",
		"kur": "`isBinary` diyar dike ka nirxek rêzek duî (binary) ye (bi vebijarkî bi pêşgira `0b`/`0B`) û `true` an `false` vedigere.",
		"lit": "`isBinary` nustato, ar reikšmė yra dvejetainė eilutė (pasirinktinai su `0b`/`0B` priešdėliu), ir grąžina `true` arba `false`.",
		"lav": "`isBinary` nosaka, vai vērtība ir bināra virkne (pēc izvēles ar prefiksu `0b`/`0B`), un atgriež `true` vai `false`.",
		"mar": "`isBinary` एखादे मूल्य बायनरी स्ट्रिंग आहे का (पर्यायी `0b`/`0B` उपसर्गासह) हे ठरवते आणि `true` किंवा `false` परत करते.",
		"mkd": "`isBinary` одредува дали некоја вредност е бинарна низа (опционално со префикс `0b`/`0B`) и враќа `true` или `false`.",
		"mya": "`isBinary` သည် တန်ဖိုးတစ်ခုသည် binary string (လိုအပ်ပါက `0b`/`0B` ရှေ့ဆက်ပါနိုင်) ဟုတ်မဟုတ်ကို သတ်မှတ်ပြီး `true` သို့မဟုတ် `false` ကို ပြန်ပေးသည်။",
		"nep": "`isBinary` ले कुनै मान बाइनरी स्ट्रिङ हो कि होइन (वैकल्पिक रूपमा `0b`/`0B` उपसर्गसहित) निर्धारण गर्छ र `true` वा `false` फर्काउँछ।",
		"nan": "`isBinary` phán-toàn chi̍t ê chhi̍t sī-m̄-sī binary ê chù-li̍p (ē-sái ū `0b`/`0B` prefix), jî-chhiáⁿ hôe-sîng `true` á-sī `false`.",
		"nld": "`isBinary` bepaalt of een waarde een binaire string is (optioneel met het voorvoegsel `0b`/`0B`) en retourneert `true` of `false`.",
		"nno": "`isBinary` avgjer om ein verdi er ein binærstreng (valfritt med prefiks `0b`/`0B`) og returnerer `true` eller `false`.",
		"nob": "`isBinary` avgjør om en verdi er en binærstreng (valgfritt med prefiks `0b`/`0B`) og returnerer `true` eller `false`.",
		"nor": "`isBinary` avgjør om en verdi er en binær streng (valgfritt med prefiks `0b`/`0B`) og returnerer `true` eller `false`.",
		"orm": "`isBinary` gatiin tokko diraa baayinarii ta'uu isaa (filannoo `0b`/`0B` durtii waliin) murteessa, `true` yookaan `false` ni deebisa.",
		"pcm": "`isBinary` dey determine whether value na binary string (e fit get `0b`/`0B` prefix) and e dey return `true` or `false`.",
		"pan": "`isBinary` ਇਹ ਨਿਰਧਾਰਤ ਕਰਦਾ ਹੈ ਕਿ ਕੋਈ ਵੈਲਯੂ ਬਾਈਨਰੀ ਸਟਰਿੰਗ ਹੈ (ਚੋਣਵਾਂ ਤੌਰ ‘ਤੇ `0b`/`0B` ਨਾਲ ਪ੍ਰੀਫਿਕਸ ਕੀਤੀ ਹੋਈ) ਅਤੇ `true` ਜਾਂ `false` ਵਾਪਸ ਕਰਦਾ ਹੈ।",
		"pol": "`isBinary` określa, czy wartość jest ciągiem binarnym (opcjonalnie z prefiksem `0b`/`0B`) i zwraca `true` lub `false`.",
		"pes": "`isBinary` تعیین می‌کند آیا یک مقدار، رشتهٔ دودویی است (به‌صورت اختیاری با پیشوند `0b`/`0B`) و `true` یا `false` برمی‌گرداند.",
		"por": "`isBinary` determina se um valor é uma string binária (opcionalmente prefixada com `0b`/`0B`) e retorna `true` ou `false`.",
		"rus": "`isBinary` определяет, является ли значение двоичной строкой (опционально с префиксом `0b`/`0B`), и возвращает `true` или `false`.",
		"ron": "`isBinary` determină dacă o valoare este un șir binar (opțional prefixat cu `0b`/`0B`) și returnează `true` sau `false`.",
		"pus": "`isBinary` ټاکي چې آیا یو ارزښت باینري سټرینګ دی (په اختیاري ډول د `0b`/`0B` مخکښ سره) او `true` یا `false` بېرته ورکوي.",
		"spa": "`isBinary` determina si un valor es una cadena binaria (opcionalmente con el prefijo `0b`/`0B`) y devuelve `true` o `false`.",
		"slv": "`isBinary` določi, ali je vrednost binarni niz (po želji s predpono `0b`/`0B`) in vrne `true` ali `false`.",
		"slk": "`isBinary` určuje, či je hodnota binárny reťazec (voliteľne s predponou `0b`/`0B`) a vráti `true` alebo `false`.",
		"srp": "`isBinary` одређује да ли је вредност бинарна ниска (опционо са префиксом `0b`/`0B`) и враћа `true` или `false`.",
		"sqi": "`isBinary` përcakton nëse një vlerë është një varg binar (opsionalisht me prefiks `0b`/`0B`) dhe kthen `true` ose `false`.",
		"swa": "`isBinary` huamua kama thamani ni tungo ya binari (kwa hiari ikiwa na kiambishi awali `0b`/`0B`) na hurejesha `true` au `false`.",
		"swe": "`isBinary` avgör om ett värde är en binär sträng (valfritt med prefixet `0b`/`0B`) och returnerar `true` eller `false`.",
		"tam": "`isBinary` ஒரு மதிப்பு இரும சரமா (விருப்பமாக `0b`/`0B` முன்னொட்டுடன்) என்பதை நிர்ணயித்து `true` அல்லது `false` ஐத் திருப்பி அளிக்கிறது.",
		"tel": "`isBinary` ఒక విలువ బైనరీ స్ట్రింగ్‌నా (ఐచ్ఛికంగా `0b`/`0B` ఉపసర్గతో) అని నిర్ణయించి `true` లేదా `false` ను తిరిగి ఇస్తుంది.",
		"tgl": "Tinutukoy ng `isBinary` kung ang isang value ay isang binary string (maaaring may `0b`/`0B` na prefix) at nagbabalik ng `true` o `false`.",
		"tha": "`isBinary` ใช้ตรวจสอบว่าค่าเป็นสตริงไบนารีหรือไม่ (อาจมีคำนำหน้า `0b`/`0B`) และคืนค่า `true` หรือ `false`",
		"tur": "`isBinary`, bir değerin ikili (binary) dize olup olmadığını (isteğe bağlı olarak `0b`/`0B` önekiyle) belirler ve `true` veya `false` döndürür.",
		"uig": "`isBinary` بىر قىممەتنىڭ ئىككىلىك تىزىم (ئىختىيارىي ھالدا `0b`/`0B` ئالدى قوشۇلغۇچىسى بىلەن) ئىكەنلىكىنى بەلگىلەپ، `true` ياكى `false` قايتۇرىدۇ.",
		"ukr": "`isBinary` визначає, чи є значення двійковим рядком (за потреби з префіксом `0b`/`0B`) і повертає `true` або `false`.",
		"urd": "`isBinary` یہ طے کرتا ہے کہ آیا کوئی قدر بائنری سٹرنگ ہے (اختیاری طور پر `0b`/`0B` کے سابقے کے ساتھ) اور `true` یا `false` واپس کرتا ہے۔",
		"vie": "`isBinary` xác định liệu một giá trị có phải là chuỗi nhị phân (tùy chọn có tiền tố `0b`/`0B`) hay không và trả về `true` hoặc `false`.",
		"uzb": "`isBinary` qiymatning ikkilik satr (ixtiyoriy ravishda `0b`/`0B` prefiksi bilan) ekanini aniqlaydi va `true` yoki `false` qaytaradi.",
		"wuu": "`isBinary` 判断一个值是不是二进制字符串（可以带 `0b`/`0B` 前缀），并返回 `true` 或 `false`。",
		"yue": "`isBinary` 用嚟判斷一個值係咪二進制字串（可選擇性帶 `0b`/`0B` 前綴），並回傳 `true` 或 `false`。",
		"zha": "`isBinary` gwnz ndaej raeuz value si binary string (hoiq youq `0b`/`0B` prefix) laengz dawz `true` hoiq `false`.",
		"zho": "`isBinary` 用于判断一个值是否为二进制字符串（可选带 `0b`/`0B` 前缀），并返回 `true` 或 `false`。",
		"zsm": "`isBinary` menentukan sama ada sesuatu nilai ialah rentetan binari (secara pilihan diawali dengan `0b`/`0B`) dan mengembalikan `true` atau `false`."
	},
	"subject": []
}