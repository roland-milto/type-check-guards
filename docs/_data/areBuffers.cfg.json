{
	"sourceLanguage": "eng",
	"referenceFileHash": "9e79399efd9364b7be5140627dee8679eff63d23fb548193896543fdd353cb78",
	"aiPromptHash": "9f5ec9e4ef2b88305876eee7c9d77766b651784d11b77ecf8ef81bd005620fa5",
	"translatedTo": {
		"eng": "`areBuffers` checks whether the provided value is a non-empty, filled array where every element is a `Buffer`, returning `true` if so and `false` otherwise.",
		"ara": "تتحقق `areBuffers` مما إذا كانت القيمة المقدمة مصفوفة غير فارغة ومعبأة حيث يكون كل عنصر فيها `Buffer`، وتُرجع `true` إذا كان الأمر كذلك و`false` خلاف ذلك.",
		"afr": "`areBuffers` kontroleer of die verskafde waarde 'n nie-leë, gevulde skikking is waar elke element 'n `Buffer` is, en gee `true` terug indien wel en `false` andersins.",
		"arz": "`areBuffers` بتتأكد إذا كانت القيمة اللي اتقدمت أراي مش فاضية ومليانة وكل عنصر فيها `Buffer`، وبترجع `true` لو كده و`false` غير كده.",
		"bel": "`areBuffers` правярае, ці з'яўляецца пададзенае значэнне непустым, запоўненым масівам, дзе кожны элемент — гэта `Buffer`, вяртаючы `true`, калі так, і `false` у адваротным выпадку.",
		"ben": "`areBuffers` যাচাই করে প্রদত্ত মানটি একটি নন-এম্পটি, পূর্ণ অ্যারে কি না যেখানে প্রতিটি উপাদান একটি `Buffer`; হলে `true` এবং না হলে `false` রিটার্ন করে।",
		"bho": "`areBuffers` जाँचेला कि दिहल मान गैर-खाली, भरल एरे बा कि ना जहाँ हर तत्व `Buffer` बा, अइसन होखे पर `true` आ ना होखे पर `false` लौटावेला।",
		"bos": "`areBuffers` provjerava da li je navedena vrijednost neprazan, popunjen niz u kojem je svaki element `Buffer`, vraćajući `true` ako jeste i `false` u suprotnom.",
		"bul": "`areBuffers` проверява дали предоставената стойност е непразен, запълнен масив, в който всеки елемент е `Buffer`, като връща `true`, ако е така, и `false` в противен случай.",
		"cat": "`areBuffers` comprova si el valor proporcionat és una matriu no buida i emplenada on cada element és un `Buffer`, retornant `true` si és així i `false` en cas contrari.",
		"cjy": "`areBuffers` 检查提供个值是不是一个非空、填满个数组，并且每个元素都是 `Buffer`；符合就返回 `true`，否则返回 `false`。",
		"ces": "`areBuffers` kontroluje, zda je poskytnutá hodnota neprázdné, vyplněné pole, kde je každý prvek `Buffer`, a pokud ano, vrací `true`, jinak `false`.",
		"cmn": "`areBuffers` 用于检查所提供的值是否为非空、已填充的数组，且其中每个元素都是 `Buffer`；若是则返回 `true`，否则返回 `false`。",
		"ckb": "`areBuffers` دەپشکنێت ئایا بەهای پێشکەشکراو ئارایەیەکی نەنەخاڵی و پڕە کە هەر توخمێکی `Buffer` ـە، ئەگەر وابێت `true` دەگەڕێنێتەوە و ئەگەر نا `false`.",
		"cym": "Mae `areBuffers` yn gwirio a yw’r gwerth a ddarperir yn arae nad yw’n wag ac sydd wedi’i llenwi lle mae pob elfen yn `Buffer`, gan ddychwelyd `true` os felly a `false` fel arall.",
		"dan": "`areBuffers` kontrollerer, om den angivne værdi er et ikke-tomt, udfyldt array, hvor hvert element er en `Buffer`, og returnerer `true` i så fald og ellers `false`.",
		"deu": "`areBuffers` prüft, ob der bereitgestellte Wert ein nicht-leeres, vollständig gefülltes Array ist, in dem jedes Element ein `Buffer` ist, und gibt in diesem Fall `true` und andernfalls `false` zurück.",
		"est": "`areBuffers` kontrollib, kas antud väärtus on mitte-tühi, täidetud massiiv, mille iga element on `Buffer`, tagastades sel juhul `true` ja muul juhul `false`.",
		"ell": "Το `areBuffers` ελέγχει αν η παρεχόμενη τιμή είναι ένας μη κενός, πλήρως συμπληρωμένος πίνακας όπου κάθε στοιχείο είναι ένα `Buffer`, επιστρέφοντας `true` αν ισχύει και `false` διαφορετικά.",
		"fas": "`areBuffers` بررسی می‌کند آیا مقدارِ ارائه‌شده یک آرایهٔ غیرخالی و پُرشده است که در آن هر عنصر یک `Buffer` باشد؛ در این صورت `true` و در غیر این صورت `false` برمی‌گرداند.",
		"fra": "`areBuffers` vérifie si la valeur fournie est un tableau non vide et rempli dont chaque élément est un `Buffer`, en renvoyant `true` si c’est le cas et `false` sinon.",
		"fin": "`areBuffers` tarkistaa, onko annettu arvo ei-tyhjä, täytetty taulukko, jossa jokainen alkio on `Buffer`, ja palauttaa tällöin `true` ja muussa tapauksessa `false`.",
		"gsw": "`areBuffers` prüeft, öb dr übergeh Wert es nöd-leers, gfüllts Array isch, wo jedes Element es `Buffer` isch; git i dem Fall `true` zrugg und susch `false`.",
		"gle": "Seiceálann `areBuffers` an bhfuil an luach a chuirtear ar fáil ina eagar neamhfholamh, líonta ina bhfuil gach eilimint ina `Buffer`, ag filleadh `true` más amhlaidh agus `false` murach sin.",
		"hak": "`areBuffers` 檢查提供个值係毋係一隻非空、填滿个陣列，而且內底逐隻元素都係 `Buffer`；係个話回傳 `true`，毋係就回傳 `false`。",
		"heb": "`areBuffers` בודקת האם הערך שסופק הוא מערך לא ריק ומלא שבו כל רכיב הוא `Buffer`, ומחזירה `true` אם כן ו-`false` אחרת.",
		"hin": "`areBuffers` जाँचता है कि दिया गया मान एक गैर-खाली, भरी हुई array है जिसमें हर तत्व `Buffer` है; ऐसा होने पर `true` और अन्यथा `false` लौटाता है।",
		"hrv": "`areBuffers` provjerava je li zadana vrijednost neprazan, popunjen niz u kojem je svaki element `Buffer`, vraćajući `true` ako jest, a `false` u suprotnom.",
		"hsn": "`areBuffers` जाँचेला कि दिहल मान गैर-खाली, भरल ऐरे बा कि ना जहाँ हर तत्व `Buffer` बा, अइसन होखे पर `true` आ ना त `false` लौटावेला।",
		"ind": "`areBuffers` memeriksa apakah nilai yang diberikan adalah array yang tidak kosong dan terisi, di mana setiap elemennya adalah `Buffer`, mengembalikan `true` jika demikian dan `false` jika tidak.",
		"hun": "Az `areBuffers` ellenőrzi, hogy a megadott érték egy nem üres, kitöltött tömb-e, amelyben minden elem `Buffer`, és ha igen, `true`-t ad vissza, egyébként pedig `false`-t.",
		"ita": "`areBuffers` verifica se il valore fornito è un array non vuoto e popolato in cui ogni elemento è un `Buffer`, restituendo `true` in tal caso e `false` altrimenti.",
		"jav": "`areBuffers` mriksa apa nilai sing diwenehake iku array sing ora kosong lan kebak, ing ngendi saben unsur iku `Buffer`, mbalekake `true` yen ya lan `false` yen ora.",
		"isl": "`areBuffers` athugar hvort gefið gildi sé ótómt, fyllt fylki þar sem hvert stak er `Buffer`, og skilar `true` ef svo er og annars `false`.",
		"kor": "`areBuffers`는 제공된 값이 비어 있지 않고 요소가 채워진 배열이며 모든 요소가 `Buffer`인지 확인하고, 그렇다면 `true`를 반환하고 그렇지 않으면 `false`를 반환합니다.",
		"jpn": "`areBuffers` は、提供された値が空でない要素が埋まった配列であり、各要素がすべて `Buffer` であるかを確認し、該当する場合は `true`、それ以外は `false` を返します。",
		"kat": "`areBuffers` ამოწმებს, არის თუ არა მიწოდებული მნიშვნელობა არაცარიელი, შევსებული მასივი, სადაც ყოველი ელემენტი არის `Buffer`, და თუ ასეა აბრუნებს `true`-ს, წინააღმდეგ შემთხვევაში — `false`-ს.",
		"lav": "`areBuffers` pārbauda, vai sniegtā vērtība ir ne-tukšs, aizpildīts masīvs, kurā katrs elements ir `Buffer`, atgriežot `true`, ja tā ir, un `false` pretējā gadījumā.",
		"lit": "`areBuffers` patikrina, ar pateikta reikšmė yra ne tuščias, užpildytas masyvas, kuriame kiekvienas elementas yra `Buffer`, ir tokiu atveju grąžina `true`, o kitu atveju – `false`.",
		"kur": "`areBuffers` دەسەلمێنێت کە ئایا بەهای پێشکەشکراو ئارایەیەکی بەتاڵ نەبوو و پڕە کە هەر توخمێکی `Buffer` ـە، ئەگەر وابێت `true` دەگەڕێنێتەوە و ئەگەر نا `false`.",
		"mar": "`areBuffers` दिलेले मूल्य हे रिकामा नसलेला, भरलेला अ‍ॅरे आहे का आणि त्यातील प्रत्येक घटक `Buffer` आहे का हे तपासते; तसे असल्यास `true` आणि अन्यथा `false` परत करते.",
		"mkd": "`areBuffers` проверува дали дадената вредност е непразна, пополнета низа каде што секој елемент е `Buffer`, враќајќи `true` ако е така и `false` во спротивно.",
		"mya": "`areBuffers` သည် ပေးထားသောတန်ဖိုးသည် အလွတ်မဟုတ်ပြီး အချက်အလက်ဖြည့်ထားသော array ဖြစ်ကာ အစိတ်အပိုင်းတိုင်းသည် `Buffer` ဖြစ်နေသလားကို စစ်ဆေးပြီး၊ ဟုတ်ပါက `true` မဟုတ်ပါက `false` ကို ပြန်ပေးသည်။",
		"nan": "`areBuffers` 會檢查提供的值敢是一个無空、內底有填滿的陣列，而且逐个元素攏是一个 `Buffer`；若是就回傳 `true`，無就是回傳 `false`。",
		"nep": "`areBuffers` ले प्रदान गरिएको मान खाली नभएको, भरिएको एरे हो कि होइन र त्यसका प्रत्येक तत्व `Buffer` हो कि होइन भनेर जाँच गर्छ; त्यस्तो भए `true` र नभए `false` फर्काउँछ।",
		"nld": "`areBuffers` controleert of de opgegeven waarde een niet-lege, gevulde array is waarbij elk element een `Buffer` is, en retourneert in dat geval `true` en anders `false`.",
		"nno": "`areBuffers` sjekkar om den oppgjevne verdien er eit ikkje-tomt, utfylt array der kvart element er ein `Buffer`, og returnerer `true` i så fall og `false` elles.",
		"nor": "`areBuffers` sjekker om den oppgitte verdien er et ikke-tomt, utfylt array der hvert element er en `Buffer`, og returnerer `true` hvis det er tilfelle og `false` ellers.",
		"nob": "`areBuffers` sjekker om den oppgitte verdien er et ikke-tomt, utfylt array der hvert element er en `Buffer`, og returnerer `true` hvis det stemmer og `false` ellers.",
		"orm": "`areBuffers` gatiin kenname tarree duwwaa hin taane fi guutamee jiru ta'ee, elementiin hundi `Buffer` ta'uu isaa ni sakatta'a; yoo akkas ta'e `true` deebisa, yoo hin ta'in immoo `false` deebisa.",
		"pcm": "`areBuffers` dey check whether di value wey you provide na non-empty, filled array wey every element inside am na `Buffer`, e go return `true` if na so and `false` if no be so.",
		"pan": "`areBuffers` ਜਾਂਚਦਾ ਹੈ ਕਿ ਦਿੱਤਾ ਗਿਆ ਮੁੱਲ ਇੱਕ ਨਾ-ਖਾਲੀ, ਭਰਿਆ ਹੋਇਆ ਐਰੇ ਹੈ ਜਿਸ ਵਿੱਚ ਹਰ ਤੱਤ `Buffer` ਹੈ; ਐਸਾ ਹੋਣ 'ਤੇ `true` ਅਤੇ ਨਹੀਂ ਤਾਂ `false` ਵਾਪਸ ਕਰਦਾ ਹੈ।",
		"pes": "`areBuffers` بررسی می‌کند آیا مقدارِ ارائه‌شده یک آرایهٔ غیرخالی و پُر است که در آن هر عنصر یک `Buffer` باشد؛ در این صورت `true` و در غیر این صورت `false` برمی‌گرداند.",
		"pol": "`areBuffers` sprawdza, czy podana wartość jest niepustą, wypełnioną tablicą, w której każdy element jest `Buffer`, zwracając `true` w takim przypadku i `false` w przeciwnym razie.",
		"por": "`areBuffers` verifica se o valor fornecido é um array não vazio e preenchido em que cada elemento é um `Buffer`, retornando `true` nesse caso e `false` caso contrário.",
		"ron": "`areBuffers` verifică dacă valoarea furnizată este un tablou ne-gol și completat în care fiecare element este un `Buffer`, returnând `true` în acest caz și `false` în caz contrar.",
		"pus": "`areBuffers` ګوري چې ورکړل شوی ارزښت يو غير-تش، ډک اراې دی چې هر عنصر يې `Buffer` وي؛ که داسې وي `true` او که نه `false` بېرته ورکوي.",
		"rus": "`areBuffers` проверяет, является ли предоставленное значение непустым, заполненным массивом, где каждый элемент — это `Buffer`, возвращая `true` в этом случае и `false` в противном.",
		"spa": "`areBuffers` comprueba si el valor proporcionado es un arreglo no vacío y relleno en el que cada elemento es un `Buffer`, devolviendo `true` si es así y `false` en caso contrario.",
		"slk": "`areBuffers` kontroluje, či je poskytnutá hodnota neprázdne, vyplnené pole, v ktorom je každý prvok `Buffer`; ak áno, vráti `true`, inak `false`.",
		"slv": "`areBuffers` preveri, ali je podana vrednost neprazno, zapolnjeno polje, kjer je vsak element `Buffer`, ter vrne `true`, če je tako, sicer pa `false`.",
		"sqi": "`areBuffers` kontrollon nëse vlera e dhënë është një varg jo-bosh dhe i mbushur ku çdo element është një `Buffer`, duke kthyer `true` nëse po dhe `false` përndryshe.",
		"srp": "`areBuffers` проверава да ли је дата вредност непразан, попуњен низ у ком је сваки елемент `Buffer`, враћајући `true` ако јесте и `false` у супротном.",
		"swe": "`areBuffers` kontrollerar om det angivna värdet är en icke-tom, ifylld array där varje element är en `Buffer`, och returnerar `true` i så fall och `false` annars.",
		"swa": "`areBuffers` hukagua kama thamani iliyotolewa ni safu isiyo tupu na iliyojaa ambapo kila kipengele ni `Buffer`, ikirejesha `true` ikiwa ndivyo na `false` vinginevyo.",
		"tgl": "Sinusuri ng `areBuffers` kung ang ibinigay na value ay isang hindi bakante, napunong array kung saan bawat elemento ay isang `Buffer`, at nagbabalik ng `true` kung ganoon at `false` kung hindi.",
		"tam": "`areBuffers` என்பது வழங்கப்பட்ட மதிப்பு காலியானதல்ல, கூறுகள் நிரப்பப்பட்ட வரிசையாகவும், அதில் உள்ள ஒவ்வொரு கூறும் `Buffer` ஆகவும் உள்ளதா என்பதைச் சரிபார்க்கிறது; அப்படியானால் `true`, இல்லையெனில் `false` திருப்பும்.",
		"tel": "`areBuffers` అందించిన విలువ ఖాళీ కాని, నింపబడిన అర్రేనా మరియు అందులోని ప్రతి అంశం `Buffer`నా అని తనిఖీ చేస్తుంది; అలా అయితే `true`, లేకపోతే `false` ని తిరిగి ఇస్తుంది.",
		"tha": "`areBuffers` ตรวจสอบว่าค่าที่ให้มาเป็นอาร์เรย์ที่ไม่ว่างและมีสมาชิกครบถ้วน โดยทุกองค์ประกอบเป็น `Buffer` หรือไม่ หากใช่จะคืนค่า `true` มิฉะนั้นคืนค่า `false`",
		"tur": "`areBuffers`, sağlanan değerin her öğesi bir `Buffer` olan boş olmayan, doldurulmuş bir dizi olup olmadığını kontrol eder; öyleyse `true`, değilse `false` döndürür.",
		"uig": "`areBuffers` تەمىنلەنگەن قىممەتنىڭ بوش بولمىغان، تولدۇرۇلغان تىزىملىك بولۇپ، ئۇنىڭدىكى ھەر بىر ئېلېمېنت `Buffer` ئىكەنلىكىنى تەكشۈرىدۇ؛ شۇنداق بولسا `true` قايتۇرىدۇ، بولمىسا `false` قايتۇرىدۇ.",
		"urd": "`areBuffers` چیک کرتا ہے کہ آیا فراہم کردہ value ایک غیر خالی، بھری ہوئی array ہے جس میں ہر عنصر ایک `Buffer` ہے؛ اگر ایسا ہو تو `true` اور بصورتِ دیگر `false` واپس کرتا ہے۔",
		"ukr": "`areBuffers` перевіряє, чи є надане значення непорожнім, заповненим масивом, у якому кожен елемент є `Buffer`, повертаючи `true` у такому разі та `false` інакше.",
		"vie": "`areBuffers` kiểm tra liệu giá trị được cung cấp có phải là một mảng không rỗng, đã được điền, trong đó mọi phần tử đều là một `Buffer` hay không; nếu đúng thì trả về `true`, nếu không thì trả về `false`.",
		"uzb": "`areBuffers` berilgan qiymat bo‘sh bo‘lmagan, to‘ldirilgan massiv ekanini va undagi har bir element `Buffer` ekanini tekshiradi; shunday bo‘lsa `true`, aks holda `false` qaytaradi.",
		"wuu": "`areBuffers` 检查提供个值是不是一个非空、填满个数组，而且里向每个元素侪是 `Buffer`；是个么返回 `true`，勿是个么返回 `false`。",
		"zho": "`areBuffers` 用于检查所提供的值是否为一个非空、已填充的数组，且其中每个元素都是 `Buffer`；若是则返回 `true`，否则返回 `false`。",
		"yue": "`areBuffers` 會檢查提供嘅值係咪一個非空、已填滿嘅陣列，而且每個元素都係 `Buffer`；符合就回傳 `true`，否則回傳 `false`。",
		"zha": "`areBuffers` cingjlaengh gwnz ndaej cungj youq raemx mbouj miz (non-empty), miz raemx gyaeuj, raemx raemx mizcinj laengh `Buffer`; daengzqij `true` gyaeuj gwnzdaengz, daengzqij `false` gyaeuj mbouj gwnzdaengz.",
		"zsm": "`areBuffers` menyemak sama ada nilai yang diberikan ialah tatasusunan yang tidak kosong dan terisi di mana setiap elemen ialah `Buffer`, mengembalikan `true` jika ya dan `false` jika tidak."
	},
	"subject": []
}