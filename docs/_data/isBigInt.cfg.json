{
	"sourceLanguage": "eng",
	"referenceFileHash": "b7e01c764605b920343808af2cac2565c003bc395b80851314b0bbe987ed1992",
	"aiPromptHash": "9f5ec9e4ef2b88305876eee7c9d77766b651784d11b77ecf8ef81bd005620fa5",
	"translatedTo": {
		"eng": "`isBigInt` checks whether a given value is of type `bigint`, returning `true` for BigInt primitives and `false` otherwise.",
		"ara": "تتحقق `isBigInt` مما إذا كانت قيمة معيّنة من النوع `bigint`، وتُرجع `true` لقيم BigInt البدائية و`false` خلاف ذلك.",
		"arz": "`isBigInt` بيشيّك إذا كانت قيمة معيّنة من نوع `bigint`، وبيُرجّع `true` للـ BigInt primitives و`false` في غير كده.",
		"afr": "`isBigInt` kontroleer of 'n gegewe waarde van tipe `bigint` is, en gee `true` terug vir BigInt-primitiewe en andersins `false`.",
		"ben": "`isBigInt` পরীক্ষা করে কোনো প্রদত্ত মান `bigint` টাইপের কি না; BigInt প্রিমিটিভের জন্য `true` এবং অন্যথায় `false` রিটার্ন করে।",
		"bel": "`isBigInt` правярае, ці мае зададзенае значэнне тып `bigint`, вяртаючы `true` для прымітываў BigInt і `false` у іншых выпадках.",
		"bho": "`isBigInt` जाँचेला कि दिहल गइल वैल्यू के टाइप `bigint` बा कि ना, BigInt प्रिमिटिव खातिर `true` आ नाहीं त `false` लौटावेला।",
		"bos": "`isBigInt` provjerava da li je data vrijednost tipa `bigint`, vraćajući `true` za BigInt primitive, a `false` u suprotnom.",
		"cat": "`isBigInt` comprova si un valor determinat és del tipus `bigint`, retornant `true` per a primitives BigInt i `false` en cas contrari.",
		"bul": "`isBigInt` проверява дали дадена стойност е от тип `bigint`, като връща `true` за BigInt примитиви и `false` в противен случай.",
		"ces": "`isBigInt` kontroluje, zda je daná hodnota typu `bigint`; vrací `true` pro primitivní BigInt a jinak `false`.",
		"cjy": "`isBigInt` 检查给定个值是不是 `bigint` 类型；对 BigInt 原始值返回 `true`，否则返回 `false`。",
		"cmn": "`isBigInt` 用于检查给定值是否为 `bigint` 类型：对 BigInt 原始值返回 `true`，否则返回 `false`。",
		"ckb": "`isBigInt` پشکنین دەکات کە ئایا بەهایەکی دیاریکراو لە جۆری `bigint` ـە، بۆ BigInt ـی بنەڕەتی `true` دەگەڕێنێتەوە و بۆ ئەوانەی تر `false`.",
		"dan": "`isBigInt` kontrollerer, om en given værdi er af typen `bigint`, og returnerer `true` for BigInt-primitiver og ellers `false`.",
		"deu": "`isBigInt` prüft, ob ein gegebener Wert vom Typ `bigint` ist, und gibt für BigInt-Primitiven `true` und andernfalls `false` zurück.",
		"cym": "Mae `isBigInt` yn gwirio a yw gwerth penodol o fath `bigint`, gan ddychwelyd `true` ar gyfer cyntefigion BigInt a `false` fel arall.",
		"est": "`isBigInt` kontrollib, kas antud väärtus on tüüpi `bigint`, tagastades BigInt primitiivide korral `true` ja muul juhul `false`.",
		"ell": "Το `isBigInt` ελέγχει αν μια δεδομένη τιμή είναι τύπου `bigint`, επιστρέφοντας `true` για πρωτογενείς τιμές BigInt και `false` διαφορετικά.",
		"fas": "`isBigInt` بررسی می‌کند آیا یک مقدار داده‌شده از نوع `bigint` است یا نه؛ برای مقادیر اولیهٔ BigInt مقدار `true` و در غیر این صورت `false` برمی‌گرداند.",
		"fra": "`isBigInt` vérifie si une valeur donnée est de type `bigint`, en renvoyant `true` pour les primitives BigInt et `false` sinon.",
		"fin": "`isBigInt` tarkistaa, onko annettu arvo tyyppiä `bigint`, palauttaen `true` BigInt-primitiiveille ja `false` muussa tapauksessa.",
		"gsw": "`isBigInt` prüeft, öb e gegebene Wärt vom Typ `bigint` isch, und git `true` zrugg für BigInt-Primitive und susch `false`.",
		"hak": "`isBigInt` 會檢查指定个值係毋係 `bigint` 型別；BigInt 原始值會回傳 `true`，其他情況回傳 `false`。",
		"gle": "Seiceálann `isBigInt` an bhfuil luach ar leith den chineál `bigint`, ag filleadh `true` do phríomhluachanna BigInt agus `false` murach sin.",
		"hrv": "`isBigInt` provjerava je li zadana vrijednost tipa `bigint`, vraćajući `true` za BigInt primitive, a `false` u suprotnom.",
		"hin": "`isBigInt` जाँचता है कि दिया गया मान `bigint` प्रकार का है या नहीं; BigInt प्रिमिटिव्स के लिए `true` और अन्यथा `false` लौटाता है।",
		"heb": "`isBigInt` בודקת האם ערך נתון הוא מטיפוס `bigint`, ומחזירה `true` עבור פרימיטיבים מסוג BigInt ו-`false` אחרת.",
		"ind": "`isBigInt` memeriksa apakah suatu nilai tertentu bertipe `bigint`, mengembalikan `true` untuk primitif BigInt dan `false` jika tidak.",
		"hsn": "`isBigInt` जाँचेला कि दिहल गइल वैल्यू के टाइप `bigint` बा कि ना, BigInt प्रिमिटिव खातिर `true` आ बाकी सब खातिर `false` लौटावेला।",
		"hun": "Az `isBigInt` ellenőrzi, hogy egy adott érték `bigint` típusú-e; BigInt primitívek esetén `true`-t, egyébként `false`-t ad vissza.",
		"ita": "`isBigInt` verifica se un determinato valore è di tipo `bigint`, restituendo `true` per i primitivi BigInt e `false` altrimenti.",
		"jav": "`isBigInt` mriksa apa sawijining nilai nduweni jinis `bigint`, mbalekake `true` kanggo primitif BigInt lan `false` kanggo liyane.",
		"isl": "`isBigInt` athugar hvort tiltekið gildi sé af gerðinni `bigint` og skilar `true` fyrir BigInt-frumgildi en `false` annars.",
		"jpn": "`isBigInt` は、与えられた値が `bigint` 型かどうかをチェックし、BigInt プリミティブであれば `true`、それ以外は `false` を返します。",
		"kor": "`isBigInt`는 주어진 값이 `bigint` 타입인지 확인하며, BigInt 기본값에는 `true`를, 그 외에는 `false`를 반환합니다.",
		"kur": "`isBigInt` kontrol dike ka nirxek dayîn ji cureya `bigint` e an na, ji bo BigInt primitive `true` vedigerîne û wekî din `false`.",
		"lav": "`isBigInt` pārbauda, vai dotā vērtība ir tipa `bigint`, atgriežot `true` BigInt primitīvajām vērtībām un `false` citos gadījumos.",
		"kat": "`isBigInt` ამოწმებს, არის თუ არა მოცემული მნიშვნელობა `bigint` ტიპის, აბრუნებს `true`-ს BigInt პრიმიტივებისთვის და სხვა შემთხვევაში `false`-ს.",
		"lit": "`isBigInt` patikrina, ar pateikta reikšmė yra `bigint` tipo, grąžindama `true` BigInt primityvams ir `false` kitu atveju.",
		"mkd": "`isBigInt` проверува дали дадена вредност е од тип `bigint`, враќајќи `true` за BigInt примитиви и `false` во спротивно.",
		"mar": "`isBigInt` दिलेले मूल्य `bigint` प्रकाराचे आहे का ते तपासते; BigInt प्रिमिटिव्हसाठी `true` आणि अन्यथा `false` परत करते.",
		"nan": "`isBigInt` 會檢查指定个值敢是 `bigint` 型別；若是 BigInt 原始值就回傳 `true`，無就是 `false`。",
		"nep": "`isBigInt` ले दिइएको मान `bigint` प्रकारको हो कि होइन भनेर जाँच गर्छ; BigInt प्रिमिटिभहरूका लागि `true` र अन्यथा `false` फर्काउँछ।",
		"mya": "`isBigInt` သည် ပေးထားသော တန်ဖိုးသည် `bigint` အမျိုးအစားဖြစ်/မဖြစ်ကို စစ်ဆေးပြီး BigInt ပရိမစ်များအတွက် `true` ကို ပြန်ပေးကာ အခြားအရာများအတွက် `false` ကို ပြန်ပေးသည်။",
		"nld": "`isBigInt` controleert of een gegeven waarde van het type `bigint` is, en geeft `true` terug voor BigInt-primitieven en anders `false`.",
		"nob": "`isBigInt` sjekker om en gitt verdi er av typen `bigint`, og returnerer `true` for BigInt-primitiver og `false` ellers.",
		"nno": "`isBigInt` sjekkar om ein gitt verdi er av typen `bigint`, og returnerer `true` for BigInt-primitive verdiar og `false` elles.",
		"nor": "`isBigInt` sjekker om en gitt verdi er av typen `bigint`, og returnerer `true` for BigInt-primitiver og `false` ellers.",
		"pcm": "`isBigInt` dey check whether di value wey you give am na type `bigint`, e go return `true` for BigInt primitives and `false` if no be so.",
		"pan": "`isBigInt` ਜਾਂਚਦਾ ਹੈ ਕਿ ਦਿੱਤਾ ਗਿਆ ਮੁੱਲ `bigint` ਟਾਈਪ ਦਾ ਹੈ ਜਾਂ ਨਹੀਂ; BigInt ਪ੍ਰਿਮਿਟਿਵ ਲਈ `true` ਅਤੇ ਹੋਰਥਾਂ `false` ਵਾਪਸ ਕਰਦਾ ਹੈ।",
		"orm": "`isBigInt` gatiin kenname gosa `bigint` ta'uu isaa ni mirkaneessa; BigInt primitives irratti `true` deebisa, kan biraa irratti immoo `false` deebisa.",
		"pes": "`isBigInt` بررسی می‌کند آیا یک مقدارِ داده‌شده از نوع `bigint` است یا نه؛ برای مقادیر اولیهٔ BigInt مقدار `true` و در غیر این صورت `false` برمی‌گرداند.",
		"pol": "`isBigInt` sprawdza, czy podana wartość jest typu `bigint`, zwracając `true` dla prymitywów BigInt i `false` w przeciwnym razie.",
		"por": "`isBigInt` verifica se um determinado valor é do tipo `bigint`, retornando `true` para primitivos BigInt e `false` caso contrário.",
		"ron": "`isBigInt` verifică dacă o valoare dată este de tip `bigint`, returnând `true` pentru primitivele BigInt și `false` în caz contrar.",
		"rus": "`isBigInt` проверяет, является ли заданное значение типом `bigint`, возвращая `true` для примитивов BigInt и `false` в противном случае.",
		"pus": "`isBigInt` ګوري چې ورکړل شوی ارزښت د `bigint` ډول دی که نه؛ د BigInt ابتدايي ارزښتونو لپاره `true` او په نورو حالاتو کې `false` بېرته ورکوي.",
		"slk": "`isBigInt` kontroluje, či je daná hodnota typu `bigint`, pričom vracia `true` pre primitívy BigInt a `false` v opačnom prípade.",
		"slv": "`isBigInt` preveri, ali je dana vrednost tipa `bigint`, ter vrne `true` za primitivne vrednosti BigInt in sicer `false`.",
		"spa": "`isBigInt` comprueba si un valor dado es de tipo `bigint`, devolviendo `true` para primitivos BigInt y `false` en caso contrario.",
		"sqi": "`isBigInt` kontrollon nëse një vlerë e dhënë është e tipit `bigint`, duke kthyer `true` për primitivët BigInt dhe `false` përndryshe.",
		"srp": "`isBigInt` proverava da li je data vrednost tipa `bigint`, vraćajući `true` za BigInt primitive, a `false` u suprotnom.",
		"swa": "`isBigInt` hukagua kama thamani iliyotolewa ni ya aina `bigint`, ikirudisha `true` kwa BigInt za msingi na `false` vinginevyo.",
		"swe": "`isBigInt` kontrollerar om ett givet värde är av typen `bigint` och returnerar `true` för BigInt-primitiver och `false` annars.",
		"tam": "`isBigInt` கொடுக்கப்பட்ட மதிப்பு `bigint` வகையா என்பதைச் சரிபார்த்து, BigInt அடிப்படை மதிப்புகளுக்கு `true` மற்றும் இல்லையெனில் `false` ஐத் திருப்புகிறது.",
		"tel": "`isBigInt` ఇచ్చిన విలువు `bigint` టైప్‌కు చెందినదో లేదో తనిఖీ చేస్తుంది; BigInt ప్రిమిటివ్‌లకు `true` ను, లేకపోతే `false` ను తిరిగి ఇస్తుంది.",
		"tgl": "Sinusuri ng `isBigInt` kung ang ibinigay na value ay may uri na `bigint`, nagbabalik ng `true` para sa mga primitibong BigInt at `false` kung hindi.",
		"tha": "`isBigInt` ตรวจสอบว่าค่าที่กำหนดเป็นชนิด `bigint` หรือไม่ โดยคืนค่า `true` สำหรับ BigInt แบบ primitive และคืนค่า `false` ในกรณีอื่น",
		"tur": "`isBigInt`, verilen bir değerin `bigint` türünde olup olmadığını kontrol eder; BigInt ilkel değerleri için `true`, aksi halde `false` döndürür.",
		"ukr": "`isBigInt` перевіряє, чи має задане значення тип `bigint`, повертаючи `true` для примітивів BigInt і `false` в інших випадках.",
		"uig": "`isBigInt` بېرىلگەن قىممەتنىڭ تىپى `bigint` ئىكەنلىكىنى تەكشۈرىدۇ؛ BigInt ئەسلى قىممەتلىرى ئۈچۈن `true`، بولمىسا `false` قايتۇرىدۇ.",
		"urd": "`isBigInt` چیک کرتا ہے کہ آیا دی گئی ویلیو کی قسم `bigint` ہے یا نہیں؛ BigInt پرِمِٹِوز کے لیے `true` اور بصورتِ دیگر `false` واپس کرتا ہے۔",
		"uzb": "`isBigInt` berilgan qiymat `bigint` turiga mansubligini tekshiradi: BigInt primitivlari uchun `true`, aks holda `false` qaytaradi.",
		"vie": "`isBigInt` kiểm tra liệu một giá trị đã cho có thuộc kiểu `bigint` hay không, trả về `true` cho các giá trị nguyên thủy BigInt và `false` trong các trường hợp khác.",
		"wuu": "`isBigInt` 用来检查给定个值是不是 `bigint` 类型：对 BigInt 原始值返回 `true`，否则返回 `false`。",
		"zho": "`isBigInt` 用于检查给定值是否为 `bigint` 类型：对 BigInt 原始值返回 `true`，否则返回 `false`。",
		"yue": "`isBigInt` 會檢查指定值係咪 `bigint` 型別；如果係 BigInt 原始值就回傳 `true`，否則回傳 `false`。",
		"zha": "`isBigInt` check roengz gij value aeu of type `bigint`, returning `true` roengz BigInt primitives raeuz `false` otherwise.",
		"zsm": "`isBigInt` menyemak sama ada nilai yang diberikan adalah daripada jenis `bigint`, mengembalikan `true` untuk primitif BigInt dan `false` jika sebaliknya."
	},
	"subject": []
}