{
	"sourceLanguage": "eng",
	"referenceFileHash": "be4e1208c0547b3a9e252350e0def6af55da05cec5c1bb4d2498c9f57afdcfeb",
	"aiPromptHash": "9f5ec9e4ef2b88305876eee7c9d77766b651784d11b77ecf8ef81bd005620fa5",
	"translatedTo": {
		"eng": "Checks whether the given `value` is a `Map`, returning `true` if it is and `false` otherwise.",
		"afr": "Kontroleer of die gegewe `value` 'n `Map` is, en gee `true` terug as dit is en `false` andersins.",
		"ara": "يتحقق مما إذا كانت `value` المعطاة هي `Map`، ويُرجع `true` إذا كانت كذلك و`false` خلاف ذلك.",
		"arz": "بيتحقق إذا كانت `value` المعطاة هي `Map`، وبيُرجّع `true` لو هي كده و`false` لو لأ.",
		"bel": "Правярае, ці з’яўляецца зададзенае `value` аб’ектам `Map`, вяртаючы `true`, калі так, і `false` у адваротным выпадку.",
		"ben": "প্রদত্ত `value` একটি `Map` কি না তা পরীক্ষা করে; `Map` হলে `true` এবং না হলে `false` রিটার্ন করে।",
		"bos": "Provjerava da li je dati `value` `Map`, vraćajući `true` ako jeste i `false` u suprotnom.",
		"bho": "दिहल गइल `value` के `Map` होखे के जाँच करेला; अगर ह त `true` लौटावेला आ ना त `false`।",
		"bul": "Проверява дали дадената `value` е `Map`, като връща `true`, ако е, и `false` в противен случай.",
		"ces": "Kontroluje, zda je zadaná hodnota `value` typu `Map`; vrací `true`, pokud ano, jinak `false`.",
		"cat": "Comprova si el `value` donat és un `Map`, retornant `true` si ho és i `false` en cas contrari.",
		"cjy": "检查给定嘅 `value` 系咪 `Map`；如果系就返回 `true`，否则返回 `false`。",
		"cmn": "检查给定的 `value` 是否为 `Map`；如果是则返回 `true`，否则返回 `false`。",
		"ckb": "دەپشکنێت ئایا `value` ـی دراو `Map` ـە، ئەگەر وابێت `true` دەگەڕێنێتەوە و ئەگەر نا `false`.",
		"cym": "Yn gwirio a yw'r `value` a roddir yn `Map`, gan ddychwelyd `true` os ydyw a `false` fel arall.",
		"deu": "Prüft, ob der angegebene `value` eine `Map` ist, und gibt `true` zurück, wenn dies der Fall ist, andernfalls `false`.",
		"dan": "Kontrollerer, om den givne `value` er en `Map`, og returnerer `true`, hvis den er, og ellers `false`.",
		"est": "Kontrollib, kas antud `value` on `Map`, tagastades `true`, kui on, ja `false` muul juhul.",
		"ell": "Ελέγχει αν το δοθέν `value` είναι `Map`, επιστρέφοντας `true` αν είναι και `false` διαφορετικά.",
		"fas": "بررسی می‌کند آیا `value` داده‌شده یک `Map` است یا نه؛ اگر باشد `true` و در غیر این صورت `false` برمی‌گرداند.",
		"fra": "Vérifie si la `value` donnée est une `Map`, en renvoyant `true` si c’est le cas et `false` sinon.",
		"fin": "Tarkistaa, onko annettu `value` `Map`, palauttaen `true`, jos on, ja `false` muussa tapauksessa.",
		"gle": "Seiceálann sé an `Map` é an `value` a thugtar, ag filleadh `true` más ea agus `false` murach sin.",
		"hak": "检查所给个 `value` 系毋系 `Map`；系就返回 `true`，毋系就返回 `false`。",
		"heb": "בודק האם ה-`value` הנתון הוא `Map`, ומחזיר `true` אם כן ו-`false` אחרת.",
		"hin": "यह जाँचता है कि दिया गया `value` एक `Map` है या नहीं; यदि है तो `true` और अन्यथा `false` लौटाता है।",
		"gsw": "Prüeft, öb s aagee `value` e `Map` isch, und git `true` zrugg, wänn ja, und `false` suscht.",
		"hrv": "Provjerava je li zadani `value` `Map`, vraćajući `true` ako jest i `false` u suprotnom.",
		"hsn": "दिहल गइल `value` के जांचेला कि ऊ `Map` बा कि ना; अगर बा त `true` लौटावेला आ ना त `false`।",
		"ind": "Memeriksa apakah `value` yang diberikan adalah sebuah `Map`, mengembalikan `true` jika ya dan `false` jika tidak.",
		"hun": "Ellenőrzi, hogy a megadott `value` egy `Map`-e; `true`-t ad vissza, ha igen, különben `false`-t.",
		"ita": "Verifica se il `value` fornito è una `Map`, restituendo `true` se lo è e `false` altrimenti.",
		"isl": "Athugar hvort gefið `value` sé `Map` og skilar `true` ef svo er, annars `false`.",
		"jav": "Mriksani apa `value` sing diwenehake iku `Map`, mbalekake `true` yen iya lan `false` yen ora.",
		"jpn": "指定された `value` が `Map` かどうかをチェックし、`Map` であれば `true`、そうでなければ `false` を返します。",
		"kor": "주어진 `value`가 `Map`인지 확인하며, `Map`이면 `true`를 반환하고 그렇지 않으면 `false`를 반환합니다.",
		"kat": "ამოწმებს, არის თუ არა მოცემული `value` ტიპის `Map`, აბრუნებს `true`-ს თუ არის და `false`-ს სხვა შემთხვევაში.",
		"lav": "Pārbauda, vai dotā `value` ir `Map`, atgriežot `true`, ja tā ir, un `false` pretējā gadījumā.",
		"lit": "Patikrina, ar pateikta `value` yra `Map`, grąžindama `true`, jei taip, ir `false` kitu atveju.",
		"kur": "Kontrol dike ka `value`-a dayîn `Map` e an na; heke `Map` be `true` vedigerîne û wekî din `false`.",
		"mkd": "Проверува дали дадената `value` е `Map`, враќајќи `true` ако е и `false` во спротивно.",
		"mar": "दिलेल्या `value` चे `Map` आहे की नाही हे तपासते; असल्यास `true` आणि अन्यथा `false` परत करते.",
		"mya": "ပေးထားသော `value` သည် `Map` ဟုတ်မဟုတ်ကို စစ်ဆေးပြီး ဟုတ်ပါက `true` ကို ပြန်ပေးကာ မဟုတ်ပါက `false` ကို ပြန်ပေးသည်။",
		"nan": "检查所提供的 `value` 是否为 `Map`；若是就回传 `true`，否则回传 `false`。",
		"nep": "दिइएको `value` `Map` हो कि होइन भनेर जाँच गर्छ; यदि हो भने `true` र नभए `false` फर्काउँछ।",
		"nld": "Controleert of de gegeven `value` een `Map` is, en retourneert `true` als dat zo is en anders `false`.",
		"nno": "Kontrollerer om den gjevne `value` er ein `Map`, og returnerer `true` viss han er det og `false` elles.",
		"nob": "Sjekker om den gitte `value` er en `Map`, og returnerer `true` hvis den er det og `false` ellers.",
		"nor": "Sjekker om den gitte `value` er en `Map`, og returnerer `true` hvis den er det og `false` ellers.",
		"orm": "`value` kenname `Map` ta'uu isaa ni mirkaneessa; yoo ta'e `true` deebisa, yoo hin taane `false` deebisa.",
		"pcm": "E check whether di given `value` be `Map`, e go return `true` if na so, and `false` if no be so.",
		"pan": "ਦਿੱਤੀ ਗਈ `value` ਦੇ `Map` ਹੋਣ ਦੀ ਜਾਂਚ ਕਰਦਾ ਹੈ; ਜੇ ਹੈ ਤਾਂ `true` ਅਤੇ ਨਹੀਂ ਤਾਂ `false` ਵਾਪਸ ਕਰਦਾ ਹੈ।",
		"pol": "Sprawdza, czy podana `value` jest `Map`, zwracając `true`, jeśli tak, i `false` w przeciwnym razie.",
		"pes": "بررسی می‌کند آیا `value` داده‌شده یک `Map` است یا نه؛ اگر باشد `true` و در غیر این صورت `false` برمی‌گرداند.",
		"por": "Verifica se o `value` fornecido é um `Map`, retornando `true` se for e `false` caso contrário.",
		"rus": "Проверяет, является ли заданное `value` объектом `Map`, возвращая `true`, если это так, и `false` в противном случае.",
		"ron": "Verifică dacă `value` dat este un `Map`, returnând `true` dacă este și `false` în caz contrar.",
		"pus": "ਇਹ ਜਾਂਚਦਾ ਹੈ ਕਿ ਦਿੱਤਾ ਗਿਆ `value` ਇੱਕ `Map` ਹੈ ਜਾਂ ਨਹੀਂ; ਜੇ ਹੈ ਤਾਂ `true` ਵਾਪਸ ਕਰਦਾ ਹੈ ਨਹੀਂ ਤਾਂ `false`।",
		"slk": "Skontroluje, či je daná hodnota `value` typu `Map`, pričom vráti `true`, ak je, a `false` v opačnom prípade.",
		"slv": "Preveri, ali je podana `value` tipa `Map`, in vrne `true`, če je, sicer pa `false`.",
		"spa": "Comprueba si el `value` proporcionado es un `Map`, devolviendo `true` si lo es y `false` en caso contrario.",
		"sqi": "Kontrollon nëse `value` e dhënë është një `Map`, duke kthyer `true` nëse është dhe `false` përndryshe.",
		"srp": "Проверава да ли је дати `value` `Map`, враћајући `true` ако јесте и `false` у супротном.",
		"swe": "Kontrollerar om det angivna `value` är en `Map` och returnerar `true` om det är det, annars `false`.",
		"swa": "Hukagua kama `value` iliyotolewa ni `Map`, ikirudisha `true` ikiwa ni hivyo na `false` vinginevyo.",
		"tam": "கொடுக்கப்பட்ட `value` ஒரு `Map` ஆக உள்ளதா என்பதைச் சரிபார்த்து, அது `Map` ஆக இருந்தால் `true` என்றும் இல்லையெனில் `false` என்றும் திருப்புகிறது.",
		"tha": "ตรวจสอบว่า `value` ที่กำหนดเป็น `Map` หรือไม่ โดยคืนค่า `true` หากเป็น และ `false` หากไม่ใช่",
		"tgl": "Sinusuri kung ang ibinigay na `value` ay isang `Map`, at ibinabalik ang `true` kung ito ay `Map` at `false` kung hindi.",
		"tel": "ఇచ్చిన `value` ఒక `Map` కాదా అని తనిఖీ చేస్తుంది; అది అయితే `true`, లేకపోతే `false` ను తిరిగి ఇస్తుంది.",
		"ukr": "Перевіряє, чи є задане `value` об’єктом `Map`, повертаючи `true`, якщо так, і `false` в іншому разі.",
		"tur": "Verilen `value` değerinin bir `Map` olup olmadığını kontrol eder; öyleyse `true`, değilse `false` döndürür.",
		"uig": "بېرىلگەن `value` نىڭ `Map` ئىكەنلىكىنى تەكشۈرىدۇ؛ ئەگەر شۇنداق بولسا `true` قايتۇرىدۇ، بولمىسا `false` قايتۇرىدۇ.",
		"uzb": "Berilgan `value` `Map` ekanligini tekshiradi: agar shunday bo‘lsa `true`, aks holda `false` qaytaradi.",
		"urd": "یہ چیک کرتا ہے کہ دیا گیا `value` ایک `Map` ہے یا نہیں؛ اگر ہے تو `true` اور بصورتِ دیگر `false` واپس کرتا ہے۔",
		"vie": "Kiểm tra xem `value` đã cho có phải là một `Map` hay không, trả về `true` nếu đúng và `false` nếu không.",
		"wuu": "检查给定嘅 `value` 是不是 `Map`；如果是就返回 `true`，否则返回 `false`。",
		"yue": "檢查指定嘅 `value` 係咪 `Map`；如果係就回傳 `true`，否則回傳 `false`。",
		"zho": "检查给定的 `value` 是否为 `Map`；如果是则返回 `true`，否则返回 `false`。",
		"zsm": "Memeriksa sama ada `value` yang diberikan ialah `Map`, mengembalikan `true` jika ya dan `false` jika tidak.",
		"zha": "Caeuq haenq gwnz `value` miz `Map` baeuj, baeuj laengz `true` neix gwnz `Map`, mbouj neix laengz `false`."
	},
	"subject": []
}