# isMap

## Опис

Проверува дали дадената `value` е `Map`, враќајќи `true` ако е и `false` во спротивно.

### Случај на употреба

Користете `isMap` кога добивате `unknown` вредност (на пр., од парсирање JSON, надворешни API-и или кориснички внес) и
треба да се осигурате дека е `Map` пред да извршувате `Map` операции.

> **Белешка за корисници на TypeScript:**
>
> `isMap` е заштитник во време на извршување што враќа `true` кога вредноста е `Map` и `false` во спротивно; користете
> го за да го стесните `unknown` пред да повикувате `Map` API-и.

### Предности

- Обезбедува брза проверка во време на извршување дали некоја вредност е `Map`.
- Помага да се спречат грешки со типови со тоа што ги штити гранките на кодот што бараат `Map` методи како `get`, `set`
  и `has`.
- Функционира добро како лесен чекор за валидација при работа со `unknown` влезови.

## Употреба

### Синтакса

Функција:

- `isMap(value)`

Параметри:

- `value`: Вредноста што треба да се провери.

### Локален импорт на функција

```ts
import { isMap } from "@type-check/guards";

const input: unknown = new Map<string, number>([["a", 1]]);

if (isMap(input)) {
  input.set("b", 2);
  const a = input.get("a");
  console.log(a);
} else {
  console.log("Not a Map");
}

```

### Глобален импорт на објект

За импорт на функциите како глобални методи на објект користете:

```ts
import "@type-check/guards/register-global-object.mjs";
```

Следниот метод потоа ќе биде достапен глобално:

- `Type.isMap(value)`

## Анализа на функции

Тука е документирана табеларна анализа на излезот што се добива при внесување различни параметри во
функциите: [isMap](../_analysis/isMap.md)

<br>

---

<small>Датотеката беше генерирана на 31 January 2026 at 16:28:53 (UTC) со користење на *
*[markdown-documentation-generator](https://github.com/roland-milto/markdown-documentation-generator)** од *
*[Roland Milto](https://roland-milto.de/)**.</small>