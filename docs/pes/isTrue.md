# isTrue

## توضیحات

`isTrue` تعیین می‌کند آیا یک مقدار داده‌شده به‌طور سخت‌گیرانه با `true` برابر است یا نه.

### مورد استفاده

از `isTrue` برای اعتبارسنجی فلگ‌ها، فیچر-تاگل‌ها یا مقادیر پیکربندی استفاده کنید؛ جاهایی که فقط مقدار ثابتِ `true` باید
پذیرفته شود و هر چیز دیگری باید `false` در نظر گرفته شود.

> **نکته برای کاربران TypeScript:**
>
> وقتی لازم دارید فقط مقدار بولیِ ثابتِ `true` پذیرفته شود (نه صرفاً مقادیر truthy)، از `isTrue` استفاده کنید.

### مزایا

- یک بررسی سخت‌گیرانه برای مقدار بولیِ ثابتِ `true` فراهم می‌کند (بدون تبدیل ضمنی).
- کمک می‌کند `true` را از مقادیر truthy مثل `1`، `"true"` یا `{}` متمایز کنید.
- رفتاری ساده و قابل‌پیش‌بینی دارد که برای گاردها و خط لوله‌های اعتبارسنجی مناسب است.

## نحوه استفاده

### نحو

تابع:

- `isTrue(value)`

پارامترها:

- `value`: مقداری که باید بررسی شود.

### وارد کردن محلی تابع

```ts
import { isTrue } from "@type-check/guards";

const a = isTrue(true);      //‎ درست
const b = isTrue(1);         //‎ نادرست
const c = isTrue("true");   //‎ نادرست

if (isTrue(a)) {
  //‎‎ اینجا a درست است
}
```

### وارد کردن سراسری شیء

برای وارد کردن توابع به‌عنوان متدهای سراسری شیء از این استفاده کنید:

```ts
import "@type-check/guards/register-global-object.mjs";
```

در این صورت متد زیر به‌صورت سراسری در دسترس خواهد بود:

- `Type.isTrue(value)`

## تحلیل توابع

در اینجا یک تحلیل جدولی از خروجی حاصل از وارد کردن پارامترهای مختلف در توابع مستند شده
است: [isTrue](‎../_analysis/isTrue.md‎)

<br>

---

<small>این فایل در 30 January 2026 at 13:45:54 (UTC) با استفاده از *
*[markdown-documentation-generator](https://github.com/roland-milto/markdown-documentation-generator)** توسط *
*[Roland Milto](https://roland-milto.de/)** ایجاد شد.</small>