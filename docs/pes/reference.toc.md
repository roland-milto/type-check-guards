# ارجاعات تابع

## توابع

| تابع                                        | توضیحات                                                                                                                                                                                                                      |
|---------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| [areArrays](areArrays.md)                   | `areArrays` تعیین می‌کند آیا یک مقدار، یک آرایهٔ دوبعدیِ پر (غیرخالی) است که همهٔ آیتم‌های آن آرایه هستند یا نه.                                                                                                             |
| [areBigInts](areBigInts.md)                 | `areBigInts` تعیین می‌کند آیا یک مقدار، آرایه‌ای غیرخالی است که فقط شامل مقادیر `bigint` باشد یا نه.                                                                                                                         |
| [areBinaries](areBinaries.md)               | بررسی می‌کند آیا مقدار ارائه‌شده یک آرایهٔ غیرخالی از رشته‌های دودویی معتبر است و فقط در صورتی `true` برمی‌گرداند که همهٔ آیتم‌ها از اعتبارسنجی عبور کنند.                                                                   |
| [areBooleans](areBooleans.md)               | `areBooleans` بررسی می‌کند آیا یک آرایهٔ غیرخالیِ داده‌شده فقط شامل مقادیر بولی است یا نه؛ اگر باشد `true` و در غیر این صورت `false` برمی‌گرداند.                                                                            |
| [areBuffers](areBuffers.md)                 | `areBuffers` بررسی می‌کند آیا مقدارِ ارائه‌شده یک آرایهٔ غیرخالی و پُر است که در آن هر عنصر یک `Buffer` باشد؛ در این صورت `true` و در غیر این صورت `false` برمی‌گرداند.                                                      |
| [areDates](areDates.md)                     | `areDates` تعیین می‌کند آیا یک آرایهٔ داده‌شده پر است و فقط شامل اشیای `Date` است یا نه؛ و تنها وقتی `true` برمی‌گرداند که همهٔ عناصر تاریخ‌های معتبر باشند.                                                                 |
| [areDecimals](areDecimals.md)               | بررسی می‌کند آیا همهٔ عناصر یک آرایه عدد اعشاری هستند و آرایه پُر است یا نه، و `true` یا `false` برمی‌گرداند.                                                                                                                |
| [areEqual](areEqual.md)                     | `areEqual` بررسی می‌کند آیا همهٔ عناصر یک آرایه با یک مقدار مورد انتظار مشخص برابر هستند یا نه، و فقط برای آرایه‌های غیرخالی که هر آیتم مطابقت دارد `true` برمی‌گرداند.                                                      |
| [areErrors](areErrors.md)                   | بررسی می‌کند آیا یک آرایه غیرخالی است و فقط شامل اشیای `Error` است یا نه، و `true` یا `false` برمی‌گرداند.                                                                                                                   |
| [areFalse](areFalse.md)                     | `areFalse` بررسی می‌کند آیا همهٔ عناصر در یک آرایهٔ ارائه‌شده به‌طور سخت‌گیرانه مقدار بولی `false` هستند یا نه.                                                                                                              |
| [areFilledArrays](areFilledArrays.md)       | `areFilledArrays` بررسی می‌کند که آیا یک آرایهٔ دوبعدی خالی نیست و همهٔ زیرآرایه‌های آن خالی نیستند.                                                                                                                         |
| [areFinite](areFinite.md)                   | `areFinite` بررسی می‌کند آیا یک مقدار، آرایه‌ای غیرخالی است که عناصر آن همگی اعداد متناهی هستند یا نه؛ اگر چنین باشد `true` و در غیر این صورت `false` برمی‌گرداند.                                                           |
| [areFloats](areFloats.md)                   | `areFloats` بررسی می‌کند آیا یک آرایهٔ داده‌شده پر است و همهٔ عناصرش float هستند یا نه.                                                                                                                                      |
| [areHexadecimals](areHexadecimals.md)       | بررسی می‌کند آیا همهٔ عناصرِ یک آرایه رشته‌های هگزادسیمال هستند یا نه، و فقط برای آرایه‌های غیرخالی که هر مورد معتبر باشد `true` برمی‌گرداند.                                                                                |
| [areIndexesFound](areIndexesFound.md)       | `areIndexesFound` بررسی می‌کند آیا یک مقدار، آرایه‌ای غیرخالی است که عناصر آن همگی ایندکس‌های معتبر هستند یا نه؛ اگر باشند `true` و در غیر این صورت `false` برمی‌گرداند.                                                     |
| [areIntegers](areIntegers.md)               | `areIntegers` تعیین می‌کند آیا همهٔ عناصر یک آرایهٔ داده‌شده عدد صحیح هستند یا نه؛ اگر باشند `true` و در غیر این صورت `false` برمی‌گرداند.                                                                                   |
| [areJson](areJson.md)                       | بررسی می‌کند آیا همهٔ عناصرِ یک آرایه رشته‌های JSON هستند یا نه؛ فقط اگر آرایه پر باشد و هر مورد JSON معتبر باشد `true` برمی‌گرداند؛ در غیر این صورت `false` برمی‌گرداند.                                                    |
| [areMaps](areMaps.md)                       | `areMaps` تعیین می‌کند که آیا یک آرایهٔ داده‌شده غیرخالی است و همهٔ عناصر آن نمونه‌های `Map` هستند یا نه.                                                                                                                    |
| [areNaNs](areNaNs.md)                       | `areNaNs` بررسی می‌کند آیا همهٔ عناصرِ یک آرایه `NaN` هستند و فقط در صورتی `true` برمی‌گرداند که هر عنصر `NaN` باشد.                                                                                                         |
| [areNullOrUndefined](areNullOrUndefined.md) | بررسی می‌کند آیا همهٔ عناصر در آرایهٔ داده‌شده `null` یا `undefined` هستند یا نه.                                                                                                                                            |
| [areNumbers](areNumbers.md)                 | `areNumbers` بررسی می‌کند که آیا یک مقدار، آرایه‌ای غیرخالی است که همهٔ عناصر آن عدد هستند یا نه.                                                                                                                            |
| [areNumerics](areNumerics.md)               | `areNumerics` بررسی می‌کند آیا یک مقدار، آرایه‌ای غیرخالی است که همهٔ عناصر آن عددی هستند یا نه.                                                                                                                             |
| [areObjects](areObjects.md)                 | `areObjects` بررسی می‌کند آیا یک آرایهٔ پُرِ ارائه‌شده فقط شامل اشیاء است یا نه.                                                                                                                                             |
| [areOctals](areOctals.md)                   | `areOctals` تعیین می‌کند آیا مقدار ارائه‌شده یک آرایهٔ غیرخالی از رشته‌های هشت‌هشتی معتبر است یا نه.                                                                                                                         |
| [areOfType](areOfType.md)                   | بررسی می‌کند آیا همهٔ عناصر `array` داده‌شده از `type` مشخص‌شده هستند یا نه.                                                                                                                                                 |
| [areOneOfType](areOneOfType.md)             | `areOneOfType` بررسی می‌کند آیا همهٔ عناصر در یک آرایهٔ غیرخالی، یکی از نوع‌های مشخص‌شده در زمان اجرا هستند یا نه.                                                                                                           |
| [arePlainObjects](arePlainObjects.md)       | بررسی می‌کند آیا همهٔ عناصر یک آرایه شیء ساده هستند یا نه، و فقط اگر هر عنصر واجد شرایط باشد `true` برمی‌گرداند.                                                                                                             |
| [arePrimitives](arePrimitives.md)           | `arePrimitives` بررسی می‌کند که آیا همهٔ عناصر در یک آرایهٔ ارائه‌شده و غیرخالی از نوع‌های اولیه هستند یا نه.                                                                                                                |
| [arePromises](arePromises.md)               | `arePromises` تعیین می‌کند که آیا همهٔ عناصرِ یک آرایه نمونه‌های `Promise` هستند یا نه.                                                                                                                                      |
| [areRegExes](areRegExes.md)                 | `areRegExes` بررسی می‌کند آیا یک مقدار، آرایه‌ای پُر است که فقط شامل اشیای `RegExp` باشد.                                                                                                                                    |
| [areSets](areSets.md)                       | بررسی می‌کند آیا یک آرایهٔ غیرخالیِ داده‌شده فقط شامل نمونه‌های `Set` است یا نه؛ اگر باشد `true` و در غیر این صورت `false` برمی‌گرداند.                                                                                      |
| [areStreams](areStreams.md)                 | `areStreams` تعیین می‌کند آیا یک مقدار، آرایه‌ای پُر است که در آن هر عنصر یک `Stream` است یا نه.                                                                                                                             |
| [areStrings](areStrings.md)                 | `areStrings` بررسی می‌کند که آیا یک آرایه غیرخالی است و همهٔ عناصر آن رشته هستند یا نه، و فقط در همان حالت `true` برمی‌گرداند.                                                                                               |
| [areSymbols](areSymbols.md)                 | بررسی می‌کند آیا ورودی یک آرایهٔ پُر (غیرخالی) است که عناصر آن همگی نماد (symbol) هستند یا نه، و `true` یا `false` برمی‌گرداند.                                                                                              |
| [areTrue](areTrue.md)                       | بررسی می‌کند آیا یک آرایهٔ غیرخالی فقط شامل مقادیر بولی `true` است یا نه.                                                                                                                                                    |
| [areUndefined](areUndefined.md)             | `areUndefined` بررسی می‌کند که آیا هر عنصر در یک آرایهٔ ارائه‌شده `undefined` است یا نه.                                                                                                                                     |
| [areValidDates](areValidDates.md)           | مشخص می‌کند آیا یک آرایه غیرخالی است و به‌طور کامل از اشیای معتبر `Date` تشکیل شده است یا نه.                                                                                                                                |
| [areWeakMaps](areWeakMaps.md)               | `areWeakMaps` بررسی می‌کند که آیا یک مقدار، آرایه‌ای غیرخالی است که هر عنصر آن یک `WeakMap` باشد؛ فقط در همان حالت `true` و در غیر این صورت `false` برمی‌گرداند.                                                             |
| [areWeakSets](areWeakSets.md)               | بررسی می‌کند آیا ورودی یک آرایهٔ غیرخالی است که هر عنصر آن یک `WeakSet` باشد و فقط در همان حالت `true` برمی‌گرداند.                                                                                                          |
| [getTypeOf](getTypeOf.md)                   | `getTypeOf` برای یک مقدار داده‌شده، یک برچسب نوع دقیق و قابل‌خواندن برای انسان برمی‌گرداند؛ شامل نوع‌های عددی پالایش‌شده و گونه‌های مشخصِ اشیاء.                                                                             |
| [isArray](isArray.md)                       | `isArray` بررسی می‌کند آیا مقدار داده‌شده یک آرایه است و اگر باشد `true` و در غیر این صورت `false` برمی‌گرداند.                                                                                                              |
| [isBigInt](isBigInt.md)                     | `isBigInt` بررسی می‌کند آیا یک مقدارِ داده‌شده از نوع `bigint` است یا نه؛ برای مقادیر اولیهٔ BigInt مقدار `true` و در غیر این صورت `false` برمی‌گرداند.                                                                      |
| [isBinary](isBinary.md)                     | `isBinary` تعیین می‌کند آیا یک مقدار، رشتهٔ دودویی است (به‌صورت اختیاری با پیشوند `0b`/`0B`) و `true` یا `false` برمی‌گرداند.                                                                                                |
| [isBoolean](isBoolean.md)                   | مشخص می‌کند آیا یک مقدار داده‌شده از نوع `boolean` است یا نه.                                                                                                                                                                |
| [isBuffer](isBuffer.md)                     | بررسی می‌کند آیا یک مقدار `Buffer` در Node.js است و `true` یا `false` برمی‌گرداند.                                                                                                                                           |
| [isDate](isDate.md)                         | `isDate` تعیین می‌کند آیا مقدار ارائه‌شده یک `Date` است یا نه؛ برای نمونه‌های `Date` مقدار `true` و در غیر این صورت `false` برمی‌گرداند.                                                                                     |
| [isDecimal](isDecimal.md)                   | `isDecimal` بررسی می‌کند آیا یک مقدار، نمایشِ رشته‌ایِ یک عدد اعشاری است که هیچ فاصلهٔ سفیدِ ابتدا/انتها ندارد و قالب اعشاریِ معتبری دارد.                                                                                   |
| [isEmpty](isEmpty.md)                       | مشخص می‌کند آیا یک مقدار داده‌شده خالی است یا نه؛ برای `null`، `undefined`، رشته‌های خالی/فقط فاصله، آرایه‌های خالی، `Map`/`Set` خالی، یا آبجکت‌هایی که هیچ ویژگی قابل‌شمارشِ متعلق به خود ندارند، مقدار `true` برمی‌گرداند. |
| [isError](isError.md)                       | بررسی می‌کند آیا `value` داده‌شده نمونه‌ای از `Error` است یا نه.                                                                                                                                                             |
| [isFalse](isFalse.md)                       | `isFalse` بررسی می‌کند آیا یک مقدار داده‌شده به‌صورت سخت‌گیرانه با مقدار بولیِ literal `false` برابر است یا نه.                                                                                                              |
| [isFilledArray](isFilledArray.md)           | بررسی می‌کند آیا `value` یک آرایه با حداقل یک عنصر است و `true` یا `false` برمی‌گرداند.                                                                                                                                      |
| [isFinite](isFinite.md)                     | مشخص می‌کند آیا `value` داده‌شده یک `number` متناهی است یا نه.                                                                                                                                                               |
| [isFloat](isFloat.md)                       | `isFloat` تعیین می‌کند آیا `value` داده‌شده یک عدد ممیز شناورِ متناهی است (یک `number` که عدد صحیح نیست).                                                                                                                    |
| [isHexadecimal](isHexadecimal.md)           | `isHexadecimal` بررسی می‌کند که آیا یک مقدار داده‌شده یک لیترال رشته‌ای هگزادسیمال با پیشوند اجباری `0x`/`0X` است یا نه.                                                                                                     |
| [isIndexFound](isIndexFound.md)             | `isIndexFound` تعیین می‌کند آیا یک مقدار داده‌شده یک عدد صحیح نامنفی است و نشان می‌دهد که یک اندیس پیدا شده است.                                                                                                             |
| [isInteger](isInteger.md)                   | مشخص می‌کند آیا `value` داده‌شده یک عدد صحیحِ امن است یا نه.                                                                                                                                                                 |
| [isMap](isMap.md)                           | بررسی می‌کند آیا `value` داده‌شده یک `Map` است یا نه؛ اگر باشد `true` و در غیر این صورت `false` برمی‌گرداند.                                                                                                                 |
| [isNaN](isNaN.md)                           | مشخص می‌کند آیا `value` ارائه‌شده یک `NaN` از نوع `number` است، بدون تبدیل کردن رشته‌ها.                                                                                                                                     |
| [isNull](isNull.md)                         | مشخص می‌کند آیا `value` ارائه‌شده برابر با `null` است یا نه.                                                                                                                                                                 |
| [isNullOrUndefined](isNullOrUndefined.md)   | بررسی می‌کند که آیا یک مقدار داده‌شده `null` یا `undefined` است یا نه.                                                                                                                                                       |
| [isNumber](isNumber.md)                     | `isNumber` بررسی می‌کند آیا یک مقدار، عددی متناهی و غیرِ `NaN` است یا نه.                                                                                                                                                    |
| [isNumeric](isNumeric.md)                   | `isNumeric` با بررسی نوعِ حل‌شدهٔ `value` در برابر `NUMERIC_TYPES` تعیین می‌کند که آیا `value` داده‌شده عددی در نظر گرفته می‌شود یا نه.                                                                                      |
| [isObject](isObject.md)                     | مشخص می‌کند آیا `value` داده‌شده یک `object` است (به‌جز `null`).                                                                                                                                                             |
| [isOctal](isOctal.md)                       | مشخص می‌کند آیا یک مقدار، رشتهٔ لیترال هشت‌هشتی (octal) معتبر است یا نه (مثلاً `0o755`).                                                                                                                                     |
| [isOfType](isOfType.md)                     | مشخص می‌کند آیا `value` داده‌شده با یک رشتهٔ نوع مشخص مطابقت دارد یا نه؛ برای انواع اولیه از `typeof` و برای انواع پیچیده از روش جایگزین استفاده می‌کند.                                                                     |
| [isOneOfType](isOneOfType.md)               | `isOneOfType` تعیین می‌کند آیا `value` داده‌شده با دست‌کم یکی از رشته‌های نوع ارائه‌شده مطابقت دارد یا نه؛ اگر هر تطابقی پیدا شود `true` و در غیر این صورت `false` برمی‌گرداند.                                              |
| [isPlainObject](isPlainObject.md)           | بررسی می‌کند آیا `value` داده‌شده یک شیء ساده است و اگر باشد `true` و در غیر این صورت `false` برمی‌گرداند.                                                                                                                   |
| [isPrimitive](isPrimitive.md)               | `isPrimitive` تعیین می‌کند آیا یک مقدار داده‌شده یک مقدار اولیه است (`null`، `undefined`، `boolean`، `number`، `string`، `bigint`، `symbol`).                                                                                |
| [isPromise](isPromise.md)                   | مشخص می‌کند آیا یک مقدار داده‌شده یک `Promise` است یا نه.                                                                                                                                                                    |
| [isRegEx](isRegEx.md)                       | مشخص می‌کند آیا مقدار ارائه‌شده یک نمونهٔ `RegExp` است یا نه.                                                                                                                                                                |
| [isSet](isSet.md)                           | مشخص می‌کند آیا یک مقدار داده‌شده یک `Set` است یا نه.                                                                                                                                                                        |
| [isStream](isStream.md)                     | `isStream` بررسی می‌کند که آیا یک مقدار داده‌شده یک شیء استریم است (شبیه استریم در Node.js، `ReadableStream` یا `WritableStream`).                                                                                           |
| [isString](isString.md)                     | `isString` تعیین می‌کند که آیا یک مقدار داده‌شده رشته است یا نه.                                                                                                                                                             |
| [isSymbol](isSymbol.md)                     | `isSymbol` تعیین می‌کند آیا یک مقدار داده‌شده از نوع `symbol` است یا نه؛ برای symbolها `true` و در غیر این صورت `false` برمی‌گرداند.                                                                                         |
| [isTrue](isTrue.md)                         | `isTrue` تعیین می‌کند آیا یک مقدار داده‌شده به‌طور سخت‌گیرانه با `true` برابر است یا نه.                                                                                                                                     |
| [isUndefined](isUndefined.md)               | بررسی می‌کند آیا یک مقدار داده‌شده `undefined` است یا نه.                                                                                                                                                                    |
| [isValidDate](isValidDate.md)               | `isValidDate` بررسی می‌کند که آیا یک مقدارِ داده‌شده یک شیء `Date` معتبر است یا نه و فقط برای تاریخ‌های واقعی و غیرنامعتبر `true` برمی‌گرداند.                                                                               |
| [isWeakMap](isWeakMap.md)                   | مشخص می‌کند آیا `value` داده‌شده یک نمونهٔ `WeakMap` است یا نه.                                                                                                                                                              |
| [isWeakSet](isWeakSet.md)                   | مشخص می‌کند آیا `value` داده‌شده یک `WeakSet` از اشیاء است یا نه.                                                                                                                                                            |

## توابع مقایسه‌شده

| توابع | توضیحات |
|-------|---------|
